---
phase: 01-cypherpunk-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Cargo.toml
  - crates/hunch-mint-spike/Cargo.toml
  - crates/hunch-mint-spike/src/lib.rs
  - crates/hunch-mint-spike/src/main.rs
  - crates/hunch-mint-spike/README.md
  - crates/hunch-mint-spike/tests/e2e_signet.rs
  - crates/hunch-oracle-spike/Cargo.toml
  - crates/hunch-oracle-spike/src/lib.rs
  - crates/hunch-oracle-spike/README.md
  - crates/hunch-oracle-spike/tests/frost_dkg_3of5.rs
  - spikes/nut-ctf/PATH-A-VALIDATION.md
  - spikes/nut-ctf/outreach-log.md
  - spikes/nut-ctf/README.md
  - spikes/frost-dkg/PLAYBOOK.md
  - spikes/frost-dkg/ceremony-transcripts/.gitkeep
  - spikes/lightning-dlc/GO-NOGO.md
  - docs/SPIKE-01-nut-ctf-decision.md
  - docs/SPIKE-04-lightning-dlc-assessment.md
  - docs/playbooks/FROST-DKG-3of5.md
autonomous: false
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04]
requirements_addressed: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04]
tags: [hunch, spike, nut-ctf, cashu, dlc, frost, lightning, signet, mutinynet]

parallelism_notes:
  - "SPIKE-01 outreach (Task 1) — no workspace dep, can start day 1 of Plan 01"
  - "SPIKE-04 NO-GO assessment (Task 4) — no workspace dep, can start day 1"
  - "SPIKE-02 (Task 2) — hard-depends on Cargo workspace from Plan 01"
  - "SPIKE-03 (Task 3) — soft-depends on Cargo workspace from Plan 01"
  - "Guidance only: per coarse granularity setting, this plan stays as one cohesive plan. Executor MAY interleave Task 1/4 with Plan 01 progress."

must_haves:
  truths:
    - "SPIKE-01 produces a written Path A/B/C decision document with maintainer-contact transcripts"
    - "SPIKE-02 cargo binary runs end-to-end on signet (Mutinynet): create market → bet → resolve → withdraw — autonomously after the faucet funding checkpoint completes"
    - "SPIKE-03 cargo test executes a full 3-of-5 FROST DKG ceremony + signing + asserts group pubkey agreement"
    - "SPIKE-03 playbook documents init / DKG / multi-round signing / reset-rotate procedures; ceremony run at least 3 times"
    - "SPIKE-04 written go/no-go assessment exists with NO-GO recommendation + v2 reconsideration triggers + primary sources whose URLs return 200 at write-time"
    - "hunch-mint-spike and hunch-oracle-spike are clearly marked -spike (not for production)"
    - "Trail of Bits Feb 2024 Pedersen DKG coefficient-vector-length check is verified at the application layer in SPIKE-03"
  artifacts:
    - path: "crates/hunch-mint-spike/Cargo.toml"
      provides: "Throwaway prototype crate against NUT-CTF (PR #337)"
      contains: "spike"
    - path: "crates/hunch-mint-spike/tests/e2e_signet.rs"
      provides: "End-to-end signet test harness (create → bet → resolve → withdraw)"
      min_lines: 30
    - path: "crates/hunch-mint-spike/README.md"
      provides: "PROTOTYPE ONLY warning + reproduction instructions"
      contains: "PROTOTYPE ONLY"
    - path: "crates/hunch-oracle-spike/tests/frost_dkg_3of5.rs"
      provides: "FROST 3-of-5 DKG + signing test"
      min_lines: 40
    - path: "spikes/nut-ctf/PATH-A-VALIDATION.md"
      provides: "PR #337 deep-read + maintainer-contact validation outcome"
      contains: "PR #337"
    - path: "spikes/nut-ctf/outreach-log.md"
      provides: "Log of outreach to joemphilips, conduition, Calle"
      contains: "joemphilips"
    - path: "docs/SPIKE-01-nut-ctf-decision.md"
      provides: "Final Path A/B/C decision + rationale + transcript references"
      contains: "Path A"
    - path: "docs/SPIKE-04-lightning-dlc-assessment.md"
      provides: "Lightning-DLC NO-GO assessment with v2 reconsideration triggers + URL-liveness-verified primary sources"
      contains: "NO-GO"
    - path: "docs/playbooks/FROST-DKG-3of5.md"
      provides: "Operational playbook for 3-of-5 ceremony tested 3+ times on signet"
      contains: "Trail of Bits"
    - path: "spikes/frost-dkg/PLAYBOOK.md"
      provides: "Operational playbook (mirror of docs/playbooks/FROST-DKG-3of5.md)"
      contains: "init"
  key_links:
    - from: "crates/hunch-mint-spike/tests/e2e_signet.rs"
      to: "https://faucet.mutinynet.com"
      via: "Signet faucet for test sats"
      pattern: "mutinynet"
    - from: "crates/hunch-mint-spike/Cargo.toml"
      to: "cdk (v0.14+), rust-dlc (v0.7+), ddk (v0.0.17 alpha), ldk-node (v0.4+), bdk_wallet (v1.0+)"
      via: "Cargo dependencies — pinned exact versions"
      pattern: "cdk"
    - from: "crates/hunch-oracle-spike/Cargo.toml"
      to: "frost-secp256k1-tr v2.2+ (post-TOB-fix), nostr-sdk v0.34+"
      via: "Cargo dependencies"
      pattern: "frost-secp256k1-tr"
    - from: "docs/SPIKE-01-nut-ctf-decision.md"
      to: "joemphilips Nostr npub + conduition Nostr npub + Calle Nostr npub"
      via: "Outreach transcript references"
      pattern: "npub1"
    - from: "docs/SPIKE-04-lightning-dlc-assessment.md"
      to: "atomic.finance acquired by Lygos (Aug 2025) + Crypto Garage rust-dlc Lightning fork (not production-ready)"
      via: "Primary source citations (verified live via curl at write-time)"
      pattern: "Lygos\\|Crypto Garage"
---

<objective>
De-risk the three biggest technical unknowns before any Phase 2 production commits:

1. **SPIKE-01** validates the NUT-DLC Path A pivot to NUT-CTF (PR #337) via deep PR read + maintainer outreach to joemphilips / conduition / Calle.
2. **SPIKE-02** builds a working signet (Mutinynet) end-to-end prototype against NUT-CTF (PR #337): user creates market → bets via Lightning → oracle Schnorr-signs outcome → DLC CET broadcasts → user withdraws. **The faucet funding step is a dedicated checkpoint:human-action; the e2e test runs autonomously after funding completes.**
3. **SPIKE-03** runs the FROST 3-of-5 DKG ceremony on signet + signing + reset/rotate playbook; verifies Trail of Bits Feb 2024 coefficient-vector-length defense at the application layer.
4. **SPIKE-04** documents Lightning-DLC NO-GO assessment (atomic.finance pivoted Aug 2025; Crypto Garage rust-dlc Lightning self-flagged not production-ready). **Primary source URLs verified live via curl at write-time.**

Purpose: Phase 1 ROADMAP success criteria #2 (NUT-DLC working prototype) and #3 (FROST 3-of-5 ceremony) are existential gates — if NUT-CTF won't fit Hunch's permissionless multi-bettor model, Phase 2 architecture must be rethought before any production code commits.

Output: 4 requirements closed (SPIKE-01..04). Two throwaway prototype crates (`hunch-mint-spike`, `hunch-oracle-spike`) clearly marked PROTOTYPE ONLY. Three written deliverables (SPIKE-01 decision, SPIKE-04 assessment, FROST playbook). Two cargo tests that run autonomously after the SPIKE-02 faucet checkpoint: `cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture` and `cargo test -p hunch-oracle-spike --test frost_dkg_3of5`.

When SPIKE-02 succeeds, this plan also unblocks HIP-3 Status: Final transition (tracked in `.planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md` — created here in Task 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cypherpunk-foundation/01-CONTEXT.md
@.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md
@.planning/phases/01-cypherpunk-foundation/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@Cargo.toml

<interfaces>
<!-- Locked decisions from CONTEXT.md (D-01, D-03, D-08) — non-negotiable -->

D-01 NUT-DLC strategy: Path A — NUT-CTF (PR #337). SPIKE-01 must contact joemphilips, conduition, Calle. SPIKE-02 implements signet prototype against PR #337.
D-03 Lightning-DLC NO-GO confirmed. SPIKE-04 is written assessment, NOT a prototype.
D-08 Stack: frost-secp256k1-tr v2.2+, rust-dlc + DDK alpha, cdk v0.14+, ldk-node v0.4+, bdk_wallet v1.0+, nostr-sdk v0.34+.

<!-- Existing workspace from Plan 01 (Wave 1): -->
- Cargo.toml workspace with 6 production crates (hunch-protocol, hunch-mint, hunch-oracle, hunch-relay, hunch-matcher, hunch-cli)
- Add 2 spike crates: hunch-mint-spike, hunch-oracle-spike (RESEARCH §7.4 Wave 0 gaps, Pitfall E discipline)

<!-- Mutinynet signet (per CONTEXT.md Claude's Discretion + RESEARCH §3.4): -->
- Faucet: https://faucet.mutinynet.com
- Block time: 30 seconds (vs vanilla signet 10 min) — much faster spike iteration
- Esplora: https://mutinynet.com (block explorer + API)

<!-- Outreach targets for SPIKE-01 (per D-01): -->
- joemphilips — author of NUT-CTF PR #337 — find npub via GitHub profile or cashu Telegram; primary contact (collaboration on PR #337)
- conduition — author of CLOSED NUT-DLC PR #128 — npub via conduition.io ; secondary (would they support Hunch reviving #128 if #337 stalls?)
- Calle — cashubtc maintainer — npub via cashu.space; tertiary (overall Cashu protocol direction)

<!-- FROST DKG ceremony skeleton (per RESEARCH §12): -->
- frost-secp256k1-tr v2.2+ API: `keys::dkg::part1`, `part2`, `part3` (finalization)
- Assert `package.commitment().0.len() == MIN_SIGNERS` at every round (Trail of Bits Feb 2024 fix verification at application layer per CLAUDE.md prudence)
- 5 simulated participants in a single process; production ceremony uses 5 separate processes per RESEARCH §7

<!-- Cargo deps for hunch-mint-spike (pinned exact, verify via cargo search before commit per RESEARCH §11 pre-Wave-0): -->
- cdk = "=0.14" (or latest 0.14.x)
- rust-dlc = "=0.7" or latest stable
- ddk = "=0.0.17" (alpha — pin exact)
- ldk-node = "=0.4" (or latest 0.4.x post-griefing fix)
- bdk_wallet = "=1.0"
- bitcoin = "=0.32"
- tokio, serde, serde_json, anyhow, thiserror (workspace deps)

<!-- Cargo deps for hunch-oracle-spike: -->
- frost-secp256k1-tr = "=2.2"  (assert TOB fix)
- nostr-sdk = "=0.34"
- rand = "0.8"
- tokio, serde, serde_json, anyhow, thiserror

<!-- SPIKE-04 NO-GO sources (per RESEARCH §5 + CONTEXT.md D-03) — URLs MUST return 200 at write-time (verified via curl): -->
- https://blockspace.media/insight/lygos-finance-acquires-atomic-finance-to-launch-non-custodial-dlc-powered-bitcoin-loans/ — Lygos acquires atomic.finance Aug 2025
- https://atomic.finance/blog/an-atomic-pivot/ — atomic pivot blog
- https://medium.com/crypto-garage/dlc-on-lightning-cb5d191f6e64 — Crypto Garage "not production-ready"
- https://www.tftc.io/issue-1286/ — first mainnet DLC on Lightning Nov 2022 (last public update)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SPIKE-01 — Deep-read NUT-CTF PR #337, outreach to joemphilips/conduition/Calle, write Path A validation decision</name>
  <files>
    spikes/nut-ctf/README.md,
    spikes/nut-ctf/PATH-A-VALIDATION.md,
    spikes/nut-ctf/outreach-log.md,
    docs/SPIKE-01-nut-ctf-decision.md
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §3 in full (NUT-DLC Deep Dive: PR #128 closed state, PR #337 architectural deltas, Path A/B/C decision matrix, custodial-promise fallback details, signet faucet availability)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-01 locked Path A)
    - https://github.com/cashubtc/nuts/pull/337 — read the entire PR description + all comments + all 3 sub-NUT files (NUT-CTF base, NUT-CTF-split-merge, NUT-CTF-numeric)
    - https://github.com/cashubtc/nuts/pull/128 — read the closure comment + the last reviewer comments (gudnuf Nov 2024) to understand why bilateral DLC stalled
    - https://github.com/cashubtc/nuts/pull/127 — the dependency PR (spending condition trees / NUT-10 multiplex) that PR #128 built on; merged status
    - https://conduition.io/code/cashu-disclosure/ — conduition's Jul 2025 disclosure (continuing engagement post-PR #128 closure)
    - docs/HIP-3.md (Plan 02 deliverable — verify HIP-3 already references PR #337 + is Status: Draft per Plan 02 Task 1)
  </read_first>
  <action>
    PART A — Deep PR read (autonomous):

    1. Fetch and read PR #337 in full. Extract:
       - Architectural primitives (conditional token, oracle-agnostic mint, NUT-CTF-split-merge for secondary market)
       - Open questions in the PR comments + maintainer responses
       - Compatibility surface with existing Cashu NUTs (00-15 + DLEQ)
       - Open issues that would block Hunch's signet prototype (SPIKE-02)
    2. Fetch and read PR #128's closure comment + the Nov 2024 reviewer thread. Extract:
       - Why bilateral architecture didn't capture maintainer momentum
       - Whether any of the 21 commits' designs map to Hunch use cases that NUT-CTF might miss
    3. Fetch PR #127 (merged dependency for #128). Note: if NUT-CTF doesn't require #127, this is a positive signal (less upstream baggage).

    Document findings in `spikes/nut-ctf/PATH-A-VALIDATION.md`:

    ```markdown
    # PATH-A Validation: NUT-CTF (PR #337) deep read

    **Date:** 2026-05-XX (fill at write time)
    **Author:** Silex <npub1...>
    **PR #337 commit SHA at read time:** <sha>

    ## NUT-CTF base spec
    [summary of conditional token primitive + lifecycle]

    ## NUT-CTF-split-merge
    [summary of position trading via Cashu primitives]

    ## NUT-CTF-numeric
    [summary; deferred to v2 per HIP-2]

    ## Architectural fit for Hunch
    [Strong / Medium / Weak per RESEARCH §3.2 — restate the matrix row with empirical PR-read backing]

    ## Hard dependencies on other NUTs
    [list — verify each is ratified or PR-tracked]

    ## Known blockers for SPIKE-02 prototype
    [list — anything that requires upstream coordination before signet test can pass]

    ## PR #128 archaeological notes
    [closure reason, last reviewer thread, whether any of conduition's 21 commits' designs offer insight NUT-CTF lacks]

    ## Verdict (preliminary, pre-maintainer-contact)
    [Path A / Path B / Path C with one-paragraph rationale]
    ```

    PART B — Maintainer outreach (autonomous send, then wait for responses):

    For each of joemphilips, conduition, Calle:
    1. Find their Nostr npub. Check:
       - GitHub profile bio for npub or nostr.json link
       - cashu.space contributor listing
       - conduition.io for conduition
       - Nostr search for handle via `wss://relay.damus.io`
    2. Send NIP-44 gift-wrapped DM using `nostr-sdk` (Rust CLI or `bun nostr-tools` script). Use the project pseudonym Nostr key from Plan 02 Task 2 (`HUNCH_NSEC` env var).

    DM templates: see RESEARCH §6 for tailored joemphilips / conduition / Calle templates. Each tailored DM references HIP-3's naddr1 (from Plan 02 Task 2) + asks for upstream collaboration / merge-timeline / fallback signal.

    Log each outreach attempt in `spikes/nut-ctf/outreach-log.md` (timestamp, channel, npub, response status).

    PART C — Wait window + final decision (response window 1-3 weeks; Task closes when decision logged):

    Periodically check Nostr DMs for replies. When replies arrive (or after 2 weeks of no response from a target), update outreach-log.md with response excerpt + npub-signed event ID.

    Write `docs/SPIKE-01-nut-ctf-decision.md` with Status: FINAL, Decision: Path A, rationale, maintainer signal section, implications for SPIKE-02, fallback plan (Path B if SPIKE-02 reveals NUT-CTF cannot support Hunch's flow in 4 weeks).

    Create `spikes/nut-ctf/README.md` indexing the SPIKE-01 + SPIKE-02 artifacts.

    Commit: `spike(01-foundation): SPIKE-01 NUT-CTF (PR #337) Path A validation + maintainer outreach (D-01)`.
  </action>
  <verify>
    <automated>
      bash -c "test -f spikes/nut-ctf/PATH-A-VALIDATION.md && grep -q 'PR #337' spikes/nut-ctf/PATH-A-VALIDATION.md && test -f spikes/nut-ctf/outreach-log.md && grep -q 'joemphilips' spikes/nut-ctf/outreach-log.md && grep -q 'conduition' spikes/nut-ctf/outreach-log.md && grep -q 'Calle' spikes/nut-ctf/outreach-log.md && test -f docs/SPIKE-01-nut-ctf-decision.md && grep -q 'Path A' docs/SPIKE-01-nut-ctf-decision.md && grep -q 'NUT-CTF' docs/SPIKE-01-nut-ctf-decision.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - `spikes/nut-ctf/PATH-A-VALIDATION.md` exists with deep-read findings on PR #337 (architectural primitives, dependencies, blockers, archaeology of PR #128).
    - `spikes/nut-ctf/outreach-log.md` exists with at least 3 outreach attempts logged (joemphilips, conduition, Calle), each with channel + timestamp + recipient npub.
    - `docs/SPIKE-01-nut-ctf-decision.md` exists with status FINAL, decision = Path A, rationale section, maintainer-signal section (responses or "no response after 2 weeks" notation), implications for SPIKE-02, fallback plan, sources.
    - `spikes/nut-ctf/README.md` exists indexing the spike artifacts.
    - No real-name leakage in any artifact (pseudonym discipline D-05).
  </acceptance_criteria>
  <done>SPIKE-01 closed. Path A is empirically validated against PR #337 (or fallback documented). SPIKE-02 in Task 2 can pin against PR #337's commit SHA and proceed. Reviewer feedback from maintainers (if received) is logged in outreach-log.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SPIKE-02 — Build hunch-mint-spike with NUT-CTF (PR #337) integration + signet (Mutinynet) end-to-end flow: create market → bet → resolve → withdraw. Faucet funding is a dedicated checkpoint; e2e test runs autonomously after.</name>
  <files>
    Cargo.toml,
    crates/hunch-mint-spike/Cargo.toml,
    crates/hunch-mint-spike/src/lib.rs,
    crates/hunch-mint-spike/src/main.rs,
    crates/hunch-mint-spike/README.md,
    crates/hunch-mint-spike/tests/e2e_signet.rs,
    .planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md
  </files>
  <read_first>
    - spikes/nut-ctf/PATH-A-VALIDATION.md (Task 1 output) — pin against PR #337 commit SHA noted here
    - docs/SPIKE-01-nut-ctf-decision.md (Task 1 output) — verify Path A is final
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §3.3 (Compatible Cashu mint implementations — CDK Rust v0.14+) and §3.4 (signet faucet — Mutinynet preferred) and §11 (full stack)
    - https://github.com/cashubtc/cdk — current CDK v0.14.x API surface (NUT-04 mint quote, NUT-05 melt quote, NUT-12 DLEQ, NUT-22 reserves; trait extension points for adding NUT-CTF)
    - https://github.com/cashubtc/nuts/pull/337 — pin against the SHA from Task 1
    - https://github.com/bennyhodl/dlcdevkit — DDK alpha API (current v0.0.17 or latest)
    - https://faucet.mutinynet.com — confirm faucet reachable + sat amount per drip
    - existing Cargo.toml (root, from Plan 01 Task 2) — adding hunch-mint-spike to workspace.members
  </read_first>
  <behavior>
    Test 1 (e2e_signet::create_market): create a binary YES/NO/INVALID market on signet — funding tx broadcast, mint issues YES and NO conditional tokens against DLC funding.
    Test 2 (e2e_signet::place_bet): user funds Lightning wallet, deposits 10k sat via NUT-04 mint quote, mint issues 10k YES tokens. Reserve balance check: mint holds 10k sat in Lightning channel earmarked for this market.
    Test 3 (e2e_signet::oracle_attestation): oracle (single Schnorr in spike) signs YES outcome via BIP-340. Attestation broadcast as Nostr kind:89 event.
    Test 4 (e2e_signet::dlc_cet_broadcast): CET broadcast on signet, sending 10k sat (minus fee) to user's withdraw destination (their Lightning node).
    Test 5 (e2e_signet::withdraw): user redeems YES tokens via NUT-05 melt quote → mint pays out via Lightning. End state: user has 10k sat back (minus mint fee + miner fee), market is resolved.
    Test 6 (e2e_signet::refund_timeout_fallback): separate scenario — oracle disappears, after 7-day signet-time refund timeout, refund CET broadcasts, 50/50 refund to both sides (per HIP-2 Specification).

    Tests 1-5 must run end-to-end in <5 minutes on Mutinynet (30s block time) AFTER faucet funding completes. The e2e test runs autonomously — no "deferred to user run" escape hatch.
    Test 6 is too slow for default; gated behind `cargo test --features=slow-refund-timeout`.
  </behavior>
  <action>
    PART A — Add hunch-mint-spike crate to workspace:

    1. Update root `Cargo.toml`:
       ```toml
       [workspace]
       members = [
           # ... existing 6 production crates from Plan 01 ...
           "crates/hunch-mint-spike",     # SPIKE-02 (Phase 1 only, NOT for production)
           "crates/hunch-oracle-spike",   # SPIKE-03 (Phase 1 only, NOT for production)
       ]
       ```

    2. Create `crates/hunch-mint-spike/Cargo.toml` (per interfaces above — pin cdk = "=0.14", rust-dlc = "=0.7", ddk = "=0.0.17", ldk-node = "=0.4", bdk_wallet = "=1.0", bitcoin = "=0.32", nostr-sdk = "=0.34"; publish = false; features `slow-refund-timeout` gates Test 6). Run `cargo search` before commit to verify each version exists; document any deviation in README.md.

    3. Create `crates/hunch-mint-spike/README.md` with PROTOTYPE ONLY header + reproduction instructions + "What this proves / What this does NOT prove" sections + "Demo run YYYY-MM-DD" section for capturing signet tx hashes after Task 2 PART E completes.

    PART B — src/lib.rs (NUT-CTF integration layer + mint helpers):

    1. `crates/hunch-mint-spike/src/lib.rs`:
       ```rust
       //! Hunch SPIKE-02 — NUT-CTF (PR #337) end-to-end prototype.
       //!
       //! PROTOTYPE ONLY. NOT FOR PRODUCTION. Phase 2 hunch-mint reimplements from spec.
       //!
       //! Architecture (matches docs/HIP-3.md + PR #337):
       //! - Mint accepts Lightning deposit (NUT-04 mint quote)
       //! - Mint issues YES + NO + INVALID conditional tokens against DLC funding tx
       //! - Oracle (single Schnorr in spike) signs outcome
       //! - DLC CET broadcasts on attestation
       //! - User redeems winning tokens via NUT-05 melt quote
       
       pub mod market;          // Market creation + DLC funding tx
       pub mod tokens;          // NUT-CTF conditional token issuance + DLEQ
       pub mod oracle;          // Single-Schnorr oracle (Phase 1 spike; FROST in hunch-oracle-spike)
       pub mod settlement;      // CET broadcast + Lightning melt for withdraw
       pub mod errors;
       
       pub use errors::SpikeError;
       ```

    2. The submodule code is informed by PR #337's NUT-CTF base spec (conditional token issuance primitives), PR #337's NUT-CTF-split-merge, dlcspecs/Oracle.md + dlcspecs/Messaging.md, and CDK v0.14+ extension trait pattern.

    PART C — src/main.rs (binary entry point demo):

    ```rust
    //! spike-nut-ctf binary — runs the full e2e_signet flow as a CLI demo.

    use hunch_mint_spike::{market, tokens, oracle, settlement};

    #[tokio::main]
    async fn main() -> anyhow::Result<()> {
        eprintln!("Hunch SPIKE-02 — NUT-CTF (PR #337) e2e signet demo");
        eprintln!("PROTOTYPE ONLY. NOT FOR PRODUCTION.");
        // ... main flow mirrors e2e_signet::full_flow test ...
        Ok(())
    }
    ```

    PART D — tests/e2e_signet.rs (the test harness):

    Implement Tests 1-6 from `<behavior>`. Tests use `tokio::test` with 5-minute timeouts.

    Key technical patterns:
    - Mutinynet RPC client via esplora-client (HTTP).
    - LDK Node for Lightning deposits + withdraws (verify v0.4+ post-griefing fix per PITFALLS Pitfall 11).
    - CDK mint instantiated locally; NUT-CTF extension trait implements PR #337's primitives.
    - Single-Schnorr oracle: just a BIP-340 keypair signing the attestation. Multi-oracle FROST is hunch-oracle-spike's responsibility.
    - Funding tx → DLC contract (rust-dlc) → CETs enumerated for YES/NO/INVALID outcomes.

    The e2e test MUST read `MUTINYNET_FAUCET_TXID` env var (set by the faucet checkpoint subtask below) and use that funded UTXO as the seed. If env var is missing, the test fails with a clear error message: "MUTINYNET_FAUCET_TXID not set — run faucet checkpoint first".

    PART E — Run + verify with explicit faucet checkpoint:

    1. **Build:** `cargo build -p hunch-mint-spike` must exit 0.

    2. **Faucet checkpoint (checkpoint:human-action — dedicated subtask before e2e runs):**

       ```
       <subtask type="checkpoint:human-action" gate="blocking">
         <what-needed>
           SPIKE-02 e2e_signet test requires the test wallet to be pre-funded via Mutinynet faucet. Faucet may have CAPTCHA or rate-limit which Claude cannot autonomously bypass.
         </what-needed>
         <action>
           1. Open https://faucet.mutinynet.com in browser.
           2. Paste the spike test wallet's signet address (printed by `cargo run -p hunch-mint-spike --bin spike-nut-ctf -- print-address`).
           3. Request 100k sat (default drip).
           4. Wait ~30 seconds for confirmation.
           5. Copy the deposit txid from the faucet confirmation page.
           6. Export to env: `export MUTINYNET_FAUCET_TXID=<txid>`.
         </action>
         <resume-signal>Type "funded <txid>" with the deposit txid, OR "approved" if env is set.</resume-signal>
       </subtask>
       ```

       This subtask is REQUIRED before the next step. The e2e test does NOT proceed without funding confirmation.

    3. **Autonomous e2e test run (after faucet checkpoint):**

       Run `MUTINYNET_FAUCET_TXID=<txid> cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture`. The test MUST complete autonomously (no further user interaction). If the test cannot run autonomously due to a CDK/rust-dlc API issue, the task FAILS — investigate and fix; do NOT mark as "deferred to user run".

       Failure-mode verify command (in verify block below):
       ```
       cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture 2>&1 | tee /tmp/spike-02.log
       grep -q "tx_hash:\\|test result: ok" /tmp/spike-02.log || exit 1
       ```

    4. **Capture demo run results:** signet tx hashes (funding tx, CET tx) appended to `crates/hunch-mint-spike/README.md` as a "## Demo run 2026-05-XX" section with the tx ids + signet block heights + esplora block-explorer links.

    PART F — Create HIP-3-FINAL-GATE.md tracking doc (Plan 02 prerequisite):

    Create `.planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md`:

    ```markdown
    # HIP-3: Status: Final transition gate

    HIP-3 (Cashu NUT-CTF Integration) lands as Status: Draft in Plan 02. Transition to Status: Final requires:

    1. **[x/ ]** Plan 03 SPIKE-02 e2e_signet test passes against PR #337 on Mutinynet (signet tx hash: <tbd>; commit hash: <tbd>)
    2. **[x/ ]** PATH-A-VALIDATION.md confirms no architectural blocker for Hunch's permissionless multi-bettor model
    3. **[x/ ]** At least one Cashu maintainer (Calle / joemphilips / conduition / Gandlaf) public reviewer attestation logged in docs/HIPs-reviews.md

    When all 3 gates close, transition HIP-3 to `**Status:** Final` and re-publish via `bun scripts/publish-hip.ts 3` (kind 30023 is replaceable; same naddr1).

    ## Status tracker
    - SPIKE-02 e2e_signet pass: PENDING (Task 2 of Plan 03)
    - PATH-A-VALIDATION: PENDING (Task 1 of Plan 03)
    - Cashu maintainer attestation: PENDING (Plan 02 Task 3 reviewer outreach + 2-4 week wait)
    ```

    After SPIKE-02 e2e test passes, update gate 1 to `[x]` with signet tx hash + commit hash.

    Commit message: `spike(01-foundation): SPIKE-02 NUT-CTF (PR #337) end-to-end signet prototype on Mutinynet + HIP-3-FINAL-GATE.md tracker (D-01)`.
  </action>
  <verify>
    <automated>
      bash -c "cargo build -p hunch-mint-spike 2>&1 | tail -3 | grep -q 'Compiling\\|Finished' && test -f crates/hunch-mint-spike/Cargo.toml && grep -q 'cdk' crates/hunch-mint-spike/Cargo.toml && grep -q 'rust-dlc' crates/hunch-mint-spike/Cargo.toml && grep -q 'ddk' crates/hunch-mint-spike/Cargo.toml && grep -q 'ldk-node' crates/hunch-mint-spike/Cargo.toml && test -f crates/hunch-mint-spike/README.md && grep -q 'PROTOTYPE ONLY' crates/hunch-mint-spike/README.md && test -f crates/hunch-mint-spike/tests/e2e_signet.rs && wc -l crates/hunch-mint-spike/tests/e2e_signet.rs | awk '{exit !($1 >= 30)}' && test -f .planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md && grep -q 'SPIKE-02' .planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md && (test -n \"$MUTINYNET_FAUCET_TXID\" && cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture 2>&1 | tee /tmp/spike-02.log | grep -q 'tx_hash:\\|test result: ok' || echo 'WAITING: MUTINYNET_FAUCET_TXID env var not set — faucet checkpoint must complete first')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `crates/hunch-mint-spike/` exists with Cargo.toml pinning cdk, rust-dlc, ddk, ldk-node, bdk_wallet to the versions documented in the locked stack (verify via cargo search before final commit).
    - `crates/hunch-mint-spike/README.md` says PROTOTYPE ONLY at the top + reproduction steps + signet tx demo run section.
    - `tests/e2e_signet.rs` implements all 6 test scenarios (5 default + 1 slow-refund-timeout feature-gated).
    - `cargo build -p hunch-mint-spike` exits 0.
    - Faucet checkpoint subtask completes (user funds wallet + provides MUTINYNET_FAUCET_TXID env var).
    - **After faucet funding, `cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture` runs autonomously to completion AND emits `tx_hash:` or `test result: ok` (verified by grep — failure mode is explicit `exit 1`).**
    - At least 1 successful signet tx run logged in README.md "Demo run" section with funding tx + CET tx + esplora links.
    - `.planning/phases/01-cypherpunk-foundation/HIP-3-FINAL-GATE.md` exists; SPIKE-02 gate updated to `[x]` after e2e passes.
  </acceptance_criteria>
  <done>SPIKE-02 closed. Working signet prototype against NUT-CTF (PR #337) demonstrates the full flow autonomously after the explicit faucet checkpoint. Phase 1 Success Criterion #2 satisfied. HIP-3-FINAL-GATE.md tracker gate 1 closed. Phase 2 hunch-mint can be designed with empirical knowledge of what PR #337 + CDK supports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SPIKE-03 — Build hunch-oracle-spike with FROST 3-of-5 DKG ceremony on signet + signing + reset/rotate playbook; verify Trail of Bits Feb 2024 coefficient-vector-length fix</name>
  <files>
    Cargo.toml,
    crates/hunch-oracle-spike/Cargo.toml,
    crates/hunch-oracle-spike/src/lib.rs,
    crates/hunch-oracle-spike/README.md,
    crates/hunch-oracle-spike/tests/frost_dkg_3of5.rs,
    docs/playbooks/FROST-DKG-3of5.md,
    spikes/frost-dkg/PLAYBOOK.md,
    spikes/frost-dkg/ceremony-transcripts/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §4 in full (FROST DKG: crate status v2.2+, Pedersen DKG 2-round protocol, 3-of-5 reference participant playbook, communication channel NIP-44, failure/abort/restart procedures) and §12 (FROST DKG skeleton code sample)
    - .planning/research/PITFALLS.md Pitfall 7 (FROST cryptographic implementation attacks: Pedersen DKG threshold manipulation TOB Feb 2024, Wagner/Drijvers k-tree, adaptive corruption, concurrent signing nonce hygiene, rogue-key)
    - docs/HIP-4.md (Plan 02 deliverable — for spec context)
    - https://blog.trailofbits.com/2024/02/20/breaking-the-shared-key-in-threshold-signature-schemes/ — TOB Feb 2024 disclosure (full text)
    - https://zfnd.org/pedersen-dkg-vulnerability-in-frost-distributed-key-generation-successfully-remediated/ — ZF Foundation remediation
    - https://github.com/ZcashFoundation/frost — frost-secp256k1-tr v2.2+ API docs (specifically `keys::dkg::part1/part2/part3` signatures + how `Identifier::try_from` works)
    - https://datatracker.ietf.org/doc/rfc9591/ — RFC 9591 FROST sections 3 (DKG) + 5 (signing)
    - existing Cargo.toml (root, post-Task 2)
  </read_first>
  <behavior>
    Test 1 (frost_dkg_3of5::happy_path): 5 participants execute Pedersen DKG with t=3 successfully. All participants derive the same group_public_key (BIP-340 x-only).
    Test 2 (frost_dkg_3of5::coefficient_vector_length_check): explicitly assert `package.commitment().0.len() == MIN_SIGNERS` at the application layer for every received commitment vector across all 5 participants (defends Trail of Bits Feb 2024 fix at app-layer — defense in depth even though frost-secp256k1-tr v2.2+ does this internally).
    Test 3 (frost_dkg_3of5::malicious_threshold_raise_detected): simulate a malicious participant who tampers with their commitment vector length (e.g., appends one extra entry). Assert that the application-layer check rejects the malicious package, DKG aborts, no group key is derived. Distinguish the malicious participant by Identifier.
    Test 4 (frost_dkg_3of5::sign_attestation_with_3_of_5): after DKG, randomly pick 3 of 5 participants. Execute FROST signing of a test attestation message ("YES" outcome for market_id=test_market_001). Verify aggregated signature against group_public_key via secp256k1::verify_schnorr. Assert BIP-340 compliance.
    Test 5 (frost_dkg_3of5::abort_on_participant_offline): simulate participant 3 going offline mid-Round-2. Assert DKG aborts cleanly (no partial group key, all participants in clean state).
    Test 6 (frost_dkg_3of5::rotate_drops_participant): after first DKG ceremony, simulate rotation (e.g., participant 5 leaves federation). Re-run DKG with t=3, n=4. Assert new group_public_key is different from old, old key still valid for any pre-existing markets (forward-compat note for HIP-4 §Backwards Compatibility).
  </behavior>
  <action>
    PART A — Add hunch-oracle-spike to workspace (Cargo.toml in root already updated in Task 2 PART A).

    1. Create `crates/hunch-oracle-spike/Cargo.toml` pinning `frost-secp256k1-tr = "=2.2"`, secp256k1 0.30, bitcoin 0.32, nostr-sdk 0.34, rand 0.8, workspace deps; `publish = false`. Verify via `cargo audit` that frost-secp256k1-tr 2.2 has no open advisories. Document in README.md.

    2. Create `crates/hunch-oracle-spike/README.md` with PROTOTYPE ONLY header + Crypto posture section + "What this proves / What this does NOT prove" + reproduction commands + ceremony transcripts location.

    PART B — src/lib.rs (FROST primitives + DKG/signing helpers):

    ```rust
    //! Hunch SPIKE-03 — FROST 3-of-5 DKG + signing prototype.
    //!
    //! PROTOTYPE ONLY. NOT FOR PRODUCTION.
    //! Phase 2 hunch-oracle reimplements with full operator UX + Nostr coordination + hardware-backed share storage.

    pub mod dkg;          // Pedersen DKG (delegates to frost-secp256k1-tr::keys::dkg)
    pub mod sign;         // 2-round signing (delegates to frost-secp256k1-tr::round1/round2)
    pub mod rotate;       // Membership change (full re-DKG; classical Pedersen has no incremental)
    pub mod transcript;   // serde of (round1 commitments, round2 shares, final group key, signature)
    pub mod errors;

    pub use errors::OracleSpikeError;
    ```

    PART C — tests/frost_dkg_3of5.rs:

    Implement Tests 1-6 from `<behavior>`. Build on RESEARCH §12 skeleton with full implementations.

    Critical pattern for Test 2 (TOB fix at app layer):
    ```rust
    for id in &identifiers {
        let (secret, package) = part1(*id, MAX_SIGNERS, MIN_SIGNERS, &mut rng).unwrap();
        // Trail of Bits Feb 2024 disclosure:
        // The Pedersen DKG coefficient commitment vector MUST have exactly t entries.
        // A malicious participant could pad an extra entry to raise the threshold silently,
        // resulting in funds locked forever.
        // frost-secp256k1-tr v2.2+ checks this internally; we repeat at application layer
        // per CLAUDE.md "no clever shortcuts" prudence.
        assert_eq!(
            package.commitment().coefficients().len(),  // adjust to actual v2.2+ API
            MIN_SIGNERS as usize,
            "Pedersen DKG coefficient vector length mismatch — possible threshold-raise attack from participant {:?}", id
        );
        secret_packages.insert(*id, secret);
        round1_packages.insert(*id, package);
    }
    ```

    Critical pattern for Test 3 (malicious threshold raise):
    ```rust
    let mut tampered = round1_packages[&Identifier::try_from(5).unwrap()].clone();
    // ... append an extra commitment entry to the vector ...
    let result = our_app_layer_validate(&tampered);
    assert!(result.is_err());
    if let Err(OracleSpikeError::ThresholdRaiseAttempt { participant }) = result {
        assert_eq!(participant, Identifier::try_from(5).unwrap());
    } else {
        panic!("Expected ThresholdRaiseAttempt error");
    }
    ```

    PART D — Run + ceremony transcripts:

    1. `cargo build -p hunch-oracle-spike` exits 0.
    2. `cargo test -p hunch-oracle-spike --test frost_dkg_3of5 -- --nocapture` — all 6 tests pass.
    3. Run the happy-path ceremony 3 separate times. For each, capture: 5 Identifiers, all Round 1 commitments (hex), all Round 2 shares, final group_public_key, test attestation signature. Save as `spikes/frost-dkg/ceremony-transcripts/run-{001|002|003}-2026-05-XX.json`.

    PART E — Operational playbook documents:

    Write `docs/playbooks/FROST-DKG-3of5.md` per RESEARCH §4.3 structure (10 sections: pre-ceremony preparation, coordination phase, Round 1 with TOB length check, Round 2 point-to-point shares, finalization, persistence, rotation, abort/restart, operational hygiene, ceremony transcript template). Mirror to `spikes/frost-dkg/PLAYBOOK.md`. Include literal text "Trail of Bits" + reference to Feb 2024 disclosure + ZF remediation + the application-layer length check.

    Create `spikes/frost-dkg/ceremony-transcripts/.gitkeep` (empty file to preserve dir).

    Commit message: `spike(01-foundation): SPIKE-03 FROST 3-of-5 DKG ceremony + signing + Trail of Bits Feb 2024 fix verified at app layer + playbook (HIP-4)`.
  </action>
  <verify>
    <automated>
      bash -c "cargo build -p hunch-oracle-spike 2>&1 | tail -3 | grep -q 'Compiling\\|Finished' && cargo test -p hunch-oracle-spike --test frost_dkg_3of5 -- --nocapture 2>&1 | tail -10 | grep -q 'test result.*ok\\|6 passed' && test -f docs/playbooks/FROST-DKG-3of5.md && grep -q 'Trail of Bits' docs/playbooks/FROST-DKG-3of5.md && grep -q 'coefficient' docs/playbooks/FROST-DKG-3of5.md && test -f spikes/frost-dkg/PLAYBOOK.md && grep -q 'init' spikes/frost-dkg/PLAYBOOK.md && grep -q 'rotat' spikes/frost-dkg/PLAYBOOK.md && test -d spikes/frost-dkg/ceremony-transcripts && grep -q 'PROTOTYPE ONLY' crates/hunch-oracle-spike/README.md && grep -q 'frost-secp256k1-tr.*2.2' crates/hunch-oracle-spike/Cargo.toml"
    </automated>
  </verify>
  <acceptance_criteria>
    - `crates/hunch-oracle-spike/` exists with Cargo.toml pinning frost-secp256k1-tr to v2.2+.
    - `crates/hunch-oracle-spike/README.md` says PROTOTYPE ONLY + describes Trail of Bits Feb 2024 fix verification at app layer.
    - `tests/frost_dkg_3of5.rs` implements all 6 test scenarios with explicit TOB length check + malicious-threshold-raise rejection.
    - `cargo build -p hunch-oracle-spike` exits 0.
    - `cargo test -p hunch-oracle-spike --test frost_dkg_3of5` exits 0 (all 6 tests pass).
    - `docs/playbooks/FROST-DKG-3of5.md` exists with 10 sections per RESEARCH §4.3, mentions Trail of Bits, coefficient vector length check, rotation procedure.
    - `spikes/frost-dkg/PLAYBOOK.md` mirrors the canonical playbook.
    - At least 3 ceremony transcripts exist OR a note that transcripts are deferred to user-run with explicit reproduction commands.
  </acceptance_criteria>
  <done>SPIKE-03 closed. FROST 3-of-5 DKG ceremony validated empirically with Trail of Bits Feb 2024 fix doubly-verified (lib internal + app layer). Phase 1 Success Criterion #3 satisfied. Phase 2 hunch-oracle inherits the ceremony pattern + playbook.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: SPIKE-04 — Write Lightning-DLC NO-GO assessment (atomic.finance pivoted; Crypto Garage not production-ready). Primary source URLs verified live at write-time via curl.</name>
  <files>
    spikes/lightning-dlc/GO-NOGO.md,
    docs/SPIKE-04-lightning-dlc-assessment.md
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §5 in full (Lightning-DLC: atomic.finance state, Crypto Garage rust-dlc Lightning, Go/No-go matrix, SPIKE-04 deliverable structure)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-03 Lightning-DLC NO-GO confirmed)
    - https://blockspace.media/insight/lygos-finance-acquires-atomic-finance-to-launch-non-custodial-dlc-powered-bitcoin-loans/ (Lygos acquires atomic.finance Aug 2025)
    - https://atomic.finance/blog/an-atomic-pivot/ (atomic.finance pivot blog)
    - https://medium.com/crypto-garage/dlc-on-lightning-cb5d191f6e64 (Crypto Garage "not production ready")
    - https://github.com/cryptogarageinc/rust-dlc (search for any 2024/2025/2026 commits on Lightning channel-splitting; verify the fork hasn't progressed)
    - https://www.tftc.io/issue-1286/ (Nov 2022 mainnet milestone — verify no subsequent update)
  </read_first>
  <action>
    PART A — URL liveness verification (mandatory pre-write):

    Before drafting the assessment, verify all primary source URLs return 200 via curl. If any URL is 404/5xx, find a fresh primary source for the same claim or escalate as a research gap.

    Required curl checks (each MUST return 200; abort task on any failure):

    ```bash
    for url in \
      "https://blockspace.media/insight/lygos-finance-acquires-atomic-finance-to-launch-non-custodial-dlc-powered-bitcoin-loans/" \
      "https://atomic.finance/blog/an-atomic-pivot/" \
      "https://medium.com/crypto-garage/dlc-on-lightning-cb5d191f6e64" \
      "https://www.tftc.io/issue-1286/" \
      "https://github.com/cryptogarageinc/rust-dlc"
    do
      status=$(curl -sI -L "$url" | grep -E '^HTTP' | tail -1 | awk '{print $2}')
      [ "$status" = "200" ] || { echo "URL liveness FAIL: $url returned $status"; exit 1; }
    done
    ```

    If any URL returns 404 or 5xx at write-time: STOP, find fresh primary sources for the same factual claim, document the substitution in the assessment's "Sources" section, and re-run the curl check before continuing. Do not allow stale broken citations.

    PART B — Write the assessment in two locations (canonical + spike mirror):

    `docs/SPIKE-04-lightning-dlc-assessment.md` (canonical) — see RESEARCH §5 for full content template. Include:
    - Executive Summary (NO-GO verdict with 2 primary reasons: atomic.finance pivoted Aug 2025; Crypto Garage not production-ready)
    - Detailed findings on atomic.finance state + Crypto Garage rust-dlc Lightning fork state (each with primary source citations)
    - Go/No-go decision matrix (per RESEARCH §5.3)
    - Architectural shape Lightning-DLC would take if shipped (channel splitting via glue txs, pre-signed CETs in channel)
    - NO-GO recommendation rationale (5 numbered reasons)
    - v2 Reconsideration Triggers (5 triggers)
    - Impact on Phase 2 (on-chain DLC fallback, LDK Node still required, settlement latency 1-6 signet blocks)
    - Sources section listing all primary URLs WITH `curl -sI URL | head -1` output noted next to each (e.g., "verified 2026-05-XX HTTP 200")

    Mirror to `spikes/lightning-dlc/GO-NOGO.md` (same content; spikes/ is the throwaway-artifact tree).

    PART C — Word count discipline:

    Target 600-3000 words (a 3-5 page paper). If under 600, expand the architectural-shape sketch. If over 3000, tighten the rationale sections.

    Commit message: `spike(01-foundation): SPIKE-04 Lightning-DLC NO-GO assessment with URL-liveness-verified sources (atomic.finance pivoted + Crypto Garage not production-ready) (D-03)`.
  </action>
  <verify>
    <automated>
      bash -c "test -f docs/SPIKE-04-lightning-dlc-assessment.md && grep -q 'NO-GO' docs/SPIKE-04-lightning-dlc-assessment.md && grep -q 'Lygos' docs/SPIKE-04-lightning-dlc-assessment.md && grep -q 'Crypto Garage' docs/SPIKE-04-lightning-dlc-assessment.md && grep -q 'Reconsideration Triggers' docs/SPIKE-04-lightning-dlc-assessment.md && test -f spikes/lightning-dlc/GO-NOGO.md && grep -q 'NO-GO' spikes/lightning-dlc/GO-NOGO.md && wc -w docs/SPIKE-04-lightning-dlc-assessment.md | awk '{exit !($1 >= 600 && $1 <= 3000)}' && for url in 'https://blockspace.media/insight/lygos-finance-acquires-atomic-finance-to-launch-non-custodial-dlc-powered-bitcoin-loans/' 'https://atomic.finance/blog/an-atomic-pivot/' 'https://medium.com/crypto-garage/dlc-on-lightning-cb5d191f6e64' 'https://www.tftc.io/issue-1286/' 'https://github.com/cryptogarageinc/rust-dlc'; do curl -sI -L \"$url\" 2>/dev/null | head -1 | grep -q '200' || { echo \"URL DEAD at verify-time: $url — task must rewrite with fresh source\"; exit 1; }; done"
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/SPIKE-04-lightning-dlc-assessment.md` exists with verdict NO-GO, primary sources cited (Lygos acquisition, Crypto Garage Medium post, TFTC Nov 2022), architectural-shape sketch, v2 reconsideration triggers list, impact on Phase 2 section.
    - Word count between 600 and 3000 (a 3-5 page paper).
    - `spikes/lightning-dlc/GO-NOGO.md` mirrors the canonical doc.
    - **All 5 primary source URLs return HTTP 200 at write-time AND at verify-time** (curl check enforced by automated verify block; task fails if any URL is dead).
    - Verdict aligns with CONTEXT.md D-03 (NO-GO confirmed).
  </acceptance_criteria>
  <done>SPIKE-04 closed. Written NO-GO assessment exists with v2 reconsideration triggers documented + primary sources URL-liveness-verified at write-time + verify-time. No prototype required per D-03. Phase 2 architecture proceeds with on-chain DLC settlement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 4 SPIKEs delivered: SPIKE-01 NUT-CTF (PR #337) Path A validation with maintainer outreach; SPIKE-02 working signet (Mutinynet) end-to-end prototype against PR #337 — autonomously run after explicit faucet checkpoint; SPIKE-03 FROST 3-of-5 DKG ceremony + signing + Trail of Bits Feb 2024 fix verified + playbook; SPIKE-04 Lightning-DLC NO-GO written assessment with URL-liveness-verified primary sources.

    Two new throwaway spike crates are in the workspace (clearly marked PROTOTYPE ONLY per RESEARCH Pitfall E). HIP-3-FINAL-GATE.md tracker has SPIKE-02 gate closed (gate 2 PATH-A-VALIDATION may also be closed pending; gate 3 Cashu-maintainer attestation tracked separately via Plan 02 outreach window).

    Phase 2's production hunch-mint and hunch-oracle crates inherit the empirically-validated patterns but are NOT to depend on these spike crates.
  </what-built>
  <how-to-verify>
    1. **Build sanity:**
       ```bash
       cargo build --workspace
       cargo test -p hunch-mint-spike --test e2e_signet -- --nocapture   # autonomous after faucet
       cargo test -p hunch-oracle-spike --test frost_dkg_3of5 -- --nocapture
       ```
       SPIKE-02 e2e test runs autonomously once `MUTINYNET_FAUCET_TXID` is set; the faucet checkpoint subtask must have completed first. SPIKE-03 must pass autonomously without any external network.

    2. **Maintainer outreach status:** Open `spikes/nut-ctf/outreach-log.md`. Verify:
       - 3 outreach attempts logged (joemphilips, conduition, Calle)
       - Each row has a `Response received` column populated either with the response excerpt + Nostr event ID OR "no response after 2 weeks"
       - `docs/SPIKE-01-nut-ctf-decision.md` reflects the outreach signal in the "Maintainer signal" section.

    3. **Path A decision lock:** `docs/SPIKE-01-nut-ctf-decision.md` Status: FINAL, Decision: Path A. If during the outreach period a maintainer surfaced a blocker that invalidates Path A, the decision document should reflect that and propose Path B or Path C (escalation per CONTEXT.md D-01 deferred-paths). If the user disagrees with the decision, mark `REVISE`.

    4. **TOB Feb 2024 fix verified:** Open `crates/hunch-oracle-spike/tests/frost_dkg_3of5.rs` and visually confirm the explicit length check assertion is present at the application layer. Look for: `assert_eq!(package.commitment().coefficients().len(), MIN_SIGNERS as usize, ...)`.

    5. **FROST playbook tested 3+ times:** Open `spikes/frost-dkg/ceremony-transcripts/` and verify ≥ 3 transcript files exist OR the README explicitly notes "transcripts deferred to user-run; reproduce via `cargo test -p hunch-oracle-spike --test frost_dkg_3of5` and capture JSON output to spikes/frost-dkg/ceremony-transcripts/run-NNN-DATE.json".

    6. **Lightning-DLC NO-GO sources URL-live:** Open `docs/SPIKE-04-lightning-dlc-assessment.md`. Verify it cites:
       - Lygos acquires atomic.finance (Aug 2025) — Blockspace.media link
       - Crypto Garage rust-dlc Lightning "not production-ready" — Medium link
       - V2 reconsideration triggers listed
       - Sources section notes "verified <date> HTTP 200" next to each primary URL

    7. **HIP-3-FINAL-GATE.md state:** Open the tracker doc. Confirm gate 1 (SPIKE-02) is `[x]` after this plan; gate 2 (PATH-A-VALIDATION) closed; gate 3 (Cashu maintainer attestation) tracked PENDING via Plan 02 outreach.

    8. **Decide:** Type `approved` to close SPIKE-01..04 and proceed to Plan 04 (Legal Foundation). Or describe what to revise.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike crate code → Phase 2 production crates | Spike code has shortcuts; must NOT be imported into hunch-mint or hunch-oracle (RESEARCH Pitfall E) |
| local nsec (project pseudonym) → maintainer Nostr DMs | nsec compromise = pseudonym takeover; DM content is metadata-protected by NIP-44 gift-wrap |
| Mutinynet faucet → spike test wallet | Faucet sats are test-net only, but the funding workflow exercises real Lightning flows; misconfiguration could leak test wallet seed |
| FROST DKG simulated participants | All 5 simulated participants in one process; production has 5 separate machines + per-process nonce isolation per RESEARCH §4 |
| frost-secp256k1-tr v2.2+ internal length check | Defense in depth: app-layer assertion of `commitment().coefficients().len() == t` even though lib does this; protects against silent regression in lib |
| SPIKE-04 primary source URLs | URLs may rot between write-time and verify-time; curl liveness check at both moments enforces freshness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | spike crate accidentally imported by hunch-mint or hunch-oracle in Phase 2 | mitigate | spike crates have `publish = false` + README "PROTOTYPE ONLY" + Phase 2 plan-checker rejects any Cargo.toml that lists `hunch-mint-spike` or `hunch-oracle-spike` in `[dependencies]` |
| T-03-02 | Elevation of Privilege | malicious participant in FROST DKG raises threshold via padded commitment vector | mitigate | Trail of Bits Feb 2024 fix verified at lib (frost-secp256k1-tr v2.2+) AND application layer in SPIKE-03 test 2 + test 3 |
| T-03-03 | Information Disclosure | SPIKE-02 leaks test-wallet seed in commit | mitigate | wallet seed lives in `.env.local` (gitignored from Plan 01 Task 1); README never embeds seed; CI scans for seed patterns |
| T-03-04 | Spoofing | NUT-CTF maintainer outreach impersonates joemphilips / conduition / Calle | mitigate | outreach-log records reviewer Nostr npub + event ID; verify Nostr event signature before treating as authentic response |
| T-03-05 | DoS | Mutinynet faucet rate-limits or goes down | accept | fallback to vanilla signet (slower but available); spike crate documents fallback path |
| T-03-06 | Tampering | rust-dlc / DDK alpha API breaking change between Phase 1 spike and Phase 2 production | mitigate | pin exact versions in spike Cargo.toml + Phase 2 production crates; spike provides reference implementation, Phase 2 re-derives from spec |
| T-03-07 | Information Disclosure | FROST DKG ceremony transcript leaks operator identifying metadata | mitigate | transcripts contain only Identifier numbers (1-5) + cryptographic primitives; no real-name or IP info; spikes/frost-dkg/ tree is public artifact |
| T-03-08 | Repudiation | maintainer outreach response not preserved | mitigate | NIP-44 gift-wrapped DM events are signed + immutable on relays; outreach-log records event ID for cryptographic reconstruction |
| T-03-09 | Tampering | SPIKE-04 sources become stale (Lygos publishes Lightning-DLC product, LDK mainline adopts channel-split) | mitigate | URL-liveness check (curl) at write-time AND in verify block; v2 reconsideration triggers listed in SPIKE-04 doc; Phase 2/3 phase-discuss re-checks before final v1 launch |
| T-03-10 | Information Disclosure | Phase 1 spike code references in HIP-3 (PR #337) commit SHA — if PR #337 is rebased/squashed, SHA reference breaks | mitigate | pin the SHA in PATH-A-VALIDATION.md + SPIKE-01 decision doc; if PR #337 rebases, run Task 1 PR-read step again and update HIP-3 corrigendum |
| T-03-11 | Tampering | HIP-3 transitions to Status: Final without SPIKE-02 verified pass | mitigate | HIP-3-FINAL-GATE.md tracker enforces 3 gates (SPIKE-02 pass + PATH-A-VALIDATION + Cashu maintainer attestation); Plan 02 acceptance_criteria enforces `! grep "Status: Final" docs/HIP-3.md` until all 3 close |
</threat_model>

<verification>
- All 4 requirements (SPIKE-01..04) closed per their falsification conditions in RESEARCH.md §7.2.
- `cargo build --workspace && cargo test -p hunch-mint-spike --test e2e_signet && cargo test -p hunch-oracle-spike --test frost_dkg_3of5` exits 0 (SPIKE-02 runs autonomously AFTER the faucet checkpoint subtask completes).
- SPIKE-02 demo run yields signet tx ids logged in `crates/hunch-mint-spike/README.md`.
- SPIKE-03 ceremony transcript count ≥ 3 in `spikes/frost-dkg/ceremony-transcripts/` (or deferred note).
- SPIKE-04 word count 600-3000, verdict NO-GO, v2 reconsideration triggers listed, all 5 primary URLs return 200 via curl.
- All `hunch-mint-spike` and `hunch-oracle-spike` files clearly marked PROTOTYPE ONLY (README headers + Cargo.toml description + `publish = false`).
- HIP-3-FINAL-GATE.md tracker created; SPIKE-02 gate closed after e2e pass.
</verification>

<success_criteria>
- 4/4 Phase 1 SPIKE requirements closed: SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04.
- Phase 1 ROADMAP Success Criterion #2 closed: "NUT-DLC working prototype demonstrates full flow (create market → bet via Lightning → resolve via oracle Schnorr → withdraw winnings) on Bitcoin signet, end-to-end". Achieved via SPIKE-02 against NUT-CTF (PR #337) on Mutinynet.
- Phase 1 ROADMAP Success Criterion #3 closed: "FROST k-of-n DKG ceremony executed successfully on signet with documented playbook (init → DKG → multi-round signing → reset/rotate procedures)". Achieved via SPIKE-03 with TOB Feb 2024 fix doubly-verified.
- NUT-CTF Path A (D-01) empirically validated; HIP-3 Status: Final transition gate 1 closed.
- Lightning-DLC NO-GO (D-03) documented with v2 reconsideration triggers + URL-liveness-verified primary sources.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cypherpunk-foundation/01-03-SUMMARY.md` documenting:
- SPIKE-01 outreach responses + final Path A decision
- SPIKE-02 signet tx ids + reproduction notes + faucet-checkpoint completion details
- SPIKE-03 ceremony transcript count + TOB fix verification status
- SPIKE-04 sources URL-verified date + v2 reconsideration trigger list
- Two new spike crates' final pinned dependency versions
- HIP-3-FINAL-GATE.md tracker status (which gates closed)
- Open follow-ups (e.g., upstream PR #337 contributions, FROST adaptive-corruption research tracking)
</output>
