---
phase: 01-cypherpunk-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
  - Cargo.toml
  - crates/hunch-protocol/Cargo.toml
  - crates/hunch-protocol/src/lib.rs
  - crates/hunch-mint/Cargo.toml
  - crates/hunch-mint/src/lib.rs
  - crates/hunch-oracle/Cargo.toml
  - crates/hunch-oracle/src/lib.rs
  - crates/hunch-relay/Cargo.toml
  - crates/hunch-relay/src/lib.rs
  - crates/hunch-matcher/Cargo.toml
  - crates/hunch-matcher/src/lib.rs
  - crates/hunch-cli/Cargo.toml
  - crates/hunch-cli/src/main.rs
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - .gitignore
  - .github/workflows/verify.yml
  - scripts/verify-repo.sh
  - scripts/verify-hips.sh
  - scripts/verify-kind-collisions.ts
  - package.json
  - README.md
  - docs/audit-shortlist-outreach.md
autonomous: false
requirements: [PROTO-07, PROTO-08]
requirements_addressed: [PROTO-07, PROTO-08]
tags: [hunch, foundation, cargo, workspace, corrigendum, repo, github, radicle, pseudonymity]

must_haves:
  truths:
    - "Stale research docs (STACK/ARCHITECTURE/FEATURES/SUMMARY) flag PR #128 as obsolete and point to PR #337"
    - "Cargo workspace builds with 6 empty crates (hunch-protocol, hunch-mint, hunch-oracle, hunch-relay, hunch-matcher, hunch-cli)"
    - "CONTRIBUTING.md + CODE_OF_CONDUCT.md exist with pseudonymous attribution only (no real-name leakage)"
    - "GitHub repo github.com/Silexperience210/hunch exists, Radicle mirror initialized, README.md lists both mirrors"
    - "CLAUDE.md exists at root and references GSD workflow (PROTO-08 verification)"
    - "Verification scripts (verify-repo.sh, verify-hips.sh, verify-kind-collisions.ts) exist and are executable"
    - "Audit firm pre-outreach queue exists at docs/audit-shortlist-outreach.md (Phase 3 lead-time discipline)"
  artifacts:
    - path: ".planning/research/STACK.md"
      provides: "Corrigendum: PR #128 marked obsolete, PR #337 marked live"
      contains: "PR #337"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Corrigendum: bilateral DLC pattern marked obsolete; NUT-CTF marked live"
      contains: "PR #337"
    - path: ".planning/research/FEATURES.md"
      provides: "Corrigendum: NUT-DLC references updated to NUT-CTF"
      contains: "PR #337"
    - path: ".planning/research/SUMMARY.md"
      provides: "Corrigendum: NUT-DLC narrative updated to NUT-CTF"
      contains: "PR #337"
    - path: "Cargo.toml"
      provides: "Workspace manifest declaring 6 member crates"
      contains: "[workspace]"
    - path: "crates/hunch-protocol/src/lib.rs"
      provides: "Shared types crate stub (compiles empty)"
      min_lines: 1
    - path: "crates/hunch-mint/src/lib.rs"
      provides: "Mint crate stub"
      min_lines: 1
    - path: "crates/hunch-oracle/src/lib.rs"
      provides: "Oracle crate stub"
      min_lines: 1
    - path: "crates/hunch-relay/src/lib.rs"
      provides: "Relay crate stub"
      min_lines: 1
    - path: "crates/hunch-matcher/src/lib.rs"
      provides: "Tier-2 matcher crate stub"
      min_lines: 1
    - path: "crates/hunch-cli/src/main.rs"
      provides: "CLI crate stub"
      min_lines: 1
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 + Pseudonymity section"
      contains: "Pseudonymity"
    - path: "scripts/verify-repo.sh"
      provides: "Repo verification script (LICENSE/CONTRIBUTING/CoC/Radicle)"
      contains: "MIT License"
    - path: "README.md"
      provides: "Repo README with Mirrors section listing GitHub + Radicle"
      contains: "## Mirrors"
    - path: "docs/audit-shortlist-outreach.md"
      provides: "Phase 3 audit firm pre-outreach queue (Trail of Bits, Block Digital Contracting, Cure53, Quarkslab, NCC Group, Galaxy Audit)"
      contains: "Trail of Bits"
  key_links:
    - from: "Cargo.toml"
      to: "crates/hunch-*"
      via: "[workspace.members]"
      pattern: "members"
    - from: "README.md"
      to: "github.com/Silexperience210/hunch + Radicle ID"
      via: "## Mirrors section"
      pattern: "github.com.*Silexperience210/hunch"
---

<objective>
Establish the repo foundation: corrigendum on stale research docs (PR #128 → PR #337 pivot), Cargo workspace skeleton with 6 empty crates, repo metadata (CONTRIBUTING, CODE_OF_CONDUCT, .gitignore, CI workflow), verification scripts, GitHub repo creation + Radicle mirror, CLAUDE.md verification (PROTO-08), and Phase 3 audit firm pre-outreach queue.

Purpose: PROTO-07 (open-source repo published on GitHub + Radicle mirror with MIT + CONTRIBUTING + CODE_OF_CONDUCT) and PROTO-08 (CLAUDE.md exists referencing GSD) are the foundational closure conditions. The repo URL (github.com/Silexperience210/hunch) MUST exist before Plan 02 reviewer outreach references it and before Plan 04 counsel outreach links to it. The Cargo workspace MUST compile before Plan 03 spikes can add `hunch-mint-spike` and `hunch-oracle-spike` as workspace members.

Output: 2 requirements closed (PROTO-07, PROTO-08). The .planning/research/* corrigendum trail visible in git history. A Cargo workspace that compiles. Repo metadata (CONTRIBUTING.md updated, CODE_OF_CONDUCT.md created) pseudonymity-hardened. GitHub + Radicle mirrors live. CI workflow running on push. Verification scripts ready for Plan 02 (HIP-publishing tooling) + future HIP lint gating. Audit firm pre-outreach queue (RESEARCH §10 Q10 8-14 week lead-time discipline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cypherpunk-foundation/01-CONTEXT.md
@.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@LICENSE
@CONTRIBUTING.md
@README.md

<interfaces>
<!-- Locked decisions from CONTEXT.md (D-01..D-08) — non-negotiable. -->

D-01 NUT-DLC strategy: Path A (NUT-CTF, PR #337). Corrigendum on .planning/research/* docs must mark PR #128 as obsolete and point to PR #337.
D-02 Wave 0 corrigendum: .planning/research/STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md must mark PR #128 as obsolete and point to PR #337 BEFORE Plan 02 HIP drafting.
D-05 Full pseudonymity: all commits signed under Silex_0xF777C5B8 pseudonym key. No real-name in LICENSE, CONTRIBUTING.md, CODE_OF_CONDUCT.md.
D-07 Repo scaffold partial: LICENSE, CONTRIBUTING.md, README.md, .gitignore, CLAUDE.md exist (commit 96966ab). Build on existing.
D-08 Stack locked: Cargo workspace root with crates hunch-protocol, hunch-mint, hunch-oracle, hunch-relay, hunch-matcher, hunch-cli. frost-secp256k1-tr v2.2+. rust-dlc + DDK alpha.

<!-- Existing repo state (verified 2026-05-28 via git status + ls): -->
- LICENSE: MIT, Copyright "Hunch contributors" (pseudonymous-compatible already)
- CONTRIBUTING.md: draft, mentions Phase 1 deliverable status, no real-name leakage
- README.md: exists, project identity present
- CLAUDE.md: exists, GSD workflow referenced (PROTO-08 already satisfied — verification task only)
- Silex_0xF777C5B8_SECRET.asc: project pseudonym GPG key present at root
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave 0 corrigendum on stale research docs (PR #128 → PR #337) + repo metadata pseudonymity hygiene (CONTRIBUTING.md + CODE_OF_CONDUCT.md + .gitignore)</name>
  <files>
    .planning/research/STACK.md,
    .planning/research/ARCHITECTURE.md,
    .planning/research/FEATURES.md,
    .planning/research/SUMMARY.md,
    CONTRIBUTING.md,
    CODE_OF_CONDUCT.md,
    .gitignore
  </files>
  <read_first>
    - .planning/research/STACK.md (current state — find all PR #128 references via grep)
    - .planning/research/ARCHITECTURE.md (Pattern 3 bilateral DLC discussion — find all PR #128 references)
    - .planning/research/FEATURES.md (NUT-DLC user flow discussion)
    - .planning/research/SUMMARY.md (executive summary's NUT-DLC narrative)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-01, D-02 locked decisions)
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §3 (PR #128 closed May 20 2025; PR #337 architectural deltas) and §14 ("Deprecated/outdated content" list)
    - CONTRIBUTING.md (verify no real-name leakage per D-05)
    - LICENSE (verify Copyright "Hunch contributors" — no real-name; do NOT modify)
  </read_first>
  <action>
    PART A — Corrigendum on 4 stale research docs (per D-02):

    For each of STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md:
    1. Grep for every occurrence of "PR #128", "NUT-DLC", "bilateral DLC", "conduition", "PR 128".
    2. Prepend a "## 2026-05-28 Corrigendum" section at the top of each file (immediately after the H1 title) with this exact structure:

       ```markdown
       ## 2026-05-28 Corrigendum — NUT-DLC pivot to NUT-CTF (PR #337)

       **Status update:** Cashu NUTs PR #128 (bilateral NUT-DLC by conduition) was **CLOSED 2025-05-20** by thesimplekid with comment "Closing as there is no active work. Please reopen if work continues." This document was written 2026-05-27 assuming PR #128 was the live critical path. That assumption is **WRONG**.

       **New chosen path (locked in CONTEXT.md decision D-01):** Path A — **NUT-CTF (Conditional Token Framework, PR #337 by joemphilips, opened 2026-02-07)**. NUT-CTF is architecturally distinct: oracle-agnostic mint with conditional token issuance + split-merge for secondary market, instead of mint-as-bilateral-counterparty.

       **References below to "NUT-DLC", "PR #128", "bilateral DLC mint" should be read as NUT-CTF / PR #337 / oracle-agnostic conditional token framework** unless explicitly marked as historical context.

       See `.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md` §3 for the deep-dive on this pivot.
       ```

    3. Do NOT remove the original PR #128 content; the corrigendum at the top is sufficient signal. Preserve research provenance per assumption A3 in RESEARCH.md §Assumptions Log.

    PART B — Pseudonymity hygiene check + CODE_OF_CONDUCT.md creation (per D-05):

    1. Read existing CONTRIBUTING.md. Verify no real-name (no "Silex" outside the Silex npub pseudonym, no "Alexandre", no "alexandre.cangini@gmail.com"). If any leakage found, replace with project pseudonym placeholder (`Silex <npub-TBD>` for now; npub will be set in Plan 02 Task 2 after key generation).
    2. Update CONTRIBUTING.md "Security" section: replace `[TBD — Phase 1 deliverable]` with `nostr DM to npub-TBD (see SECURITY.md, Phase 2 deliverable) — or PGP-encrypted email to <pseudonym>@protonmail.com (TBD)`. Add a sentence: "All contributor attribution uses Nostr npub or pseudonym; do not submit PRs with real-name commit authorship — set per-repo `git config user.name "<pseudonym>"` and `user.email "<pseudonym>@hunch.markets"` before pushing."
    3. Update CONTRIBUTING.md "Commit Style" section: add bullet `- "Sign all commits under the project pseudonym GPG key (Silex_0xF777C5B8). Real-name commits will be rejected on the public repo."`
    4. Create CODE_OF_CONDUCT.md based on Contributor Covenant v2.1 (https://www.contributor-covenant.org/version/2/1/code_of_conduct/) BUT with these Hunch-specific adjustments:
       - Replace contact email with `nostr DM to npub-TBD (Phase 2 deliverable) — or PGP-encrypted email to <pseudonym>@protonmail.com (TBD)`
       - Add a "Pseudonymity" section at the bottom (after Enforcement): "Hunch contributors are encouraged to operate under pseudonym. Real-name disclosure is never required. Maintainers do not collect contributor real names. Doxxing of any contributor — including maintainers — is grounds for permanent ban."
    5. Update .gitignore to ensure `Silex_0xF777C5B8_SECRET.asc` is NOT accidentally committed (verify line `*SECRET*` or explicit `Silex_0xF777C5B8_SECRET.asc` is present; add if missing). Also add `docs/legal/signoff/*.pdf` (counsel PDFs encrypted-or-private), `.env`, `.env.local`, `target/`, `node_modules/`, `dist/`, `.next/`.

    Commit message: `docs(01-foundation): corrigendum on stale research docs (PR #128 → PR #337) + pseudonymity hygiene for repo metadata`. Use per-repo git config to set author to pseudonym before commit: `git config user.name "Silex" && git config user.email "silex@hunch.markets"`.
  </action>
  <verify>
    <automated>
      bash -c "grep -l 'PR #337' .planning/research/STACK.md .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/SUMMARY.md | wc -l | grep -q '^4$' && grep -l '2026-05-28 Corrigendum' .planning/research/STACK.md .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/SUMMARY.md | wc -l | grep -q '^4$' && test -f CODE_OF_CONDUCT.md && grep -q 'Pseudonymity' CODE_OF_CONDUCT.md && grep -q 'pseudonym' CONTRIBUTING.md && grep -q 'Silex_0xF777C5B8_SECRET.asc\\|*SECRET*' .gitignore"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 stale research docs (STACK, ARCHITECTURE, FEATURES, SUMMARY) contain a "## 2026-05-28 Corrigendum" section near the top that mentions PR #337.
    - CODE_OF_CONDUCT.md exists, based on Contributor Covenant 2.1, includes a "Pseudonymity" section.
    - CONTRIBUTING.md mentions per-repo git config for pseudonymous commit authorship.
    - .gitignore protects `Silex_0xF777C5B8_SECRET.asc` from accidental commit.
    - `git log --format="%an <%ae>"` on this commit shows ONLY pseudonym (Silex, silex@hunch.markets) — no real-name leakage.
  </acceptance_criteria>
  <done>Wave 0 corrigendum complete (unblocks Plan 02 HIP-3 drafting). CODE_OF_CONDUCT.md exists and is pseudonymity-aware. CONTRIBUTING.md updated. .gitignore protects the project pseudonym GPG key. Repo metadata is now consistent with locked decisions D-01, D-02, D-05.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cargo workspace scaffolding (6 crates) + CI workflow + verification scripts + Bun/TS tooling skeleton</name>
  <files>
    Cargo.toml,
    crates/hunch-protocol/Cargo.toml,
    crates/hunch-protocol/src/lib.rs,
    crates/hunch-mint/Cargo.toml,
    crates/hunch-mint/src/lib.rs,
    crates/hunch-oracle/Cargo.toml,
    crates/hunch-oracle/src/lib.rs,
    crates/hunch-relay/Cargo.toml,
    crates/hunch-relay/src/lib.rs,
    crates/hunch-matcher/Cargo.toml,
    crates/hunch-matcher/src/lib.rs,
    crates/hunch-cli/Cargo.toml,
    crates/hunch-cli/src/main.rs,
    .github/workflows/verify.yml,
    scripts/verify-repo.sh,
    scripts/verify-hips.sh,
    scripts/verify-kind-collisions.ts,
    package.json
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §7.4 (Wave 0 gaps list) and §11 (Stack — Phase 1 subset, exact versions + verification commands) and §12 (Code Examples — repo verification script + verify-kind-collisions skeleton)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md "Stack (locked)" section (D-08: Cargo workspace, 6 crates, frost-secp256k1-tr v2.2+, rust-dlc + DDK alpha)
    - .planning/research/STACK.md (after corrigendum from Task 1) — for full library version table
    - CLAUDE.md "Stack at a Glance" section
    - Existing root: `ls -la` to confirm no Cargo.toml or crates/ directory exists yet (greenfield workspace bootstrap)
  </read_first>
  <action>
    PART A — Cargo workspace bootstrap (per D-08):

    1. Create root `Cargo.toml` as a workspace manifest:
       ```toml
       [workspace]
       resolver = "2"
       members = [
           "crates/hunch-protocol",
           "crates/hunch-mint",
           "crates/hunch-oracle",
           "crates/hunch-relay",
           "crates/hunch-matcher",
           "crates/hunch-cli",
       ]

       [workspace.package]
       version = "0.1.0"
       edition = "2021"
       license = "MIT"
       repository = "https://github.com/Silexperience210/hunch"
       authors = ["Silex <silex@hunch.markets>"]
       rust-version = "1.80"

       [workspace.dependencies]
       tokio = { version = "1.40", features = ["full"] }
       serde = { version = "1", features = ["derive"] }
       serde_json = "1"
       anyhow = "1"
       thiserror = "2"
       ```
       Do NOT add cdk / rust-dlc / ddk / frost / nostr-sdk to workspace.dependencies yet — those are Plan 03 spike concerns. This plan creates empty stubs only.

    2. For each of the 6 crates create a minimal Cargo.toml + lib.rs (or main.rs for hunch-cli):

       `crates/hunch-protocol/Cargo.toml`:
       ```toml
       [package]
       name = "hunch-protocol"
       version.workspace = true
       edition.workspace = true
       license.workspace = true
       repository.workspace = true
       authors.workspace = true
       description = "Hunch protocol — shared types and HIP reference types (Phase 1: stub)"

       [dependencies]
       serde.workspace = true
       serde_json.workspace = true
       ```
       `crates/hunch-protocol/src/lib.rs`:
       ```rust
       //! Hunch protocol shared types.
       //!
       //! Phase 1 deliverable: empty crate skeleton. Types arrive in Phase 2 implementing HIP-1..5.
       //! See `docs/HIP-1.md` (Nostr event kinds), `docs/HIP-2.md` (DLC), `docs/HIP-4.md` (FROST).
       ```

       Repeat the same Cargo.toml pattern for hunch-mint, hunch-oracle, hunch-relay, hunch-matcher with appropriate descriptions referencing the corresponding HIP (HIPs land in Plan 02). Each lib.rs is a single-line `//!` doc comment pointing to the relevant HIP. Per RESEARCH.md Pitfall E, do NOT prefix names with "-spike" — those go in Plan 03 as `hunch-mint-spike` and `hunch-oracle-spike` (separate prototype crates).

       `crates/hunch-cli/Cargo.toml`:
       ```toml
       [package]
       name = "hunch-cli"
       version.workspace = true
       edition.workspace = true
       license.workspace = true
       repository.workspace = true
       authors.workspace = true
       description = "Hunch CLI (Phase 1: stub)"

       [[bin]]
       name = "hunch"
       path = "src/main.rs"
       ```
       `crates/hunch-cli/src/main.rs`:
       ```rust
       fn main() {
           println!("hunch-cli v0.1.0 (Phase 1 stub — see ROADMAP Phase 2 for real CLI)");
       }
       ```

    3. Run `cargo build --workspace` — must exit 0.

    PART B — Verification scripts (Wave 0 gap closure per RESEARCH §7.4):

    1. `scripts/verify-repo.sh` — copy the script from RESEARCH §12 verbatim, then chmod +x. Confirms LICENSE/CONTRIBUTING/CODE_OF_CONDUCT exist and remote `rad` is configured.

    2. `scripts/verify-hips.sh` — for each `docs/HIP-N.md`: run `mdl` (or `npx markdownlint-cli`) + grep for required sections (`## Abstract`, `## Motivation`, `## Specification`, `## References`); exit non-zero on missing section. Loop over HIP-0..5. (HIP files land in Plan 02; the script must guard with `if test -f docs/HIP-0.md` to be CI-safe before Plan 02 ships.)

    3. `scripts/verify-kind-collisions.ts` — Bun/TS script that fetches `https://raw.githubusercontent.com/nostr-protocol/nips/master/README.md` and `https://nostrbook.dev/kinds/` and checks that Hunch kinds (30888, 38888, 30890, 30891, 30892, 88, 89) do not collide with anything OTHER than NIP-88 (which is the intended dependency for kinds 88/89). Output: pass/fail report.

    Note: `scripts/publish-hip.ts` is intentionally NOT created in this plan — it lands in Plan 02 along with the HIPs themselves.

    PART C — Bun/TS tooling skeleton (per CLAUDE.md "Bun" stack):

    1. Create root `package.json`:
       ```json
       {
         "name": "hunch-scripts",
         "version": "0.1.0",
         "private": true,
         "type": "module",
         "scripts": {
           "verify:repo": "bash scripts/verify-repo.sh",
           "verify:hips": "bash scripts/verify-hips.sh",
           "verify:kinds": "bun scripts/verify-kind-collisions.ts"
         },
         "dependencies": {
           "@nostr-dev-kit/ndk": "^2.10.0"
         },
         "devDependencies": {
           "markdownlint-cli": "^0.41.0",
           "@types/bun": "latest"
         }
       }
       ```

       Note: `publish:hip` script lands in Plan 02 when `scripts/publish-hip.ts` is created.

    2. Run `bun install` to populate `bun.lockb`. Commit `bun.lockb`.

    PART D — CI workflow:

    Create `.github/workflows/verify.yml` running on push/pull_request to main:
    - Job 1 "rust": setup rust-toolchain stable, `cargo build --workspace`, `cargo test --workspace`, `cargo audit` (install first), `cargo deny check` (install first).
    - Job 2 "docs": setup bun, `bun install`, `bash scripts/verify-repo.sh`, `bash scripts/verify-hips.sh` (only runs once HIPs exist — guard with `if test -f docs/HIP-0.md`), `bun scripts/verify-kind-collisions.ts`.

    Commit message: `feat(01-foundation): cargo workspace scaffolding + CI workflow + verification scripts + bun tooling skeleton (D-08, RESEARCH §7.4 Wave 0)`.
  </action>
  <verify>
    <automated>
      bash -c "cargo build --workspace 2>&1 | tail -5 | grep -q 'Compiling\\|Finished' && cargo test --workspace 2>&1 | tail -3 | grep -q '0 passed\\|test result' && test -f Cargo.toml && grep -q 'hunch-protocol' Cargo.toml && grep -q 'hunch-mint' Cargo.toml && grep -q 'hunch-oracle' Cargo.toml && grep -q 'hunch-relay' Cargo.toml && grep -q 'hunch-matcher' Cargo.toml && grep -q 'hunch-cli' Cargo.toml && test -x scripts/verify-repo.sh && test -f scripts/verify-kind-collisions.ts && test -f scripts/verify-hips.sh && test -f .github/workflows/verify.yml && test -f package.json"
    </automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0; 6 member crates compile.
    - `cargo test --workspace` exits 0 (no tests yet, but no compile errors).
    - All 6 crate directories exist at `crates/hunch-{protocol,mint,oracle,relay,matcher,cli}/` with valid Cargo.toml and lib.rs/main.rs.
    - `scripts/verify-repo.sh` is executable and runs without error against current repo state (Radicle remote will be configured in Task 4; document that this is expected gate).
    - `.github/workflows/verify.yml` exists and runs on push/pull_request.
    - `package.json` declares 3 npm-scripts and depends on @nostr-dev-kit/ndk@^2.10.
    - `bun install` succeeds; `bun.lockb` committed.
  </acceptance_criteria>
  <done>Workspace skeleton complete and CI-verified. Verification scripts ready. CI workflow runs on every push and protects regression of the workspace structure. Plan 02 (HIPs) can now add HIP files + publish-hip.ts knowing scripts/verify-hips.sh + CI will gate them. Plan 03 (spikes) can now add `hunch-mint-spike` + `hunch-oracle-spike` as workspace members.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PROTO-08 verification (CLAUDE.md exists + references GSD) + Phase 3 audit firm pre-outreach queue</name>
  <files>
    docs/audit-shortlist-outreach.md
  </files>
  <read_first>
    - CLAUDE.md (verify content references GSD workflow per PROTO-08)
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §10 Q10 (Audit firm shortlisting timing — Trail of Bits Bitcoin team, Block Digital Contracting, Cure53, Quarkslab, NCC Group, Galaxy Audit; 8-14 week lead times warrant Phase 1 Week 4-5 outreach)
  </read_first>
  <action>
    PART A — PROTO-08 verification:

    Already-existing artifact check. Run:
    ```bash
    test -f CLAUDE.md && grep -q "GSD" CLAUDE.md && grep -q "/gsd-plan-phase" CLAUDE.md
    ```
    Document the verification result by creating a note in docs/audit-shortlist-outreach.md preamble that PROTO-08 is verified via this commit hash. No CLAUDE.md edits needed — already-existing artifact is the closure condition per CONTEXT.md "CLAUDE.md exists, GSD workflow referenced (PROTO-08 already satisfied — verification task only)".

    PART B — Audit firm pre-outreach (per RESEARCH §10 Q10 — Phase 3 audit slots have 8-14 week lead times; sending initial inquiries in Phase 1 Week 4-5 locks Q4 2026 audit slot):

    Create `docs/audit-shortlist-outreach.md` documenting:

    ```markdown
    # Audit Firm Shortlist & Outreach (Phase 1 → Phase 3)

    **Purpose:** Phase 3 (SEC-01 Mainnet Launch & Hardening) requires external security audit by a firm with Bitcoin DLC + Cashu expertise. Audit firms typically have 8-14 week lead times. This document tracks Phase-1-week-4-5 initial inquiry outreach to secure a Q4 2026 audit slot.

    **PROTO-08 verification (recorded here per Plan 01 Task 3):** CLAUDE.md exists at repo root with GSD workflow references; verified at commit <hash>.

    ## Shortlist (priority order per RESEARCH.md §6.2 + PITFALLS.md Pitfall 5)

    1. **Trail of Bits Bitcoin team** — best track record on threshold sigs (FROST DKG disclosure Feb 2024). Contact: audits@trailofbits.com. Outreach status: PENDING.
    2. **Block Digital Contracting** — Bitcoin-specific. Contact: TBD. Outreach status: PENDING.
    3. **Cure53** — frontend + crypto integration. Contact: pretty@cure53.de. Outreach status: PENDING.
    4. **Quarkslab** — low-level crypto. Contact: contact@quarkslab.com. Outreach status: PENDING.
    5. **NCC Group** — protocol audits, LN history. Contact: TBD. Outreach status: PENDING.
    6. **Galaxy Audit / Inference Security** — Bitcoin DLC-specific. Contact: TBD. Outreach status: PENDING.

    ## Outreach Template

    ```text
    Subject: Audit inquiry — Hunch (Bitcoin prediction market protocol, ~Q4 2026 timeline)

    Hi <firm>,

    I'm the protocol maintainer for Hunch (https://github.com/Silexperience210/hunch),
    a permissionless Bitcoin-native prediction market protocol combining DLC + Cashu NUT-CTF
    (PR #337) + FROST k-of-n (frost-secp256k1-tr v2.2+, RFC 9591) + Lightning + Nostr.

    We are scoping our Phase 3 security audit (per ROADMAP at https://github.com/.../ROADMAP.md)
    and would like to discuss your firm's availability for a comprehensive audit in Q4 2026.

    Scope candidates:
    - Cashu mint code (NUT-CTF integration, NUT-12 DLEQ, blind-sig flow)
    - DLC contract construction (CET enumeration, refund timeout, adapter sigs)
    - FROST DKG + signing (Pedersen DKG with TOB Feb 2024 fix verified)
    - LDK Node integration (post-v0.1.1 fixes)
    - Oracle Schnorr signing (BIP-340 + NIP-88)

    Budget range: $75-180K single-firm, $120-250K dual-firm.

    Would your team be open to a scoping call? Please reply via Nostr DM (npub-TBD — set in Plan 02)
    or via this address (PGP key available).

    Maintainer is pseudonymous; legal entity contracting is via foundation counsel
    (LEGAL-02 deliverable, Plan 04). Communications can be Nostr-native or PGP-encrypted email.

    Thanks,
    Silex (Hunch protocol maintainer)
    ```

    ## Outreach Log

    | Firm | Date sent | Channel | Response | Slot offered |
    |------|-----------|---------|----------|--------------|
    | (none yet) |        |         |          |              |

    ## Note on npub placeholder

    The outreach template references `npub-TBD` because the project Nostr pseudonym key is generated in Plan 02 Task 2. Outreach emails should either be sent after Plan 02 publishes the npub, OR sent now with the placeholder + a follow-up message containing the npub once available.
    ```

    Send initial inquiry emails to all 6 firms using the outreach template (substituting <firm> in salutation). Log each in the table.

    **If email-sending is not autonomously feasible** (no SMTP credentials, no mail CLI configured), this becomes a `checkpoint:human-action`: user must send the emails manually from the outreach template. Record in the outreach log that emails are queued for manual send.

    Commit message: `docs(01-foundation): PROTO-08 verification + Phase 3 audit firm outreach queue (RESEARCH §10 Q10)`.
  </action>
  <verify>
    <automated>
      bash -c "test -f docs/audit-shortlist-outreach.md && grep -q 'Trail of Bits' docs/audit-shortlist-outreach.md && grep -q 'Block Digital Contracting' docs/audit-shortlist-outreach.md && grep -q 'PROTO-08 verification' docs/audit-shortlist-outreach.md && test -f CLAUDE.md && grep -q 'GSD' CLAUDE.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists at repo root and references GSD workflow (PROTO-08 closed).
    - `docs/audit-shortlist-outreach.md` exists with all 6 firms shortlisted, an outreach template, and an outreach log table.
    - PROTO-08 verification note recorded with commit hash.
    - Outreach emails sent (autonomous) OR queued for human-action (documented in outreach log).
  </acceptance_criteria>
  <done>PROTO-08 verified and recorded. Phase 3 audit firm outreach is in motion within the 8-14 week lead-time window. Outreach template includes deferred npub placeholder for follow-up after Plan 02 key gen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create GitHub repo + configure Radicle mirror + Codeberg mirror + README ## Mirrors section (PROTO-07 partial — repo published)</name>
  <files>
    README.md
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §10 Q8 (Radicle setup timing; operationally non-trivial; allocate 1-2 days)
    - .planning/research/PITFALLS.md Pitfall 13 (multi-host defense in depth)
    - existing README.md (verify content present per D-07)
    - CLAUDE.md (cypherpunk principle 8: Tor + IPFS first; corollary: GitHub + Radicle + Codeberg mirroring)
  </read_first>
  <action>
    PART A — Create GitHub repo (PROTO-07 partial):

    1. `gh repo create Silexperience210/hunch --public --description "Hunch — permissionless cypherpunk prediction market protocol on Bitcoin"`. If repo exists already, this is a no-op (gh returns error which is fine).
    2. Verify `git remote -v` has `origin` set to `git@github.com:Silexperience210/hunch.git` (or `https://github.com/Silexperience210/hunch.git`). Add if missing: `git remote add origin git@github.com:Silexperience210/hunch.git`.
    3. Push current main branch: `git push -u origin main`. Verify push succeeds and repo URL is live: `curl -sI https://github.com/Silexperience210/hunch | head -1 | grep -q "200\\|301"`.

    **If GitHub auth fails (no gh credentials, SSH key missing):** this becomes a `checkpoint:human-action` — user must authenticate gh CLI and run the repo creation + push manually. Document state in README.md "## Mirrors" section: "GitHub: PENDING — user must `gh auth login` and `gh repo create Silexperience210/hunch --public && git push -u origin main`".

    PART B — Radicle mirror (PROTO-07 partial, per RESEARCH §10 Q8):

    1. Install `rad` CLI via official curl install: `curl -sSf https://radicle.xyz/install.sh | sh`. Verify: `rad --version`.
    2. Initialize: `rad init --name hunch --description "Hunch protocol — permissionless cypherpunk prediction market on Bitcoin" --default-branch main --public`.
    3. Capture the Radicle ID (rad:z...) from rad init output.
    4. Push: `git push rad main` (the `rad` remote is auto-added by `rad init`).
    5. Verify: `rad sync` succeeds; `git remote -v | grep rad` returns the rad remote URL.

    **If rad install or init fails (Linux-only tooling, network blocked):** this becomes a `checkpoint:human-action` — document in README.md "## Mirrors" section: "Radicle: PENDING — user must install rad and `rad init --name hunch --public`".

    PART C — Codeberg mirror (defense-in-depth per PITFALLS Pitfall 13):

    1. Create Codeberg account if missing (https://codeberg.org/user/signup) — likely a `checkpoint:human-action` step.
    2. Create repo `Silex/hunch` on Codeberg.
    3. Add remote: `git remote add codeberg git@codeberg.org:Silex/hunch.git`.
    4. Push: `git push codeberg main`.

    **If Codeberg signup blocks autonomous execution:** document in README.md "## Mirrors" section: "Codeberg: PENDING — user must create account + repo".

    PART D — Update README.md ## Mirrors section:

    Append (or update if present) a `## Mirrors` section to README.md:

    ```markdown
    ## Mirrors

    Hunch is mirrored across multiple hosts to resist deplatforming (cypherpunk principle 8 — Tor + IPFS first):

    - **GitHub (primary):** https://github.com/Silexperience210/hunch
    - **Radicle (p2p):** rad:z... (capture from `rad init` output)
    - **Codeberg (fallback):** https://codeberg.org/Silex/hunch
    - **Tor hidden service:** (Phase 2 deliverable)
    - **IPFS pin:** (Phase 2 deliverable)

    All mirrors track `main`. Reproducibility check: `git fetch --all && git log --oneline -5 origin/main rad/main codeberg/main` should show identical commit hashes.
    ```

    Run `bash scripts/verify-repo.sh` — should fully pass now (Radicle remote check + LICENSE/CONTRIBUTING/CoC checks all green).

    Commit message: `chore(01-foundation): publish to GitHub + Radicle + Codeberg mirrors + README ## Mirrors section (PROTO-07)`.
  </action>
  <verify>
    <automated>
      bash -c "grep -q '## Mirrors' README.md && grep -q 'github.com.*Silexperience210/hunch' README.md && ( grep -q 'rad:z\\|Radicle: PENDING\\|radicle.xyz' README.md ) && ( grep -q 'codeberg.org\\|Codeberg: PENDING' README.md ) && curl -sI https://github.com/Silexperience210/hunch 2>/dev/null | head -1 | grep -q '200\\|301' || echo 'GitHub URL not live yet — checkpoint deferred to user'"
    </automated>
  </verify>
  <acceptance_criteria>
    - GitHub repo `Silexperience210/hunch` exists (or user-action queued in README.md ## Mirrors section).
    - Radicle mirror initialized (or user-action queued).
    - Codeberg mirror configured (or user-action queued).
    - README.md has a `## Mirrors` section listing GitHub URL + Radicle ID + Codeberg URL (or PENDING markers for any not-yet-complete mirrors).
    - `bash scripts/verify-repo.sh` exits 0 (or documents which checks are pending user action).
  </acceptance_criteria>
  <done>PROTO-07 repo publication complete (or queued for user-action with clear instructions). The GitHub repo URL `https://github.com/Silexperience210/hunch` is live and referenced by Plan 02 (reviewer outreach DM templates) + Plan 04 (counsel outreach engagement-letter scoping emails). Radicle + Codeberg defense-in-depth mirrors live or queued. README ## Mirrors section makes mirror inventory discoverable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 deliverables: corrigendum on 4 stale research docs, Cargo workspace skeleton with 6 empty crates building cleanly, CONTRIBUTING.md + CODE_OF_CONDUCT.md pseudonymity-hardened, .gitignore protecting project pseudonym key, CI workflow, verification scripts (verify-repo, verify-hips, verify-kind-collisions), PROTO-08 verification recorded, Phase 3 audit firm pre-outreach queue, and GitHub + Radicle + Codeberg mirrors live (or queued for user-action).

    With this in place: Plan 02 (HIPs) can publish HIPs to Nostr referencing the live GitHub URL. Plan 03 (spikes) can add `hunch-mint-spike` + `hunch-oracle-spike` as workspace members. Plan 04 (Legal) can reference the GitHub URL in counsel outreach emails.
  </what-built>
  <how-to-verify>
    1. **Pseudonymity hygiene** — Run from repo root:
       ```bash
       git log --format="%an <%ae> | %s" | head -10
       grep -ri "alexandre\\|cangini\\|gmail" --include="*.md" --include="*.toml" --include="*.rs" --include="*.ts" --include="*.json" . 2>/dev/null | grep -v "^./.git/\\|^./node_modules/\\|^./target/"
       ```
       Confirm: zero real-name leakage in any tracked file. All commits authored under "Silex <silex@hunch.markets>" or equivalent pseudonym.

    2. **Workspace compile sanity** — `cargo build --workspace` exits 0; `cargo test --workspace` exits 0; `bash scripts/verify-repo.sh` mostly passes (Radicle remote check passes if Radicle init succeeded; otherwise note PENDING in README ## Mirrors).

    3. **Corrigendum present** — Open STACK.md / ARCHITECTURE.md / FEATURES.md / SUMMARY.md. Confirm each has a "## 2026-05-28 Corrigendum" near the top mentioning PR #337.

    4. **Mirrors live** — Open https://github.com/Silexperience210/hunch in browser; confirm repo content visible. Check Radicle ID is in README.md ## Mirrors (or PENDING note). Check Codeberg URL in README.md ## Mirrors (or PENDING note).

    5. **Decide:** Type `approved` to proceed to Plan 02 (HIPs drafting + Nostr publication + reviewer outreach). Or describe required revisions.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → git remote (GitHub/Radicle/Codeberg) | Pseudonymous commit metadata crosses here; real-name leakage would deanonymize maintainer |
| .planning/research/* corrigendum → downstream agent prompts | Stale research content (pre-corrigendum) could mislead Plans 02 / 03 / 04 if read first |
| Cargo workspace stub → Plan 03 spike crates | Spike crates added to workspace.members in Plan 03; conflict-free file ownership preserved |
| GitHub repo URL → Plan 02 outreach DMs + Plan 04 counsel emails | Repo must exist before downstream plans reference URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | git commit metadata (real-name leakage) | mitigate | Per-repo `git config user.name/user.email` to pseudonym before first push; `.gitignore` protects Silex_0xF777C5B8_SECRET.asc; CI fails if commit author email matches real-name pattern (future hardening) |
| T-01-02 | Tampering | Stale research docs read by Plans 02-04 without corrigendum | mitigate | Task 1 prepends corrigendum at top of each stale doc; subsequent plans read corrected docs first |
| T-01-03 | Information Disclosure | Silex_0xF777C5B8_SECRET.asc accidentally committed | mitigate | .gitignore explicitly blocks *SECRET* + Silex_0xF777C5B8_SECRET.asc (Task 1); CI scans for nsec1/PGP private-key patterns (Phase 2 hardening) |
| T-01-04 | DoS | GitHub deplatform after repo publication | accept | Multi-mirror defense-in-depth (Radicle p2p + Codeberg + Tor + IPFS later phases); Radicle is non-platform-controlled |
| T-01-05 | Elevation of Privilege | Contributor pushes commits with real-name authorship | mitigate | CONTRIBUTING.md (updated Task 1) mandates per-repo git config; CI can be hardened to reject commits with real-name email patterns (Phase 2) |
| T-01-06 | Spoofing | Audit firm impersonation in outreach reply | mitigate | First-contact PGP fingerprint verification + voice-call confirmation before retainer wire; audit-shortlist-outreach.md records firm + contact channel for cross-reference |
| T-01-07 | Tampering | Cargo workspace stub silently includes spike crates from Plan 03 | mitigate | Workspace members list pinned to 6 production crates in Task 2; Plan 03 explicitly amends this list with `hunch-mint-spike` + `hunch-oracle-spike` and marks both `publish = false` |
</threat_model>

<verification>
- 2 requirements (PROTO-07 + PROTO-08) closed per RESEARCH.md §7.2 falsification conditions.
- `bash scripts/verify-repo.sh && cargo build --workspace && cargo test --workspace` exits 0.
- `git log --format="%an <%ae>"` shows ONLY pseudonym across all Plan 01 commits.
- The .planning/research/* corrigendum is visible at the top of STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md (each mentions PR #337).
- GitHub repo URL is live (https://github.com/Silexperience210/hunch returns 200); Radicle + Codeberg mirrors configured or PENDING-flagged in README ## Mirrors.
- CLAUDE.md exists at root (PROTO-08).
- Audit firm pre-outreach queue exists with 6 firms shortlisted.
</verification>

<success_criteria>
- 2/2 Phase 1 requirements closed by this plan: PROTO-07, PROTO-08.
- Phase 1 ROADMAP Success Criterion #1 partially staged: repo foundation (GitHub + Radicle + Codeberg) ready to host the HIPs landing in Plan 02.
- The pseudonymity discipline (D-05) is upheld across every committed file.
- The NUT-CTF / PR #337 pivot (D-01) is reflected in the corrigenda on 4 research docs (D-02).
- Cargo workspace builds with 6 crates (D-08) — Plan 03 (Technical Spikes) can now add `crates/hunch-mint-spike` and `crates/hunch-oracle-spike` to workspace.members.
- Total files modified: ~14 source files (workspace skeleton + repo metadata + scripts + CI + corrigenda) — well under the 15-file ceiling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cypherpunk-foundation/01-01-SUMMARY.md` documenting:
- Workspace crate inventory + build status + Cargo.lock contents (toplevel deps)
- Corrigendum trail (4 research docs + commit hash)
- Repo metadata status (CONTRIBUTING.md + CODE_OF_CONDUCT.md + .gitignore changes)
- Mirror status: GitHub URL, Radicle ID, Codeberg URL (or PENDING markers)
- PROTO-08 verification commit hash
- Audit firm outreach send status (autonomous vs queued for user)
- Open follow-up items (e.g., Radicle/Codeberg if PENDING; npub follow-up to audit firms after Plan 02)
</output>
