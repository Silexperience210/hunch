---
phase: 01-cypherpunk-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
  - Cargo.toml
  - crates/hunch-protocol/Cargo.toml
  - crates/hunch-protocol/src/lib.rs
  - crates/hunch-mint/Cargo.toml
  - crates/hunch-mint/src/lib.rs
  - crates/hunch-oracle/Cargo.toml
  - crates/hunch-oracle/src/lib.rs
  - crates/hunch-relay/Cargo.toml
  - crates/hunch-relay/src/lib.rs
  - crates/hunch-matcher/Cargo.toml
  - crates/hunch-matcher/src/lib.rs
  - crates/hunch-cli/Cargo.toml
  - crates/hunch-cli/src/main.rs
  - docs/HIP-0.md
  - docs/HIP-1.md
  - docs/HIP-2.md
  - docs/HIP-3.md
  - docs/HIP-4.md
  - docs/HIP-5.md
  - docs/MANIFESTO.md
  - docs/HIPs-reviews.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - .gitignore
  - .github/workflows/verify.yml
  - scripts/verify-repo.sh
  - scripts/verify-hips.sh
  - scripts/verify-kind-collisions.ts
  - scripts/publish-hip.ts
  - package.json
autonomous: false
requirements: [PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05, PROTO-06, PROTO-07, PROTO-08]
requirements_addressed: [PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05, PROTO-06, PROTO-07, PROTO-08]
tags: [hunch, protocol, hip, nostr, dlc, cashu, frost, bitcoin]

must_haves:
  truths:
    - "Stale research docs (STACK/ARCHITECTURE/FEATURES/SUMMARY) flag PR #128 as obsolete and point to PR #337"
    - "Cargo workspace builds with 6 empty crates (hunch-protocol, hunch-mint, hunch-oracle, hunch-relay, hunch-matcher, hunch-cli)"
    - "All 6 HIPs exist in docs/HIP-0.md ... docs/HIP-5.md with required section structure"
    - "Manifesto exists at docs/MANIFESTO.md, ~500-1000 words, cypherpunk register"
    - "HIP-3 references PR #337 (NUT-CTF) as the chosen path, NOT PR #128"
    - "HIP-1 reserves Hunch kind ranges 308xx/388xx + cites NIP-88 PR #1681 for kinds 88/89"
    - "HIP-4 cites RFC 9591 + BIP-445 + frost-secp256k1-tr v2.2+ Trail of Bits Feb 2024 fix"
    - "CONTRIBUTING.md + CODE_OF_CONDUCT.md exist with pseudonymous attribution only (no real-name leakage)"
    - "All 6 HIPs published to Nostr as kind:30023 long-form events under project pseudonym key"
    - "Each HIP has at least one external Bitcoin/Cashu/DLC contributor review documented in docs/HIPs-reviews.md"
    - "Radicle mirror configured and reachable; GitHub remote configured"
    - "CLAUDE.md exists at root and references GSD workflow (PROTO-08 verification)"
  artifacts:
    - path: ".planning/research/STACK.md"
      provides: "Corrigendum: PR #128 marked obsolete, PR #337 marked live"
      contains: "PR #337"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Corrigendum: bilateral DLC pattern marked obsolete; NUT-CTF marked live"
      contains: "PR #337"
    - path: ".planning/research/FEATURES.md"
      provides: "Corrigendum: NUT-DLC references updated to NUT-CTF"
      contains: "PR #337"
    - path: ".planning/research/SUMMARY.md"
      provides: "Corrigendum: NUT-DLC narrative updated to NUT-CTF"
      contains: "PR #337"
    - path: "Cargo.toml"
      provides: "Workspace manifest declaring 6 member crates"
      contains: "[workspace]"
    - path: "crates/hunch-protocol/src/lib.rs"
      provides: "Shared types crate stub (compiles empty)"
      min_lines: 1
    - path: "crates/hunch-mint/src/lib.rs"
      provides: "Mint crate stub"
      min_lines: 1
    - path: "crates/hunch-oracle/src/lib.rs"
      provides: "Oracle crate stub"
      min_lines: 1
    - path: "crates/hunch-relay/src/lib.rs"
      provides: "Relay crate stub"
      min_lines: 1
    - path: "crates/hunch-matcher/src/lib.rs"
      provides: "Tier-2 matcher crate stub"
      min_lines: 1
    - path: "crates/hunch-cli/src/main.rs"
      provides: "CLI crate stub"
      min_lines: 1
    - path: "docs/HIP-0.md"
      provides: "Protocol overview + manifesto reference"
      contains: "## Cypherpunk Manifesto"
    - path: "docs/HIP-1.md"
      provides: "Nostr event kinds spec"
      contains: "30888"
    - path: "docs/HIP-2.md"
      provides: "DLC contract structure spec"
      contains: "INVALID"
    - path: "docs/HIP-3.md"
      provides: "Cashu NUT-CTF integration spec"
      contains: "PR #337"
    - path: "docs/HIP-4.md"
      provides: "Multi-oracle FROST protocol"
      contains: "RFC 9591"
    - path: "docs/HIP-5.md"
      provides: "Reputation event format"
      contains: "kind:30891"
    - path: "docs/MANIFESTO.md"
      provides: "Cypherpunk manifesto under project pseudonym"
      contains: "Trust the math"
    - path: "docs/HIPs-reviews.md"
      provides: "External reviewer attestation tracking"
      contains: "## HIP-0"
    - path: "scripts/verify-repo.sh"
      provides: "Repo verification script (LICENSE/CONTRIBUTING/CoC/Radicle)"
      contains: "MIT License"
    - path: "scripts/publish-hip.ts"
      provides: "NIP-23 long-form publisher for HIPs (kind 30023)"
      contains: "30023"
  key_links:
    - from: "docs/HIP-3.md"
      to: "https://github.com/cashubtc/nuts/pull/337"
      via: "References section + Specification section"
      pattern: "pull/337"
    - from: "docs/HIP-1.md"
      to: "NIP-88 PR #1681"
      via: "Backwards Compatibility section"
      pattern: "1681"
    - from: "docs/HIP-4.md"
      to: "RFC 9591 + frost-secp256k1-tr"
      via: "References section"
      pattern: "9591"
    - from: "Cargo.toml"
      to: "crates/hunch-*"
      via: "[workspace.members]"
      pattern: "members"
    - from: "scripts/publish-hip.ts"
      to: "wss://relay.damus.io + nos.lol + relay.nostr.band + relay.hunch.markets"
      via: "explicitRelayUrls config"
      pattern: "relay.damus.io"
---

<objective>
Establish the cypherpunk identity of Hunch: corrigendum on stale research docs (PR #128 → PR #337 pivot), Cargo workspace skeleton with 6 empty crates, all 6 HIPs (HIP-0..5) drafted + linted + published to Nostr long-form (NIP-23), Manifesto, repo metadata (CONTRIBUTING, CODE_OF_CONDUCT, .gitignore, CI workflow), verification scripts, and at least one external Bitcoin/Cashu/DLC contributor review per HIP recorded in docs/HIPs-reviews.md.

Purpose: HIPs are the spec; the spec is the protocol; the protocol is what survives any single operator. Without published, externally-reviewed HIPs, Hunch is just code under a name. This plan turns Hunch into a protocol others can implement.

Output: 8 requirements closed (PROTO-01 through PROTO-08), the .planning/research/* corrigendum trail visible in git history, a Cargo workspace that compiles, 6 HIP markdown files lint-clean, 6 NIP-23 long-form events published on Nostr with naddr1 footers in each HIP, and ≥6 external reviewer attestation records (one per HIP minimum).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cypherpunk-foundation/01-CONTEXT.md
@.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@LICENSE
@CONTRIBUTING.md
@README.md

<interfaces>
<!-- Locked decisions from CONTEXT.md (D-01..D-08) — non-negotiable. -->

D-01 NUT-DLC strategy: Path A (NUT-CTF, PR #337). HIP-3 written against PR #337's Conditional Token Framework, NOT bilateral-DLC PR #128 (CLOSED 2025-05-20).
D-02 Wave 0 corrigendum: .planning/research/STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md must mark PR #128 as obsolete and point to PR #337 BEFORE HIP drafting.
D-03 Lightning-DLC: NO-GO confirmed (defers to v2; SPIKE-04 in Plan 02 is assessment doc).
D-04 LEGAL-01 = jurisdiction comparison memo with counsel input; working hypothesis Swiss Stiftung + BVI BC.
D-05 Full pseudonymity: all commits signed under Silex_0xF777C5B8 pseudonym key. No real-name in LICENSE, CONTRIBUTING.md, CODE_OF_CONDUCT.md, HIP authorship.
D-06 HIP publication: two channels — docs/HIP-N.md (repo) + Nostr kind:30023 long-form under project pseudonym key.
D-07 Repo scaffold partial: LICENSE, CONTRIBUTING.md, README.md, .gitignore, CLAUDE.md exist (commit 96966ab). Build on existing.
D-08 Stack locked: Cargo workspace root with crates hunch-protocol, hunch-mint, hunch-oracle, hunch-relay, hunch-matcher, hunch-cli. frost-secp256k1-tr v2.2+. rust-dlc + DDK alpha.

<!-- Provisional Nostr event kinds (HIP-1 reserves these): -->
- 30888 (parameterized replaceable) → market
- 38888 (parameterized replaceable) → order
- 30890 (parameterized replaceable) → dispute
- 30891 (parameterized replaceable) → reputation
- 88 (regular) → oracle announce (NIP-88 PR #1681 draft)
- 89 (regular) → oracle attestation (NIP-88 PR #1681 draft)
- 30023 (parameterized replaceable, addressable) → HIP long-form publication (NIP-23)

<!-- Existing repo state (verified 2026-05-28 via git status + ls): -->
- LICENSE: MIT, Copyright "Hunch contributors" (pseudonymous-compatible already)
- CONTRIBUTING.md: draft, mentions Phase 1 deliverable status, no real-name leakage
- README.md: exists, project identity present
- CLAUDE.md: exists, GSD workflow referenced (PROTO-08 already satisfied — verification task only)
- Silex_0xF777C5B8_SECRET.asc: project pseudonym GPG key present at root

<!-- HIP template structure (all HIPs must follow): -->
```markdown
# HIP-N: <Title>

**Status:** Draft
**Author(s):** Silex <npub...> (pseudonym)
**Created:** 2026-05-28
**Updated:** 2026-05-28
**Replaces:** (optional)
**Depends on:** (optional)

## Abstract
## Motivation
## Specification
### <Subsections>
## Rationale
## Reference Implementation
## Backwards Compatibility
## Security Considerations
## Test Vectors
## References

<!-- Footer (added on publication to Nostr): -->
*Published on Nostr as <naddr1...>*
```

<!-- External reviewer target list (recruit one or more per HIP): -->
- HIP-0 (manifesto / overview): any Bitcoin/cypherpunk-adjacent voice (Calle, conduition, mononaut, Lyn Alden, Adam Back if reachable)
- HIP-1 (Nostr event kinds): fiatjaf, hodlbod, vitorpamplona, mikedilger, jb55, semisol
- HIP-2 (DLC contract structure): Tibo Le Guilly (rust-dlc), benthecarman, Suredbits team, ChrisStewart-Casa
- HIP-3 (Cashu NUT-CTF integration): joemphilips (NUT-CTF author — already in our outreach plan via SPIKE-01), Calle (cashubtc maintainer), conduition, Gandlaf
- HIP-4 (FROST multi-oracle): Tim Ruffing (Blockstream/ROAST), Jonas Nick (Blockstream), Chelsea Komlo (ZF FROST), Conrado Gouvea (ZF), Trail of Bits crypto team
- HIP-5 (Reputation event format): fiatjaf, hodlbod, Mostro maintainers, Robosats team
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave 0 corrigendum on stale research docs (PR #128 → PR #337) + repo metadata pseudonymity hygiene check</name>
  <files>
    .planning/research/STACK.md,
    .planning/research/ARCHITECTURE.md,
    .planning/research/FEATURES.md,
    .planning/research/SUMMARY.md,
    CONTRIBUTING.md,
    CODE_OF_CONDUCT.md,
    .gitignore
  </files>
  <read_first>
    - .planning/research/STACK.md (current state — find all PR #128 references via grep)
    - .planning/research/ARCHITECTURE.md (Pattern 3 bilateral DLC discussion — find all PR #128 references)
    - .planning/research/FEATURES.md (NUT-DLC user flow discussion)
    - .planning/research/SUMMARY.md (executive summary's NUT-DLC narrative)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-01, D-02 locked decisions)
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §3 (PR #128 closed May 20 2025; PR #337 architectural deltas) and §14 ("Deprecated/outdated content" list)
    - CONTRIBUTING.md (verify no real-name leakage per D-05)
    - LICENSE (verify Copyright "Hunch contributors" — no real-name; do NOT modify)
  </read_first>
  <action>
    PART A — Corrigendum on 4 stale research docs (per D-02):

    For each of STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md:
    1. Grep for every occurrence of "PR #128", "NUT-DLC", "bilateral DLC", "conduition", "PR 128".
    2. Prepend a "## 2026-05-28 Corrigendum" section at the top of each file (immediately after the H1 title) with this exact structure:

       ```markdown
       ## 2026-05-28 Corrigendum — NUT-DLC pivot to NUT-CTF (PR #337)

       **Status update:** Cashu NUTs PR #128 (bilateral NUT-DLC by conduition) was **CLOSED 2025-05-20** by thesimplekid with comment "Closing as there is no active work. Please reopen if work continues." This document was written 2026-05-27 assuming PR #128 was the live critical path. That assumption is **WRONG**.

       **New chosen path (locked in CONTEXT.md decision D-01):** Path A — **NUT-CTF (Conditional Token Framework, PR #337 by joemphilips, opened 2026-02-07)**. NUT-CTF is architecturally distinct: oracle-agnostic mint with conditional token issuance + split-merge for secondary market, instead of mint-as-bilateral-counterparty.

       **References below to "NUT-DLC", "PR #128", "bilateral DLC mint" should be read as NUT-CTF / PR #337 / oracle-agnostic conditional token framework** unless explicitly marked as historical context.

       See `.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md` §3 for the deep-dive on this pivot.
       ```

    3. Do NOT remove the original PR #128 content; the corrigendum at the top is sufficient signal. Preserve research provenance per assumption A3 in RESEARCH.md §Assumptions Log.

    PART B — Pseudonymity hygiene check + CODE_OF_CONDUCT.md creation (per D-05):

    1. Read existing CONTRIBUTING.md. Verify no real-name (no "Silex" outside the Silex npub pseudonym, no "Alexandre", no "alexandre.cangini@gmail.com"). If any leakage found, replace with project pseudonym placeholder (`Silex <npub-TBD>` for now; npub will be set in Task 5).
    2. Update CONTRIBUTING.md "Security" section: replace `[TBD — Phase 1 deliverable]` with `nostr DM to npub-TBD (see SECURITY.md, Phase 2 deliverable) — or PGP-encrypted email to <pseudonym>@protonmail.com (TBD)`. Add a sentence: "All contributor attribution uses Nostr npub or pseudonym; do not submit PRs with real-name commit authorship — set per-repo `git config user.name "<pseudonym>"` and `user.email "<pseudonym>@hunch.markets"` before pushing."
    3. Update CONTRIBUTING.md "Commit Style" section: add bullet `- "Sign all commits under the project pseudonym GPG key (Silex_0xF777C5B8). Real-name commits will be rejected on the public repo."`
    4. Create CODE_OF_CONDUCT.md based on Contributor Covenant v2.1 (https://www.contributor-covenant.org/version/2/1/code_of_conduct/) BUT with these Hunch-specific adjustments:
       - Replace contact email with `nostr DM to npub-TBD (Phase 2 deliverable) — or PGP-encrypted email to <pseudonym>@protonmail.com (TBD)`
       - Add a "Pseudonymity" section at the bottom (after Enforcement): "Hunch contributors are encouraged to operate under pseudonym. Real-name disclosure is never required. Maintainers do not collect contributor real names. Doxxing of any contributor — including maintainers — is grounds for permanent ban."
    5. Update .gitignore to ensure `Silex_0xF777C5B8_SECRET.asc` is NOT accidentally committed (verify line `*SECRET*` or explicit `Silex_0xF777C5B8_SECRET.asc` is present; add if missing). Also add `docs/legal/signoff/*.pdf` (counsel PDFs encrypted-or-private), `.env`, `target/`, `node_modules/`, `dist/`, `.next/`.

    Commit message: `docs(01-foundation): corrigendum on stale research docs (PR #128 → PR #337) + pseudonymity hygiene for repo metadata`. Use per-repo git config to set author to pseudonym before commit: `git config user.name "Silex" && git config user.email "silex@hunch.markets"`.
  </action>
  <verify>
    <automated>
      bash -c "grep -l 'PR #337' .planning/research/STACK.md .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/SUMMARY.md | wc -l | grep -q '^4$' && grep -l '2026-05-28 Corrigendum' .planning/research/STACK.md .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/SUMMARY.md | wc -l | grep -q '^4$' && test -f CODE_OF_CONDUCT.md && grep -q 'Pseudonymity' CODE_OF_CONDUCT.md && grep -q 'pseudonym' CONTRIBUTING.md && grep -q 'Silex_0xF777C5B8_SECRET.asc\\|*SECRET*' .gitignore"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 stale research docs (STACK, ARCHITECTURE, FEATURES, SUMMARY) contain a "## 2026-05-28 Corrigendum" section near the top that mentions PR #337.
    - CODE_OF_CONDUCT.md exists, based on Contributor Covenant 2.1, includes a "Pseudonymity" section.
    - CONTRIBUTING.md mentions per-repo git config for pseudonymous commit authorship.
    - .gitignore protects `Silex_0xF777C5B8_SECRET.asc` from accidental commit.
    - `git log --format="%an <%ae>"` on this commit shows ONLY pseudonym (Silex, silex@hunch.markets) — no real-name leakage.
  </acceptance_criteria>
  <done>Wave 0 corrigendum complete (unblocks HIP-3 drafting in Task 3). CODE_OF_CONDUCT.md exists and is pseudonymity-aware. CONTRIBUTING.md updated. .gitignore protects the project pseudonym GPG key. Repo metadata is now consistent with locked decisions D-01, D-02, D-05.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cargo workspace scaffolding (6 crates) + CI workflow + verification scripts + Bun/TS HIP-publishing tooling</name>
  <files>
    Cargo.toml,
    crates/hunch-protocol/Cargo.toml,
    crates/hunch-protocol/src/lib.rs,
    crates/hunch-mint/Cargo.toml,
    crates/hunch-mint/src/lib.rs,
    crates/hunch-oracle/Cargo.toml,
    crates/hunch-oracle/src/lib.rs,
    crates/hunch-relay/Cargo.toml,
    crates/hunch-relay/src/lib.rs,
    crates/hunch-matcher/Cargo.toml,
    crates/hunch-matcher/src/lib.rs,
    crates/hunch-cli/Cargo.toml,
    crates/hunch-cli/src/main.rs,
    .github/workflows/verify.yml,
    scripts/verify-repo.sh,
    scripts/verify-hips.sh,
    scripts/verify-kind-collisions.ts,
    scripts/publish-hip.ts,
    package.json
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §7.4 (Wave 0 gaps list) and §11 (Stack — Phase 1 subset, exact versions + verification commands) and §12 (Code Examples — repo verification script + publish-hip.ts template + FROST DKG skeleton)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md "Stack (locked)" section (D-08: Cargo workspace, 6 crates, frost-secp256k1-tr v2.2+, rust-dlc + DDK alpha)
    - .planning/research/STACK.md (after corrigendum from Task 1) — for full library version table
    - CLAUDE.md "Stack at a Glance" section
    - Existing root: `ls -la` to confirm no Cargo.toml or crates/ directory exists yet (greenfield workspace bootstrap)
  </read_first>
  <action>
    PART A — Cargo workspace bootstrap (per D-08):

    1. Create root `Cargo.toml` as a workspace manifest:
       ```toml
       [workspace]
       resolver = "2"
       members = [
           "crates/hunch-protocol",
           "crates/hunch-mint",
           "crates/hunch-oracle",
           "crates/hunch-relay",
           "crates/hunch-matcher",
           "crates/hunch-cli",
       ]

       [workspace.package]
       version = "0.1.0"
       edition = "2021"
       license = "MIT"
       repository = "https://github.com/Silexperience210/hunch"
       authors = ["Silex <silex@hunch.markets>"]
       rust-version = "1.80"

       [workspace.dependencies]
       tokio = { version = "1.40", features = ["full"] }
       serde = { version = "1", features = ["derive"] }
       serde_json = "1"
       anyhow = "1"
       thiserror = "2"
       ```
       Do NOT add cdk / rust-dlc / ddk / frost / nostr-sdk to workspace.dependencies yet — those are Plan 02 spike concerns. This crate has empty stubs only.

    2. For each of the 6 crates create a minimal Cargo.toml + lib.rs (or main.rs for hunch-cli):

       `crates/hunch-protocol/Cargo.toml`:
       ```toml
       [package]
       name = "hunch-protocol"
       version.workspace = true
       edition.workspace = true
       license.workspace = true
       repository.workspace = true
       authors.workspace = true
       description = "Hunch protocol — shared types and HIP reference types (Phase 1: stub)"

       [dependencies]
       serde.workspace = true
       serde_json.workspace = true
       ```
       `crates/hunch-protocol/src/lib.rs`:
       ```rust
       //! Hunch protocol shared types.
       //!
       //! Phase 1 deliverable: empty crate skeleton. Types arrive in Phase 2 implementing HIP-1..5.
       //! See `docs/HIP-1.md` (Nostr event kinds), `docs/HIP-2.md` (DLC), `docs/HIP-4.md` (FROST).
       ```

       Repeat the same Cargo.toml pattern for hunch-mint, hunch-oracle, hunch-relay, hunch-matcher with appropriate descriptions referencing the corresponding HIP. Each lib.rs is a single-line `//!` doc comment pointing to the relevant HIP. Per RESEARCH.md Pitfall E, do NOT prefix names with "-spike" — those go in Plan 02 as `hunch-mint-spike` and `hunch-oracle-spike` (separate prototype crates).

       `crates/hunch-cli/Cargo.toml`:
       ```toml
       [package]
       name = "hunch-cli"
       version.workspace = true
       edition.workspace = true
       license.workspace = true
       repository.workspace = true
       authors.workspace = true
       description = "Hunch CLI (Phase 1: stub)"

       [[bin]]
       name = "hunch"
       path = "src/main.rs"
       ```
       `crates/hunch-cli/src/main.rs`:
       ```rust
       fn main() {
           println!("hunch-cli v0.1.0 (Phase 1 stub — see ROADMAP Phase 2 for real CLI)");
       }
       ```

    3. Run `cargo build --workspace` — must exit 0.

    PART B — Verification scripts (Wave 0 gap closure per RESEARCH §7.4):

    1. `scripts/verify-repo.sh` — copy the script from RESEARCH §12 verbatim, then chmod +x.

    2. `scripts/verify-hips.sh` — for each `docs/HIP-N.md`: run `mdl` (or `npx markdownlint-cli`) + grep for required sections (`## Abstract`, `## Motivation`, `## Specification`, `## References`); exit non-zero on missing section. Loop over HIP-0..5.

    3. `scripts/verify-kind-collisions.ts` — Bun/TS script that fetches `https://raw.githubusercontent.com/nostr-protocol/nips/master/README.md` and `https://nostrbook.dev/kinds/` and checks that Hunch kinds (30888, 38888, 30890, 30891, 30892, 88, 89) do not collide with anything OTHER than NIP-88 (which is the intended dependency for kinds 88/89). Output: pass/fail report.

    4. `scripts/publish-hip.ts` — adapt RESEARCH §12 template. Reads `docs/HIP-${N}.md`, sets kind 30023, d-tag `hip-${N}`, title from first H1 line, summary from first paragraph after Abstract. Publishes to `wss://relay.hunch.markets`, `wss://relay.damus.io`, `wss://nos.lol`, `wss://relay.nostr.band`. Signer = NDKPrivateKeySigner loaded from `HUNCH_NSEC` env var (not committed). Outputs the naddr1 identifier. Append `*Published on Nostr as <naddr1...>*` footer to the source `docs/HIP-${N}.md` after success.

    PART C — Bun/TS tooling (per CLAUDE.md "Bun" stack):

    1. Create root `package.json`:
       ```json
       {
         "name": "hunch-scripts",
         "version": "0.1.0",
         "private": true,
         "type": "module",
         "scripts": {
           "verify:repo": "bash scripts/verify-repo.sh",
           "verify:hips": "bash scripts/verify-hips.sh",
           "verify:kinds": "bun scripts/verify-kind-collisions.ts",
           "publish:hip": "bun scripts/publish-hip.ts"
         },
         "dependencies": {
           "@nostr-dev-kit/ndk": "^2.10.0"
         },
         "devDependencies": {
           "markdownlint-cli": "^0.41.0",
           "@types/bun": "latest"
         }
       }
       ```

    2. Run `bun install` to populate `bun.lockb`. Commit `bun.lockb`.

    PART D — CI workflow:

    Create `.github/workflows/verify.yml` running on push/pull_request to main:
    - Job 1 "rust": setup rust-toolchain stable, `cargo build --workspace`, `cargo test --workspace`, `cargo audit` (install first), `cargo deny check` (install first).
    - Job 2 "docs": setup bun, `bun install`, `bash scripts/verify-repo.sh`, `bash scripts/verify-hips.sh` (only runs once HIPs exist — guard with `if test -f docs/HIP-0.md`), `bun scripts/verify-kind-collisions.ts`.

    Commit message: `feat(01-foundation): cargo workspace scaffolding + CI workflow + verification scripts + bun HIP-publishing tooling (D-08, RESEARCH §7.4 Wave 0)`.
  </action>
  <verify>
    <automated>
      bash -c "cargo build --workspace 2>&1 | tail -5 | grep -q 'Compiling\\|Finished' && cargo test --workspace 2>&1 | tail -3 | grep -q '0 passed\\|test result' && test -f Cargo.toml && grep -q 'hunch-protocol' Cargo.toml && grep -q 'hunch-mint' Cargo.toml && grep -q 'hunch-oracle' Cargo.toml && grep -q 'hunch-relay' Cargo.toml && grep -q 'hunch-matcher' Cargo.toml && grep -q 'hunch-cli' Cargo.toml && test -x scripts/verify-repo.sh && test -f scripts/publish-hip.ts && test -f scripts/verify-kind-collisions.ts && test -f scripts/verify-hips.sh && test -f .github/workflows/verify.yml && test -f package.json"
    </automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0; 6 member crates compile.
    - `cargo test --workspace` exits 0 (no tests yet, but no compile errors).
    - All 6 crate directories exist at `crates/hunch-{protocol,mint,oracle,relay,matcher,cli}/` with valid Cargo.toml and lib.rs/main.rs.
    - `scripts/verify-repo.sh` is executable and runs without error against current repo state (Radicle remote will fail until Task 6; document that this is expected gate).
    - `scripts/publish-hip.ts` references kind 30023, d-tag `hip-${N}`, and the 4-relay broadcast list.
    - `.github/workflows/verify.yml` exists and runs on push/pull_request.
    - `package.json` declares the 4 npm-scripts and depends on @nostr-dev-kit/ndk@^2.10.
    - `bun install` succeeds; `bun.lockb` committed.
  </acceptance_criteria>
  <done>Workspace skeleton complete and CI-verified. Verification scripts ready. HIP-publishing tooling armed for Task 5. CI workflow runs on every push and protects regression of the workspace + HIP structure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Draft all 6 HIPs (HIP-0 manifesto/overview + HIP-1 Nostr kinds + HIP-2 DLC + HIP-3 NUT-CTF + HIP-4 FROST + HIP-5 reputation) and the standalone Manifesto + initialize HIPs-reviews.md tracker</name>
  <files>
    docs/HIP-0.md,
    docs/HIP-1.md,
    docs/HIP-2.md,
    docs/HIP-3.md,
    docs/HIP-4.md,
    docs/HIP-5.md,
    docs/MANIFESTO.md,
    docs/HIPs-reviews.md
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §2 (HIP authoring conventions, NIP-23 mechanics, DLC spec convention reference posture, Cashu NUT spec conventions), §3 (NUT-DLC pivot to NUT-CTF, full content for HIP-3), §4 (FROST DKG: RFC 9591, TOB fix, 3-of-5 playbook — full content for HIP-4), §6.6 (pseudonymity plan, for HIP-0 voice)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-01 Path A NUT-CTF, D-05 full pseudonymity, D-06 two-channel publication, D-08 stack)
    - .planning/research/PITFALLS.md Pitfall 9 (Polymarket UMA $7M attack — informs HIP-5 reputation design rationale), Pitfall 7 (FROST cryptographic attacks — informs HIP-4 security considerations)
    - .planning/research/ARCHITECTURE.md (after corrigendum — Pattern 2 DLC settlement, Pattern 3 mint architecture, oracle layer)
    - CLAUDE.md "Cypherpunk Principles" section (for HIP-0 manifesto tone)
    - existing CONTRIBUTING.md (updated Task 1)
    - existing LICENSE (MIT, "Hunch contributors")
  </read_first>
  <action>
    Draft 6 HIPs + 1 standalone Manifesto + 1 reviews tracker. Every HIP uses the structure from the `<interfaces>` block above.

    **All HIPs use these frontmatter values (per D-05 pseudonymity):**
    - Status: Draft
    - Author(s): Silex <npub-TBD> (pseudonym; npub set in Task 5 after key generation)
    - Created: 2026-05-28
    - Updated: 2026-05-28

    ---

    **`docs/HIP-0.md` (PROTO-01) — Protocol Overview + Cypherpunk Manifesto reference**

    Sections:
    - Abstract (2-3 sentences): "HIP-0 introduces Hunch, a permissionless cypherpunk prediction market protocol on Bitcoin. It anchors the protocol's identity in a cypherpunk register (Hughes, May, Hettinga, Finney) and indexes HIPs 1-5 which together specify the protocol."
    - Motivation: why Hunch, why now (post-Polymarket QCX 2025 acquisition; post-Storm conviction Aug 6 2025; post-Coplan FBI raid Nov 13 2024). Reference these primary sources by date and source name (e.g., "DOJ SDNY press release Aug 6 2025"). Pull from RESEARCH §1 + §6.
    - Specification: Hunch is a PROTOCOL not a product. Manifesto reference (link to docs/MANIFESTO.md). Index of HIP-1..5 with one-sentence summary each. Reference architecture stack: Bitcoin L1 settlement (DLC), Cashu mints (NUT-CTF — PR #337), Lightning (LDK Node), Nostr (discovery + reputation + oracle attestation).
    - Rationale: 8 cypherpunk principles (verbatim from CLAUDE.md "Cypherpunk Principles" + brief expansion each). Trust the math; no KYC; no US; MIT open source; protocol-first; multi-frontend/mint/oracle; no tokens; Tor + IPFS first.
    - Reference Implementation: `crates/hunch-protocol` (Phase 2 deliverable). Reference frontend `apps/hunch-web` (Phase 2).
    - Backwards Compatibility: N/A (HIP-0 is foundational).
    - Security Considerations: brief — refer to HIP-2 (DLC security), HIP-3 (NUT-CTF trust model), HIP-4 (FROST cryptographic posture), HIP-5 (reputation gameability).
    - Test Vectors: N/A (HIP-0 is non-normative).
    - References: NIP-01, NIP-23, RFC 9591, dlcspecs, PR #337, NIP-88 PR #1681, CLAUDE.md, MANIFESTO.md. CFTC Docket 22-09, 18 USC § 1960, NBC News Coplan raid, DOJ Storm press release.

    Must include the heading `## Cypherpunk Manifesto` (links to MANIFESTO.md per verification key-link).

    ---

    **`docs/MANIFESTO.md` (~500-1000 words, cypherpunk register)**

    Voice: terse, declarative, no fluff. Draws from Tim May's "Crypto Anarchist Manifesto" + Eric Hughes' "Cypherpunk's Manifesto" + Hal Finney's writings + Phil Hettinga's framings. Sample opening: "Cypherpunks write code. Hunch is code that asks questions and lets math answer." Sections (no formal headings — flowing manifesto): the problem (custodial markets, KYC, regulatory choke points, oracle manipulation by token whales), the principle (trust the math, not the operator), the design (DLC + Cashu + FROST + Nostr — no token, no governance, no operator-as-trust-point), the commitment (open source MIT, multi-frontend, multi-mint, multi-oracle, pseudonymous maintainers), the closing ("If Hunch becomes a trust point, it has already failed. Fork it."). End with phrase `Trust the math.` Sign-off: `Silex <npub-TBD>` + date 2026-05-28.

    ---

    **`docs/HIP-1.md` (PROTO-02) — Nostr Event Kinds Specification**

    Sections:
    - Abstract: defines event kinds for Hunch markets, orders, disputes, reputation, oracle announce/attestation; tracks NIP-88 PR #1681 for oracle event kinds.
    - Motivation: discovery + identity + oracle layer on Nostr; need kind reservation to avoid collision with future NIPs.
    - Specification: full event-kind table:
      - **30888** (parameterized replaceable, addressable) — Market announce. `d` tag: market UUID. Tags: `oracle` (npub), `mint` (URL), `outcomes` (YES/NO/INVALID), `expiry` (unix), `resolution_rule` (text). Content: free-form question text + resolution criteria.
      - **38888** (parameterized replaceable) — Order. `d` tag: order ID. Tags: `e` (market event), `side` (YES/NO), `amount_sat`, `price_bps`, `expiry`. Content: optional human-readable note.
      - **30890** (parameterized replaceable, addressable) — Dispute. `d` tag: dispute ID. Tags: `e` (market event), `evidence` (URL or Nostr event ID).
      - **30891** (parameterized replaceable, addressable) — Reputation. `d` tag: target npub. Tags: `target` (npub of oracle/mint/creator), `vote` (vouch/report), `weight` (0-100). See HIP-5 for aggregation.
      - **88** (regular, NIP-88 PR #1681) — Oracle announce. Per NIP-88 draft.
      - **89** (regular, NIP-88 PR #1681) — Oracle attestation. Per NIP-88 draft.
    - Rationale: 308xx range for replaceable + parameterized (markets evolve; reputation aggregates; disputes have lifecycle). 38xxx range for orders (frequent + per-market replaceability). 88/89 inherited from NIP-88 (track upstream).
    - Reference Implementation: `crates/hunch-protocol::events` (Phase 2 module names).
    - Backwards Compatibility: HIP-1 commits Hunch to (1) track NIP-88 PR #1681; if it merges with material changes, publish HIP-1a corrigendum; (2) Hunch reserves 308xx and 38xxx ranges via a public Nostr long-form announce + a PR to `nostr-protocol/nips` repo registering Hunch ranges. **TODO before Status: Final** — open the NIPs-repo PR.
    - Security Considerations: kind collision risk (mitigated by §Backwards Compat); replay attacks on orders (mitigated by `expiry` tag); spam (mitigated by NIP-13 PoW at relay layer per RELAY-03 Phase 2).
    - Test Vectors: 2-3 example events with full JSON for kind 30888 and kind 38888 (include `id`, `pubkey`, `created_at`, `kind`, `tags`, `content`, `sig` — use placeholder sigs).
    - References: NIP-01, NIP-13 PoW, NIP-23 long-form (kind 30023), NIP-88 PR #1681 (https://github.com/nostr-protocol/nips/pull/1681), NIP-88 PR #919 (https://github.com/nostr-protocol/nips/pull/919). Cite `nostrbook.dev/kinds/`.

    ---

    **`docs/HIP-2.md` (PROTO-03) — DLC Contract Structure (binary YES/NO/INVALID, refund timeout, multi-oracle FROST-ready)**

    Sections:
    - Abstract: defines Hunch's DLC contract shape — binary YES/NO/INVALID outcomes, refund timeout fallback, multi-oracle FROST-ready (k-of-n attestation consumable as a single adapter signature per CET).
    - Motivation: DLCs are Bitcoin's primitive for oracle-attested settlement; bilateral DLC primitive (dlcspecs) needs an outcome set definition for prediction markets.
    - Specification:
      - Outcome set: `{YES, NO, INVALID}` (3 enumerated outcomes per dlcspecs/Oracle.md single-nonce enumerated-outcome contract).
      - Payout function: YES → 1.0 to YES holders; NO → 1.0 to NO holders; INVALID → 0.5 to each side (refund CET).
      - Refund timeout: every DLC includes a refund timeout CET activatable after `expiry + 7 days` if oracle has not attested. 50/50 refund.
      - Multi-oracle composition: aggregated FROST k-of-n attestation signature is consumable by a single adapter signature per CET. Mint sees one oracle pubkey + one aggregated signature; underlying federation requires k-of-n cooperation. See HIP-4 for the FROST protocol.
      - CET enumeration: 3 outcomes × N denomination bins. Hunch v1 supports single-bin (full pool to winner). Multi-bin numeric markets deferred to v2 per dlcspecs/NumericOutcome.md.
    - Rationale: INVALID built-in is the Augur-v2 lesson (kind:30890 dispute → oracle attests INVALID → automatic 50/50 refund). Refund timeout is dlcspecs/v0Milestone primitive — covers oracle disappearance failure mode (RESEARCH §6 Pitfall 9). FROST-ready means HIP-4 federation is plug-in without HIP-2 v2.
    - Reference Implementation: `crates/hunch-protocol::dlc` (Phase 2).
    - Backwards Compatibility: numeric / range markets deferred to v2 via digit decomposition (dlcspecs/NumericOutcome.md). Conditional / parlay markets deferred.
    - Security Considerations: oracle bribery (mitigated by HIP-4 FROST k-of-n + HIP-5 reputation); CET malleability (mitigated by Taproot adapter signatures); refund timeout race condition (mitigated by 7-day buffer — operator must broadcast refund CET within window).
    - Test Vectors: example CET enumeration table for a $100 (100M sat) binary market with 3 outcomes.
    - References: dlcspecs/Introduction.md, dlcspecs/Oracle.md, dlcspecs/Messaging.md, dlcspecs/MultiOracle.md, dlcspecs/PayoutCurve.md, dlcspecs/v0Milestone.md. RFC 9591 (FROST), HIP-4. Augur v2 INVALID outcome (CoinDesk Jul 2020).

    Must contain the substring `INVALID` (verification key-link).

    ---

    **`docs/HIP-3.md` (PROTO-04) — Cashu NUT-CTF Integration Specification (PER D-01: written against PR #337, NOT PR #128)**

    Sections:
    - Abstract: defines Hunch's Cashu integration profile using NUT-CTF (PR #337 — Conditional Token Framework by joemphilips), supporting permissionless multi-bettor prediction markets via oracle-agnostic conditional token issuance + split/merge atomic-swap secondary market.
    - Motivation: Phase 1 RESEARCH §3 identified PR #128 (bilateral NUT-DLC) as CLOSED 2025-05-20 by thesimplekid; PR #337 (NUT-CTF) is the architecturally-correct successor for Hunch's permissionless multi-bettor model. CONTEXT.md decision D-01 locks Path A. **HIP-3 is written against PR #337; PR #128 references in this document are historical context only.**
    - Specification:
      - Conditional Token Framework per NUT-CTF base: each market outcome (YES, NO, INVALID) is a distinct tradeable Cashu token type, keyed by `(market_id, outcome)`.
      - Mint role: oracle-agnostic. Mint accepts Lightning deposits, issues YES + NO + INVALID conditional tokens against a DLC funding tx (per HIP-2). Mint does NOT custody the underlying sats at settlement — DLC CET broadcast pays winning side directly from on-chain funding.
      - NUT-CTF-split-merge: trade positions via atomic Cashu swap (kind 38888 order on Nostr signals intent; Cashu split-merge primitive executes off-chain). Secondary market = free entry/exit.
      - NUT-CTF-numeric: deferred to v2 per HIP-2 (binary v1 only).
      - Lightning deposit/withdraw: via NUT-04 mint + NUT-05 melt quotes against Hunch operator's Lightning node.
      - DLEQ proofs (NUT-12): mandatory on every token receive (CLAUDE.md "Do verify Cashu DLEQ proofs").
    - Rationale: NUT-CTF preserves no-custody-at-settlement (DLC fires from funding tx, mint never holds settled funds). Bilateral PR #128 model required mint-as-counterparty abstraction which is awkward for permissionless many-bettor markets. PR #337 is the live upstream conversation; Hunch contributes upstream rather than forks.
    - Reference Implementation: `crates/hunch-mint` (Phase 2); upstream `cashubtc/cdk` v0.14+. Phase 1 prototype = `crates/hunch-mint-spike` (Plan 02, SPIKE-02).
    - Backwards Compatibility: if PR #337 merges with material changes, Hunch publishes HIP-3a corrigendum. If PR #337 stalls and PR #128 revives (unlikely per maintainer signal), Hunch may publish HIP-3 v2 against PR #128 — explicit fallback documented.
    - Security Considerations: mint operator rug risk during market lifetime (mitigated by weekly reserves proofs per MINT-10 Phase 2 + multi-mint protocol-level support); blind-signature anonymity preserved by NUT-12 DLEQ; DoS via HTLC vector (Cashu Nov 2025 disclosure — track CDK patches).
    - Test Vectors: example flow narrative — user deposits 100k sat via Lightning → mint issues 100k YES tokens + 100k NO tokens (one side held by user, other by counterparty mint reserve) → user atomic-swaps NO tokens to a Phase 1 prototype user → market resolves YES → DLC CET broadcasts → user redeems YES tokens for sats via Lightning melt.
    - References: PR #337 (https://github.com/cashubtc/nuts/pull/337), PR #128 (CLOSED — https://github.com/cashubtc/nuts/pull/128), NUT-04, NUT-05, NUT-12, CDK (github.com/cashubtc/cdk), HIP-2, conduition Cashu disclosure (Jul 2025), Cashu HTLC DoS disclosure (Nov 2025).

    Must contain the literal text `PR #337` (verification key-link); must NOT promote PR #128 as live (PR #128 may be cited as historical reference but not as critical-path spec).

    ---

    **`docs/HIP-4.md` (PROTO-05) — Multi-Oracle FROST k-of-n Protocol**

    Sections:
    - Abstract: specifies Hunch's multi-oracle FROST k-of-n protocol for threshold-Schnorr oracle attestation; v1 designs the spec; v1 production runs single-oracle Schnorr with FROST-ready code paths; v2 (Phase 4) deploys production multi-oracle federations.
    - Motivation: single-oracle = single point of trust. Multi-oracle FROST k-of-n means k operators must cooperate to attest, preserving BIP-340 Schnorr signature shape (verifiable as a single signature on-chain). Defends against UMA-style oracle manipulation ($7M Polymarket attack, March 24-25 2025).
    - Specification:
      - Cryptographic protocol: RFC 9591 FROST with frost-secp256k1-tr v2.2+ (Taproot-compatible).
      - **CRITICAL — Trail of Bits Feb 2024 Pedersen DKG fix:** every implementation MUST validate the coefficient commitment vector length = `t` (threshold). frost-secp256k1-tr v2.2+ does this check internally; Hunch's reference impl repeats the check at the application layer per CLAUDE.md "Reserves proofs published weekly" prudence.
      - DKG ceremony (Pedersen, 2-round + finalization): see SPIKE-03 playbook (Plan 02 deliverable `docs/playbooks/FROST-DKG-3of5.md`).
      - Signing ceremony (2-round commit + reveal): coordinator role is transport-only, not authority. Coordinator may withhold/reorder but cannot forge.
      - Coordination channel: Nostr NIP-44 (gift-wrapped DMs), NOT NIP-04. Optional dedicated coordination relay `coord.hunch.markets` (Phase 2 deliverable).
      - Default thresholds:
        - Low-value markets (<100k sat): single Schnorr oracle (v1 production).
        - Medium-value (100k - 10M sat): 3-of-5 FROST.
        - High-value (>10M sat): 5-of-7 FROST.
      - Key rotation: full DKG re-ceremony required (no incremental membership change in classical Pedersen DKG); track ChillDKG (Blockstream BIP-DKG) for v2 async-friendly variant.
    - Rationale: threshold cryptography is native to Schnorr/BIP-340; BIP-445 advances FROST as a native BIP-340 primitive. Trail of Bits Feb 2024 disclosure proved Pedersen DKG implementations had a critical bug for ~6 years across 10+ projects; pinning v2.2+ + repeating the check defensively is non-negotiable per CLAUDE.md "no custom crypto".
    - Reference Implementation: `crates/hunch-oracle` (Phase 2); Phase 1 spike = `crates/hunch-oracle-spike` (Plan 02, SPIKE-03).
    - Backwards Compatibility: v1 single-Schnorr oracle is forward-compatible — single Schnorr is the 1-of-1 degenerate case of FROST k-of-n.
    - Security Considerations: adaptive corruption (Crites et al. — open question; static-corruption proof from FROST paper holds for now); concurrent signing nonce hygiene (mitigated by frost-secp256k1-tr per-process isolation); rogue-key attacks (mitigated by DKG — do NOT support trusted-dealer fallback per CLAUDE.md no custom crypto); k operators colluding (mitigated by HIP-5 reputation + INVALID outcome in HIP-2 + dispute period per HIP-2 Specification).
    - Test Vectors: deferred to SPIKE-03 deliverable (3-of-5 ceremony transcript on signet).
    - References: RFC 9591 (https://datatracker.ietf.org/doc/rfc9591/), FROST paper eprint 2020/852, ROAST paper eprint 2022/550, Trail of Bits Feb 2024 disclosure (https://blog.trailofbits.com/2024/02/20/breaking-the-shared-key-in-threshold-signature-schemes/), ZF Foundation Pedersen DKG remediation, BIP-445 (https://github.com/siv2r/bip-frost-signing), ChillDKG (https://github.com/BlockstreamResearch/bip-frost-dkg), NIP-44, NIP-88 PR #1681. CITED: Polymarket UMA $7M attack (CoinDesk Mar 27 2025).

    Must contain literal text `RFC 9591` (verification key-link).

    ---

    **`docs/HIP-5.md` (PROTO-06) — Reputation Event Format and Aggregation Rules**

    Sections:
    - Abstract: defines kind:30891 Nostr reputation event format + aggregation rules for Hunch oracles, mints, and market creators. Time-decayed weighting + social-graph-aware sum.
    - Motivation: Hunch uses **reputation, not slashing** (PROJECT.md key decision — Bitcoin has no smart contract to slash). Reputation must be cryptographically signed (Nostr), aggregable by frontends, gameable-resistant (Sybil resistance via social graph), and time-decayed (a 2-year-old vouch weighs less than a 2-month-old one).
    - Specification:
      - Event format: kind:30891 (parameterized replaceable, addressable), `d` tag = target npub. Tags: `target` (npub of oracle/mint/creator), `target_type` (oracle | mint | creator), `vote` (vouch | report), `weight` (0-100 int — voter's confidence). Optional `evidence` (URL or event ID). Content: optional free-form note.
      - Replaceability: each (voter, target) pair has at most one current reputation event. Voter updates by publishing a new event with the same `d` tag.
      - Aggregation rules (frontend-side):
        - Raw score: `Σ (weight × decay_factor × graph_distance_factor) for all vouch events` − `Σ (weight × decay_factor × graph_distance_factor) for all report events`.
        - `decay_factor = exp(-age_days / 180)` (half-life ~125 days, full-decay ~2 years).
        - `graph_distance_factor`: 1.0 for 1-hop, 0.5 for 2-hop, 0.25 for 3-hop, 0 for beyond. Sybil-resistant.
        - Normalized score: `raw / Σ(theoretical_max_weight)` × 100, clamped [-100, 100].
      - Minimum threshold to display: a target with <5 distinct voters within the viewer's 2-hop social graph is shown as "Insufficient reputation data" — protects against zero-data manipulation.
    - Rationale: time decay + social-graph weighting + per-voter replaceability discourages buying old accounts to vouch en masse (Augur lesson). Frontend-side aggregation means no centralized reputation registry. Voters' decisions are public per-event but private in aggregate (Nostr's identity model).
    - Reference Implementation: `crates/hunch-protocol::reputation` (Phase 2 module).
    - Backwards Compatibility: rules may tune (decay constant, graph distance falloff). All tuning published as HIP-5a/b/c addenda. Underlying event format is stable.
    - Security Considerations: Sybil farms (mitigated by social-graph distance factor); coordinated brigades (mitigated by graph + 5-voter minimum + community report flags via kind:30890 dispute); reputation poisoning (target reports the brigader; brigader's votes self-discount over time).
    - Test Vectors: 2-3 example reputation events with full JSON + sample aggregation calculation showing decay + graph weighting.
    - References: kind:30891 (HIP-1), kind:30890 dispute (HIP-1). Augur v2 dispute history (CoinDesk Jul 2020). Robosats reputation model. Mostro reputation model. Polymarket UMA $7M attack as cautionary tale of token-weighted reputation gone wrong.

    Must contain literal text `kind:30891` (verification key-link).

    ---

    **`docs/HIPs-reviews.md` — External reviewer attestation tracker**

    Structure:
    ```markdown
    # HIPs External Reviews

    Each HIP requires at least one external Bitcoin/Cashu/DLC contributor review before transitioning from `Status: Draft` to `Status: Final` (per CONTEXT.md decision D-06 and Phase 1 success criterion #1).

    Reviews must be public on Nostr (kind:1 reply to the HIP's kind:30023 long-form event, OR kind:30023 long-form review) and recorded here with the event ID + reviewer npub + summary.

    ## HIP-0: Protocol Overview + Manifesto

    Status: Awaiting review (drafted 2026-05-28)
    Target reviewer pool: [list candidates from interfaces above]
    Outreach attempts: (filled in Task 6 after publish)

    | Reviewer npub | Nostr event ID | Date | Summary | Verdict |
    |---------------|----------------|------|---------|---------|
    | (none yet)    |                |      |         |         |

    ## HIP-1: Nostr Event Kinds
    [same structure]

    ## HIP-2: DLC Contract Structure
    [same structure]

    ## HIP-3: Cashu NUT-CTF Integration
    [same structure]

    ## HIP-4: Multi-Oracle FROST Protocol
    [same structure]

    ## HIP-5: Reputation Event Format
    [same structure]
    ```

    Populate the "Target reviewer pool" entries from the `<interfaces>` block above (HIP-0..5 reviewer targets).

    Commit message: `docs(01-foundation): draft HIP-0..5 + MANIFESTO + HIPs-reviews tracker (PROTO-01..06; HIP-3 per D-01 NUT-CTF/PR #337)`.
  </action>
  <verify>
    <automated>
      bash -c "test -f docs/HIP-0.md && test -f docs/HIP-1.md && test -f docs/HIP-2.md && test -f docs/HIP-3.md && test -f docs/HIP-4.md && test -f docs/HIP-5.md && test -f docs/MANIFESTO.md && test -f docs/HIPs-reviews.md && grep -q 'Cypherpunk Manifesto' docs/HIP-0.md && grep -q '30888' docs/HIP-1.md && grep -q 'INVALID' docs/HIP-2.md && grep -q 'PR #337' docs/HIP-3.md && ! grep -q 'PR #128.*live\\|PR #128.*chosen' docs/HIP-3.md && grep -q 'RFC 9591' docs/HIP-4.md && grep -q 'Trail of Bits' docs/HIP-4.md && grep -q 'kind:30891' docs/HIP-5.md && grep -q 'Trust the math' docs/MANIFESTO.md && wc -w docs/MANIFESTO.md | awk '{exit !($1 >= 400 && $1 <= 1200)}' && bash scripts/verify-hips.sh"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 HIPs exist with the 11 required sections (Abstract, Motivation, Specification, Rationale, Reference Implementation, Backwards Compatibility, Security Considerations, Test Vectors, References + frontmatter + H1 title).
    - HIP-3 references PR #337 in its References section AND in its Specification; PR #128 only appears in Motivation/Background as CLOSED historical context (verified by grep).
    - HIP-4 references RFC 9591, frost-secp256k1-tr v2.2+, AND Trail of Bits Feb 2024 disclosure.
    - HIP-1 reserves kinds 30888, 38888, 30890, 30891, AND cites NIP-88 PR #1681 for kinds 88/89.
    - HIP-2 contains both `YES`, `NO`, and `INVALID` outcomes + refund timeout language.
    - HIP-5 references kind:30891 + time-decay + social-graph weighting.
    - MANIFESTO.md is 400-1200 words AND contains `Trust the math`.
    - HIPs-reviews.md exists with 6 H2 sections (one per HIP) and a target reviewer pool per HIP.
    - `bash scripts/verify-hips.sh` exits 0 (markdown lint + section structure check).
  </acceptance_criteria>
  <done>All 6 HIPs drafted, Manifesto written, reviews tracker initialized. Each HIP follows the HIP template structure. HIP-3 honors D-01 (Path A NUT-CTF, PR #337). HIP-4 honors the Trail of Bits fix discipline. HIP-1 reserves Hunch kinds + tracks NIP-88 PR #1681.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify PROTO-08 (CLAUDE.md exists + references GSD) + open NIPs-repo PR reserving Hunch kind ranges (HIP-1 backward-compat commitment) + audit firm pre-outreach inquiries</name>
  <files>
    docs/HIPs-reviews.md,
    docs/audit-shortlist-outreach.md
  </files>
  <read_first>
    - CLAUDE.md (verify content references GSD workflow per PROTO-08)
    - docs/HIP-1.md (after Task 3 — find the §Backwards Compatibility TODO that promises a NIPs-repo PR)
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §10 Q10 (Audit firm shortlisting timing — Trail of Bits Bitcoin team, Block Digital Contracting, Cure53, Quarkslab, NCC Group, Galaxy Audit; 8-14 week lead times warrant Phase 1 Week 4-5 outreach)
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §2.2 (NIP-88 status + kind-collision check)
  </read_first>
  <action>
    PART A — PROTO-08 verification:

    Already-existing artifact check. Run:
    ```bash
    test -f CLAUDE.md && grep -q "GSD" CLAUDE.md && grep -q "/gsd-plan-phase" CLAUDE.md
    ```
    Document the verification result by appending to `docs/HIPs-reviews.md` a new H2 section `## PROTO-08 Verification (CLAUDE.md)` with the result + git commit hash where CLAUDE.md was created. No code changes needed.

    PART B — NIPs-repo kind reservation PR:

    Open a real GitHub PR at https://github.com/nostr-protocol/nips/ titled `Reserve event kind ranges 308xx-3089x and 388xx for Hunch protocol (HIP-1)`. PR description:

    ```
    Hunch is a permissionless Bitcoin-native prediction market protocol (HIPs published at https://github.com/Silexperience210/hunch/docs/HIP-1.md, also on Nostr at <naddr1...>).

    Reserving the following addressable + parameterized-replaceable event kinds for Hunch (range 30000-39999 per NIP-01):

    - 30888 — Market announce
    - 38888 — Order
    - 30890 — Dispute
    - 30891 — Reputation
    - 30892 — Mint announce (reserved for Phase 2)

    Hunch also depends on (tracks upstream) kinds 88 and 89 from NIP-88 PR #1681 (oracle announce + attestation).

    HIP-1 spec: <link>
    HIP-1 Nostr long-form: <naddr1...>
    Maintainer: Silex <npub-TBD> (pseudonym)

    Hunch commits to:
    1. Track NIP-88 PR #1681; publish HIP-1a corrigendum if PR #1681 merges with material changes.
    2. Use only the reserved kinds for Hunch events (no overflow into other ranges).
    3. Update this PR with naddr1 after HIP-1 Status: Final transition.

    No conflict with current NIP master kinds verified 2026-05-28 against `nostrbook.dev/kinds/` and `github.com/nostr-protocol/nips` master.
    ```

    Record the PR URL in `docs/HIPs-reviews.md` under HIP-1 section as `Kind-reservation PR: <url>`.

    **If `gh` CLI is unavailable or authentication fails:** this becomes a `checkpoint:human-action` because PR creation requires GitHub auth that Claude cannot perform. Note in HIPs-reviews.md: "PR creation deferred — user must authenticate gh CLI and run `gh pr create -R nostr-protocol/nips --title '...' --body-file <draft>`." Create the body draft as `docs/nips-pr-kind-reservation.md`.

    PART C — Audit firm pre-outreach (per RESEARCH §10 Q10 — Phase 3 audit slots have 8-14 week lead times; sending initial inquiries in Phase 1 Week 4-5 locks Q4 2026 audit slot):

    Create `docs/audit-shortlist-outreach.md` documenting:

    ```markdown
    # Audit Firm Shortlist & Outreach (Phase 1 → Phase 3)

    **Purpose:** Phase 3 (SEC-01 Mainnet Launch & Hardening) requires external security audit by a firm with Bitcoin DLC + Cashu expertise. Audit firms typically have 8-14 week lead times. This document tracks Phase-1-week-4-5 initial inquiry outreach to secure a Q4 2026 audit slot.

    ## Shortlist (priority order per RESEARCH.md §6.2 + PITFALLS.md Pitfall 5)

    1. **Trail of Bits Bitcoin team** — best track record on threshold sigs (FROST DKG disclosure Feb 2024). Contact: audits@trailofbits.com. Outreach status: PENDING.
    2. **Block Digital Contracting** — Bitcoin-specific. Contact: TBD. Outreach status: PENDING.
    3. **Cure53** — frontend + crypto integration. Contact: pretty@cure53.de. Outreach status: PENDING.
    4. **Quarkslab** — low-level crypto. Contact: contact@quarkslab.com. Outreach status: PENDING.
    5. **NCC Group** — protocol audits, LN history. Contact: TBD. Outreach status: PENDING.
    6. **Galaxy Audit / Inference Security** — Bitcoin DLC-specific. Contact: TBD. Outreach status: PENDING.

    ## Outreach Template

    ```text
    Subject: Audit inquiry — Hunch (Bitcoin prediction market protocol, ~Q4 2026 timeline)

    Hi <firm>,

    I'm the protocol maintainer for Hunch (https://github.com/Silexperience210/hunch),
    a permissionless Bitcoin-native prediction market protocol combining DLC + Cashu NUT-CTF
    (PR #337) + FROST k-of-n (frost-secp256k1-tr v2.2+, RFC 9591) + Lightning + Nostr.

    We are scoping our Phase 3 security audit (per ROADMAP at https://github.com/.../ROADMAP.md)
    and would like to discuss your firm's availability for a comprehensive audit in Q4 2026.

    Scope candidates:
    - Cashu mint code (NUT-CTF integration, NUT-12 DLEQ, blind-sig flow)
    - DLC contract construction (CET enumeration, refund timeout, adapter sigs)
    - FROST DKG + signing (Pedersen DKG with TOB Feb 2024 fix verified)
    - LDK Node integration (post-v0.1.1 fixes)
    - Oracle Schnorr signing (BIP-340 + NIP-88)

    Budget range: $75-180K single-firm, $120-250K dual-firm.

    Would your team be open to a scoping call? Please reply via Nostr DM (npub-TBD)
    or via this address (PGP key available).

    Maintainer is pseudonymous; legal entity contracting is via foundation counsel
    (LEGAL-02 deliverable). Communications can be Nostr-native or PGP-encrypted email.

    Thanks,
    Silex (Hunch protocol maintainer)
    ```

    ## Outreach Log

    | Firm | Date sent | Channel | Response | Slot offered |
    |------|-----------|---------|----------|--------------|
    | (none yet) |        |         |          |              |
    ```

    Send initial inquiry emails to all 6 firms using the outreach template (substituting <firm> in salutation; npub will be filled from Task 5 publish-hip output). Log each in the table.

    **If email-sending is not autonomously feasible** (no SMTP credentials, no mail CLI configured), this becomes a `checkpoint:human-action`: user must send the emails manually from the outreach template. Record in the outreach log that emails are queued for manual send.

    Commit message: `docs(01-foundation): PROTO-08 verification + NIPs-repo kind reservation PR + Phase 3 audit firm outreach queue (RESEARCH §10 Q10)`.
  </action>
  <verify>
    <automated>
      bash -c "test -f docs/audit-shortlist-outreach.md && grep -q 'Trail of Bits' docs/audit-shortlist-outreach.md && grep -q 'Block Digital Contracting' docs/audit-shortlist-outreach.md && grep -q 'PROTO-08 Verification' docs/HIPs-reviews.md && test -f CLAUDE.md && grep -q 'GSD' CLAUDE.md && (grep -q 'Kind-reservation PR' docs/HIPs-reviews.md || test -f docs/nips-pr-kind-reservation.md)"
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists and references GSD workflow (PROTO-08 closed; recorded in HIPs-reviews.md).
    - Either a real PR opened at github.com/nostr-protocol/nips (URL recorded in HIPs-reviews.md) OR a draft PR body file `docs/nips-pr-kind-reservation.md` exists for human-action checkpoint.
    - `docs/audit-shortlist-outreach.md` exists with all 6 firms shortlisted, an outreach template, and an outreach log table.
    - Outreach emails sent (autonomous) OR queued for human-action.
  </acceptance_criteria>
  <done>PROTO-08 verified. HIP-1 backward-compat commitment (NIPs-repo PR) satisfied or queued. Phase 3 audit firm outreach is in motion within the 8-14 week lead-time window.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    HIPs and supporting infrastructure are drafted (Task 3), the repo is scaffolded (Task 2), and the corrigendum/pseudonymity hygiene (Task 1) + PROTO-08/audit outreach (Task 4) are committed. Before publishing the 6 HIPs to Nostr long-form (Task 6), recruiting external reviewers (Task 6), and configuring GitHub + Radicle mirrors (Task 6), the user must verify that the HIP content reflects their intent and that the pseudonymity discipline is intact.
  </what-built>
  <how-to-verify>
    1. **Pseudonymity hygiene** — Run from repo root:
       ```bash
       git log --format="%an <%ae> | %s" | head -10
       grep -ri "alexandre\\|cangini\\|gmail" --include="*.md" --include="*.toml" --include="*.rs" --include="*.ts" --include="*.json" . 2>/dev/null | grep -v "^./.git/\\|^./node_modules/\\|^./target/"
       ```
       Confirm: zero real-name leakage in any tracked file. All commits authored under "Silex <silex@hunch.markets>" or equivalent pseudonym.

    2. **HIPs content review** — Read all 6 HIPs in order. Confirm:
       - HIP-0 captures the cypherpunk identity in YOUR voice (or close enough; tone is a Claude's-discretion item per CONTEXT.md). If the voice is wrong, mark `REVISE`.
       - HIP-3 references PR #337 as the chosen path. PR #128 appears only as CLOSED historical context, never as live spec.
       - HIP-4 cites RFC 9591, BIP-445, frost-secp256k1-tr v2.2+, AND the Trail of Bits Feb 2024 disclosure with the coefficient-vector-length fix.
       - HIP-1 reserves kinds 30888 / 38888 / 30890 / 30891 / 30892 and tracks NIP-88 PR #1681 for 88/89.
       - HIP-2 includes YES / NO / INVALID outcomes AND a refund-timeout fallback.
       - HIP-5 includes time-decayed weighting AND social-graph distance factor (Sybil resistance).
       - MANIFESTO.md reads as a manifesto (~500-1000 words), not a spec.

    3. **Workspace compile sanity** — `cargo build --workspace` exits 0; `cargo test --workspace` exits 0; `bash scripts/verify-repo.sh` mostly passes (Radicle remote check expected to fail — Task 6 deliverable).

    4. **NIPs-repo PR + audit outreach** — Either the gh PR URL is in `docs/HIPs-reviews.md`, or the draft body is at `docs/nips-pr-kind-reservation.md` for you to submit manually. Same for audit firm outreach: either emails went out, or the outreach log shows them queued for your manual send.

    5. **Decide:** Type `approved` to proceed to Task 6 (Nostr key generation + HIP publication + reviewer outreach + Radicle mirror + GitHub publication). Or describe required revisions.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to revise.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Generate project Nostr pseudonym keypair, publish 6 HIPs as NIP-23 long-form (kind 30023), configure Radicle + GitHub remotes, recruit external HIP reviewers, finalize HIPs-reviews.md</name>
  <files>
    docs/HIP-0.md,
    docs/HIP-1.md,
    docs/HIP-2.md,
    docs/HIP-3.md,
    docs/HIP-4.md,
    docs/HIP-5.md,
    docs/HIPs-reviews.md,
    .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md
  </files>
  <read_first>
    - .planning/phases/01-cypherpunk-foundation/01-RESEARCH.md §2.3 (NIP-23 long-form publication mechanics) and §10 Q8 (Radicle setup timing)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (D-05 full pseudonymity, D-06 two-channel publication)
    - scripts/publish-hip.ts (after Task 2)
    - docs/HIPs-reviews.md (after Tasks 3+4 — pending reviewer outreach status)
    - All 6 HIP files (need npub-TBD placeholder replaced with real npub)
  </read_first>
  <action>
    PART A — Generate project Nostr pseudonym keypair (per D-05):

    Use `bun` to generate via the nostr-tools or @nostr-dev-kit/ndk library. Script:
    ```ts
    import { generateSecretKey, getPublicKey, nip19 } from "nostr-tools";
    const sk = generateSecretKey();
    const pk = getPublicKey(sk);
    const nsec = nip19.nsecEncode(sk);
    const npub = nip19.npubEncode(pk);
    console.log(JSON.stringify({ npub, nsec }, null, 2));
    ```
    Run once. Capture output. **STORE THE NSEC IN A KEEPASS/HARDWARE-BACKED PASSWORD MANAGER — do NOT commit it.** Add `HUNCH_NSEC` to `.env.local` (already in .gitignore from Task 1).

    Update every HIP's `Author(s)` frontmatter from `Silex <npub-TBD>` to `Silex <npub1...>` using the real npub. Same for MANIFESTO.md sign-off.

    PART B — Publish 6 HIPs as Nostr kind 30023 long-form (per D-06):

    Run `HUNCH_NSEC=<nsec> bun scripts/publish-hip.ts 0` … `bun scripts/publish-hip.ts 5`. For each HIP:
    - Verify Nostr publish to all 4 relays returned positive ack
    - Capture the naddr1 NIP-19 identifier
    - Append footer to source `docs/HIP-${N}.md`: `*Published on Nostr as <naddr1...>*` (the publish-hip.ts script does this automatically per Task 2 spec; verify it happened).

    PART C — Configure GitHub + Radicle remotes (PROTO-07):

    1. Create the GitHub repo `Silexperience210/hunch` (if not exists). Use `gh repo create Silexperience210/hunch --public --description "Hunch — permissionless cypherpunk prediction market protocol on Bitcoin"`. Set `git remote add origin git@github.com:Silexperience210/hunch.git` if not already set. Push current main branch.

    2. Set up Radicle mirror per RESEARCH §10 Q8 (operationally non-trivial; allocate 1-2 days):
       - Install `rad` CLI via official curl install: `curl -sSf https://radicle.xyz/install.sh | sh`
       - Initialize: `rad init --name hunch --description "Hunch protocol" --default-branch main --public`
       - Push: `git push rad main` (the `rad` remote is auto-added by `rad init`)
       - Record the Radicle ID (rad:z...) in README.md under a new `## Mirrors` section: `- Radicle: rad:z...`

    3. Add Codeberg mirror (defense-in-depth per PITFALLS Pitfall 13): create Codeberg account if missing, create repo `Silex/hunch`, add remote `git remote add codeberg git@codeberg.org:Silex/hunch.git`, push. Record in README.md `## Mirrors` section.

    Run `bash scripts/verify-repo.sh` — should now fully pass including the Radicle remote check.

    **If GitHub auth, Radicle install, or Codeberg signup blocks autonomous execution:** this Task becomes a partial `checkpoint:human-action` with detailed instructions; record state in HIPs-reviews.md.

    PART D — Recruit external reviewers (per Phase 1 Success Criterion #1):

    For each HIP, send outreach DMs to 2-3 reviewers from the target list (interfaces above). Use NDK / nostr-tools to send NIP-44 gift-wrapped DMs from the project nsec, OR send via GitHub @-mentions on a tracking issue, OR send via email if the reviewer is reachable that way.

    Outreach DM template (per HIP):
    ```text
    Hi {reviewer},

    Hunch is a new Bitcoin prediction market protocol (DLC + Cashu NUT-CTF + FROST + Nostr) — fully cypherpunk, no token, MIT, multi-mint/multi-oracle.

    We just published HIP-{N}: {title} on Nostr long-form ({naddr1...}) and at https://github.com/Silexperience210/hunch/blob/main/docs/HIP-{N}.md.

    Would you have time to review and reply (kind:1 reply or kind:30023 review) within the next 2-4 weeks? Even a "looks reasonable" or "here are 3 issues" is enormously valuable — we're trying to ratify these specs before any mainnet code commits.

    Specifically interested in your perspective on: {1-2 reviewer-specific topics, e.g., for fiatjaf on HIP-1 — "kind range reservation strategy + NIP registry collision check" }.

    No compensation expected; happy to credit you in HIP {N} References + foundation acknowledgments.

    Thanks,
    Silex <{npub}>
    ```

    Log every outreach attempt in `docs/HIPs-reviews.md` under each HIP's section with timestamp, channel (Nostr DM / GitHub / email), and reviewer npub/handle.

    Update `.planning/phases/01-cypherpunk-foundation/01-RESEARCH.md` Open Questions §10 Q1 / Q3: mark Q1 (NUT-DLC path) as RESOLVED per D-01 (Path A NUT-CTF), Q3 (pseudonymity scope) as RESOLVED per D-05 (full pseudonymity).

    Commit message: `feat(01-foundation): publish HIPs to Nostr long-form (NIP-23) + GitHub/Radicle/Codeberg mirrors + reviewer outreach (PROTO-07)`.

    **Wait window:** External reviewer responses arrive over 2-4 weeks. This task closes on outreach completion; the formal "≥1 review per HIP" success criterion is gated in the phase-level checkpoint at end of Task 7.
  </action>
  <verify>
    <automated>
      bash -c "for n in 0 1 2 3 4 5; do grep -q 'Published on Nostr as naddr1' docs/HIP-$n.md || { echo 'HIP-'$n' not published'; exit 1; }; grep -q 'npub1' docs/HIP-$n.md || { echo 'HIP-'$n' has unresolved npub'; exit 1; }; done && grep -q '## Mirrors' README.md && git remote -v | grep -q 'origin.*github.com.*hunch' && git remote -v | grep -q 'rad' && grep -q 'rad:z\\|radicle.xyz' README.md && for n in 0 1 2 3 4 5; do grep -q 'naddr1' docs/HIPs-reviews.md || { echo 'reviews tracker missing naddr1 for HIP-'$n; exit 1; }; done && bash scripts/verify-repo.sh"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 HIPs contain a `Published on Nostr as naddr1...` footer + resolved `npub1...` author identifier.
    - `docs/HIPs-reviews.md` records the naddr1 for each HIP + outreach attempts logged with reviewer npub/handle + timestamp + channel.
    - `git remote -v` lists `origin` (GitHub), `rad` (Radicle), and optionally `codeberg`.
    - README.md has a `## Mirrors` section listing GitHub, Radicle (rad:z...), and Codeberg URLs.
    - `bash scripts/verify-repo.sh` fully passes (LICENSE MIT + CONTRIBUTING + CoC + Radicle remote + HIPs lint).
    - .env.local contains HUNCH_NSEC (gitignored — verify via `git check-ignore .env.local`).
  </acceptance_criteria>
  <done>HIPs are published to Nostr (D-06 two-channel honored). GitHub, Radicle, and Codeberg mirrors live. Reviewer outreach is sent. Project pseudonym npub is in every HIP and in MANIFESTO. Phase 1 success criterion #1 outreach portion complete; formal review gating is at Task 7 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 Plan 01 deliverables — corrigendum, workspace scaffold, 6 HIPs + Manifesto, Nostr publication, GitHub/Radicle/Codeberg mirrors, audit firm pre-outreach, NIPs-repo kind reservation PR, reviewer outreach — all complete. The remaining gate is Phase 1 Success Criterion #1: at least one external Bitcoin/Cashu/DLC contributor review per HIP, documented in docs/HIPs-reviews.md, observable as kind:1 replies or kind:30023 review events on Nostr.

    This checkpoint may need to wait 2-4 weeks elapsed time for reviewer responses to arrive. The user re-runs this checkpoint when responses have landed.
  </what-built>
  <how-to-verify>
    1. **Fetch reviews from Nostr.** Open the project's preferred Nostr client (Damus / Amethyst / Iris / coracle) and check replies (kind:1) and quoted long-form reviews (kind:30023 with `e` tag to HIP naddr) for each of the 6 HIPs.

    2. **Read each review.** For each HIP, expect at least one substantive reply from a reviewer in the target pool (HIP-3 needs Cashu maintainer signal: joemphilips / Calle / conduition / Gandlaf — this is the highest priority because of the PR #337 pivot dependency).

    3. **Log into docs/HIPs-reviews.md.** For each review received, fill in the reviewer table:
       ```
       | Reviewer npub | Nostr event ID | Date | Summary | Verdict |
       |---------------|----------------|------|---------|---------|
       | npub1...      | event_id_hex   | 2026-06-XX | "Reviewer noted X, Y, Z" | OK / Revise / Reject |
       ```

    4. **If any HIP has zero external reviews after 4 weeks:** escalate. Options:
       - Re-do outreach with different / broader candidate list
       - Post the HIP on /r/Bitcoin, /r/CashuBTC, Cashu / FROST Telegram channels (mind pseudonymity)
       - Reach out via existing personal Nostr network (mindful of identity-link risk)
       - Defer Phase 1 sign-off and continue with other phases that don't depend on Plan 01 closure

    5. **If a reviewer flags a "Revise" verdict:** decide whether to (a) revise the HIP and re-publish (kind 30023 is replaceable, so a new event with same d-tag updates the published version), (b) note the open question and proceed to Status: Final regardless, or (c) hold Status: Draft until the issue is resolved.

    6. **When all 6 HIPs have ≥1 review verdict logged:** transition each HIP's `Status: Draft` → `Status: Final` in the frontmatter, re-run `bun scripts/publish-hip.ts N` for each updated HIP (kind 30023 replaceable — same naddr, updated content), and commit `docs(01-foundation): HIP-0..5 transition to Status: Final after external review (Phase 1 SC #1)`.

    7. **Decide:** Type `approved` when all 6 HIPs have ≥1 external review logged. Or describe the gap.
  </how-to-verify>
  <resume-signal>Type "approved" when ≥1 external review per HIP is logged in docs/HIPs-reviews.md, or describe revisions needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → git remote (GitHub/Radicle/Codeberg) | Pseudonymous commit metadata crosses here; real-name leakage would deanonymize maintainer |
| local nsec → Nostr relays | Project pseudonym private key signs HIP publications; nsec compromise = HIP forgery + pseudonym takeover |
| HIP-3 spec → upstream Cashu PR #337 | Hunch's spec depends on an upstream open PR; if PR #337 stalls or changes architecture, HIP-3 must publish corrigendum |
| HIP author → external reviewer | Reviewers may attempt to deanonymize maintainer through tone-fingerprinting or stylistic analysis |
| .planning/research/* corrigendum → downstream agent prompts | Stale research content (pre-corrigendum) could mislead Plan 02 / 03 if read first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | git commit metadata (real-name leakage) | mitigate | Per-repo `git config user.name/user.email` to pseudonym before first push; `.gitignore` protects Silex_0xF777C5B8_SECRET.asc; CI fails if commit author email matches real-name pattern (future hardening) |
| T-01-02 | Spoofing | HIP authorship (impersonation of reviewer) | mitigate | HIPs-reviews.md records reviewer npub; verify Nostr event signature before treating as a review |
| T-01-03 | Tampering | HIP content drift between repo and Nostr long-form | mitigate | scripts/publish-hip.ts publishes the exact `docs/HIP-N.md` content; kind 30023 d-tag = `hip-N` ensures replaceability; verification script compares repo content vs latest Nostr event content as a future hardening |
| T-01-04 | Repudiation | reviewer denies their review later | accept | reviews are public Nostr events; signature attaches; no repudiation possible cryptographically |
| T-01-05 | Information Disclosure | HUNCH_NSEC committed accidentally | mitigate | .env.local in .gitignore (Task 1); HUNCH_NSEC stored in password manager; CI scans for nsec1 / private-key patterns in PRs |
| T-01-06 | Information Disclosure | Maintainer deanonymization via stylometric analysis of HIPs + Manifesto | accept | RESEARCH §6.6 acknowledges stylometric risk; mitigated only by writing in a deliberately neutral spec voice (HIP-1..5) + manifesto voice that draws from established cypherpunk register (not personal phrasing) |
| T-01-07 | DoS | Nostr relay refuses to accept HIP publication | mitigate | publish to 4+ relays (relay.damus.io, nos.lol, relay.nostr.band, relay.hunch.markets); any single relay failure non-fatal |
| T-01-08 | Elevation of Privilege | a contributor with commit access pushes real-name in commit metadata | mitigate | CONTRIBUTING.md mandates per-repo `git config` for pseudonymous authorship; CI can be hardened to reject commits with email patterns matching common providers (Phase 2 hardening) |
| T-01-09 | Tampering | NIPs-repo kind reservation PR rejected, kind collision later occurs | accept (low risk) | RESEARCH §2.2 verified 2026-05-28 no current collision; HIP-1 Backwards Compatibility commits to corrigendum if collision emerges |
| T-01-10 | Information Disclosure | Audit firm outreach reveals project before HIPs are ratified | mitigate | outreach uses pseudonym; pseudonym Nostr DM or PGP-encrypted email channels; no real-name in outreach copy |
</threat_model>

<verification>
- All 8 requirements (PROTO-01 through PROTO-08) closed per their falsification conditions in RESEARCH.md §7.2.
- `bash scripts/verify-repo.sh && cargo build --workspace && cargo test --workspace && bash scripts/verify-hips.sh` exits 0.
- `git log --format="%an <%ae>"` shows ONLY pseudonym across all Plan 01 commits.
- Every HIP has at least one external reviewer entry in `docs/HIPs-reviews.md`, with the reviewer's npub + Nostr event ID + verdict.
- The .planning/research/* corrigendum is visible at the top of STACK.md, ARCHITECTURE.md, FEATURES.md, SUMMARY.md.
- HIP-3 references PR #337 in Specification + References; PR #128 appears only as CLOSED historical context.
- The 6 HIPs are published as kind:30023 Nostr long-form events under the project pseudonym key; naddr1 identifiers in each HIP footer.
</verification>

<success_criteria>
- 8/8 Phase 1 requirements closed by this plan: PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05, PROTO-06, PROTO-07, PROTO-08.
- Phase 1 ROADMAP Success Criterion #1 partially closed: "HIP-0..5 published in repo and accessible on Nostr long-form (NIP-23) — content fully reviewed by at least one external Bitcoin protocol contributor". Phase 1 SC #1 outreach is done; formal "≥1 review per HIP" gate is at Task 7 checkpoint.
- The pseudonymity discipline (D-05) is upheld across every committed file.
- The NUT-CTF / PR #337 pivot (D-01) is reflected in HIP-3 and in the corrigenda on 4 research docs (D-02).
- Cargo workspace builds with 6 crates (D-08) — Plan 02 (Technical Spikes) can now build `crates/hunch-mint-spike` and `crates/hunch-oracle-spike` alongside.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cypherpunk-foundation/01-01-SUMMARY.md` documenting:
- Each HIP's naddr1 identifier + state of reviewer responses
- Git remote configuration (GitHub URL, Radicle ID, Codeberg URL)
- Workspace crate inventory + build status
- NUT-CTF / PR #337 pivot artifact trail (corrigendum locations + HIP-3 specifics)
- Audit firm outreach status + response log
- Open follow-up items (e.g., HIPs in Draft awaiting reviews → Status: Final transition)
</output>
