---
phase: 02-mainnet-spine
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  # Anti-spam wiring
  - apps/hunch-web/src/lib/curation/policy.ts
  - apps/hunch-web/src/lib/curation/policy.test.ts
  - apps/hunch-web/src/lib/curation/community-reports.ts
  - apps/hunch-web/src/lib/curation/new-creator-visibility.ts
  - apps/hunch-web/src/lib/curation/trending.ts
  - apps/hunch-web/src/hooks/use-markets.ts
  - apps/hunch-web/src/components/curation/CurationBadge.tsx
  - apps/hunch-web/src/components/curation/ReportMarketButton.tsx
  - apps/hunch-web/src/app/legal/curation/page.tsx
  - apps/hunch-web/docs/CURATION_POLICY.md
  # Mint side: INVALID outcome wiring already in Plan 02-01 Task 3; this plan adds verification + community report ingestion
  - crates/hunch-mint/src/curation/mod.rs
  - crates/hunch-mint/src/curation/reports.rs
  - crates/hunch-mint/tests/curation.rs
  # Distribution: hunch.io (Cloudflare Pages)
  - .github/workflows/deploy-hunch-io.yml
  - apps/hunch-web/wrangler.toml
  - apps/hunch-web/functions/_middleware.ts
  - deploy/cloudflare/pages-config.md
  # Distribution: hunch.onion (Tor hidden service)
  - deploy/tor/torrc.example
  - deploy/tor/onion-deploy.sh
  - deploy/tor/README.md
  # Distribution: IPFS pin (Pinata + web3.storage)
  - .github/workflows/ipfs-pin.yml
  - scripts/ipfs-pin.ts
  - scripts/verify-mirror-hashes.ts
  - deploy/ipfs/README.md
  # Distribution: source mirrors (GitHub + Radicle + Codeberg)
  - .github/workflows/sync-mirrors.yml
  - scripts/verify-mirrors.sh
  - docs/MIRRORS.md
  # Status page
  - status/index.html
  - status/check.ts
  - status/uptime.json
  - .github/workflows/status-check.yml
  - deploy/status/README.md
  # Monitoring (Prometheus + Grafana on top of Plan 02-01 prometheus.yml)
  - deploy/grafana/datasources.yml
  - deploy/grafana/dashboards/mint.json
  - deploy/grafana/dashboards/oracle.json
  - deploy/grafana/dashboards/relay.json
  - deploy/grafana/provisioning.yml
  - deploy/docker-compose.yml
  - deploy/prometheus.yml
  - deploy/operator-guide.md
autonomous: false
requirements: [CURATE-01, CURATE-02, CURATE-03, CURATE-04, CURATE-05, CURATE-06, CURATE-07, OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06]
requirements_addressed: [CURATE-01, CURATE-02, CURATE-03, CURATE-04, CURATE-05, CURATE-06, CURATE-07, OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06]
tags: [hunch, curation, anti-spam, distribution, cloudflare, tor, ipfs, radicle, status, prometheus, grafana, monitoring]

must_haves:
  truths:
    - "CURATE-01 social-graph filter is wired in frontend browse + verified by curation policy test (default ON)"
    - "CURATE-02 user mute lists (NIP-51 kind:10000) load from Nostr + filter markets — verified in policy.test.ts"
    - "CURATE-03 trending threshold algorithm: N=10 unique participants minimum before market reaches Trending tab"
    - "CURATE-04 INVALID outcome CET wired in mint (Plan 02-01 Task 3) — this plan adds anti_spam tests verifying frontend ResolutionPanel renders INVALID flow correctly"
    - "CURATE-05 frontend curation policy hides violence / harm / sanctioned-entity markets at frontend layer (protocol stays neutral) — policy.ts + docs/CURATION_POLICY.md"
    - "CURATE-06 community report event support (kind:30890 dispute event with 'invalid' flag) — reports.rs aggregates; UI surfaces signal"
    - "CURATE-07 new-creator gradual visibility — markets from creators with no prior reputation surface slowly (linear ramp over 7 days)"
    - "hunch.io deployed via Cloudflare Pages — automated on push to main via .github/workflows/deploy-hunch-io.yml"
    - "hunch.onion Tor hidden service live with torrc snippet + deploy script + README"
    - "IPFS pin: every release pinned via Pinata + web3.storage; verify-mirror-hashes.ts confirms all 3 mirrors serve identical SHA256 bundle"
    - "GitHub + Radicle + Codeberg source mirror sync verified weekly via .github/workflows/sync-mirrors.yml"
    - "status.hunch.markets public uptime page (static HTML + scheduled health checks via .github/workflows/status-check.yml)"
    - "Prometheus + Grafana scrapes mint + oracle + relay; 3 dashboards (mint reserves, oracle attestations, relay throughput) provisioned via docker-compose"
  artifacts:
    - path: "apps/hunch-web/src/lib/curation/policy.ts"
      provides: "Frontend curation policy (CURATE-05 — hides violence/harm/sanctioned markets)"
      contains: "shouldHide"
    - path: "apps/hunch-web/docs/CURATION_POLICY.md"
      provides: "Human-readable curation policy for transparency"
      min_lines: 40
    - path: "apps/hunch-web/src/lib/curation/community-reports.ts"
      provides: "kind:30890 community report aggregator (CURATE-06)"
      contains: "30890"
    - path: "apps/hunch-web/src/lib/curation/new-creator-visibility.ts"
      provides: "New-creator gradual visibility (CURATE-07) — 7-day linear ramp"
      contains: "rampVisibility"
    - path: "apps/hunch-web/src/lib/curation/trending.ts"
      provides: "Trending threshold (CURATE-03) — min N=10 unique participants"
      contains: "TRENDING_MIN_PARTICIPANTS"
    - path: "crates/hunch-mint/src/curation/reports.rs"
      provides: "Mint-side community report ingestion (kind:30890 → aggregate)"
      contains: "ingest_report"
    - path: ".github/workflows/deploy-hunch-io.yml"
      provides: "Cloudflare Pages deploy pipeline (OPS-01)"
      contains: "cloudflare/pages-action"
    - path: "deploy/tor/torrc.example"
      provides: "Tor hidden service config (OPS-02)"
      contains: "HiddenServiceDir"
    - path: "scripts/ipfs-pin.ts"
      provides: "Pinata + web3.storage pin automation (OPS-03)"
      contains: "pinata"
    - path: "scripts/verify-mirror-hashes.ts"
      provides: "Verify hunch.io + hunch.onion + IPFS pin serve identical SHA256 (phase success criterion #2)"
      contains: "sha256"
    - path: ".github/workflows/sync-mirrors.yml"
      provides: "GitHub + Radicle + Codeberg sync verification (OPS-04)"
      contains: "radicle"
    - path: "docs/MIRRORS.md"
      provides: "Source mirror documentation"
      min_lines: 30
    - path: "status/index.html"
      provides: "Public status page (OPS-05)"
      contains: "status"
    - path: ".github/workflows/status-check.yml"
      provides: "Scheduled uptime checks → updates status/uptime.json"
      contains: "schedule:"
    - path: "deploy/grafana/dashboards/mint.json"
      provides: "Grafana dashboard for mint metrics (OPS-06)"
      contains: "mint_reserves_sats"
    - path: "deploy/grafana/dashboards/oracle.json"
      provides: "Grafana dashboard for oracle metrics"
      contains: "oracle"
    - path: "deploy/grafana/dashboards/relay.json"
      provides: "Grafana dashboard for relay metrics"
      contains: "relay"
  key_links:
    - from: "apps/hunch-web/src/hooks/use-markets.ts"
      to: "apps/hunch-web/src/lib/curation/policy.ts"
      via: "Pipeline: social-graph → mute → curation policy → new-creator-visibility → trending"
      pattern: "curation"
    - from: "apps/hunch-web/src/lib/curation/community-reports.ts"
      to: "kind:30890 dispute event (Nostr)"
      via: "NDK filter for reports on a given market id"
      pattern: "30890"
    - from: "scripts/verify-mirror-hashes.ts"
      to: "hunch.io / hunch.onion / IPFS pin"
      via: "Fetches index.html from all 3 mirrors + asserts SHA256 equality"
      pattern: "sha256"
    - from: "deploy/grafana/dashboards/mint.json"
      to: "crates/hunch-mint/src/metrics.rs"
      via: "Prometheus scrape /metrics → Grafana panels query mint_reserves_sats, dlc_contracts_open"
      pattern: "mint_reserves_sats"
    - from: "deploy/docker-compose.yml"
      to: "deploy/grafana/provisioning.yml"
      via: "Grafana service mounts provisioning + dashboards as bind volumes"
      pattern: "grafana"

# Rationale for files_modified: 13 reqs collapsed into 4 tasks. Anti-spam +
# distribution + monitoring touch ~35 files. Adds to (not replaces) Plan 02-01
# docker-compose.yml + prometheus.yml + operator-guide.md (Grafana service +
# dashboards added). Plan 02-02 use-markets.ts hook edited to wire curation
# pipeline (one file overlap with 02-02 — documented; this plan strictly runs
# AFTER 02-02 in wave 3).
---

<objective>
Close out Phase 2 by:
1. **Anti-spam** (CURATE-01..07) — wire frontend curation pipeline (social-graph + mute lists + frontend curation policy + community reports + new-creator gradual visibility + trending threshold) on top of the default-ON filter from Plan 02-02; verify INVALID outcome CET (Plan 02-01) renders end-to-end.
2. **Distribution** (OPS-01..04) — deploy hunch.io (Cloudflare Pages), hunch.onion (Tor hidden service), IPFS pin (Pinata + web3.storage); verify all three serve identical SHA256 bundle (success criterion #2); GitHub + Radicle + Codeberg source mirrors with sync verification.
3. **Status + Monitoring** (OPS-05, OPS-06) — public status.hunch.markets uptime page + Prometheus + Grafana dashboards on mint + oracle + relay.

Purpose: Phase 2 close gate. Frontend exists in 3 jurisdictions/transports; protocol services have operability surfaces; anti-spam mechanisms verified end-to-end.

Output: 3 live frontend mirrors + 3 Grafana dashboards + public status page + verified anti-spam pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-mainnet-spine/02-CONTEXT.md
@.planning/phases/02-mainnet-spine/02-01-SUMMARY.md
@.planning/phases/02-mainnet-spine/02-02-SUMMARY.md
@docs/HIP-1.md
@docs/HIP-2.md
@docs/HIP-5.md

<interfaces>
From Plan 02-01 (mint + oracle + relay):
- crates/hunch-mint/src/metrics.rs exports Prometheus metrics:
  mint_tokens_issued_total, mint_reserves_sats, dlc_contracts_open,
  dlc_settlements_total{outcome=YES|NO|INVALID},
  ldk_channel_balance_sats, http_request_duration_seconds
- crates/hunch-mint/src/dlc/invalid_cet.rs (INVALID outcome wiring, CURATE-04)
- deploy/docker-compose.yml (mint + oracle + relay + prometheus stack — extend with Grafana)
- deploy/prometheus.yml (scrape config — extend with relay metrics + Grafana)
- deploy/operator-guide.md (extend with Grafana access + status page)

From Plan 02-02 (frontend):
- apps/hunch-web/src/hooks/use-markets.ts (current pipeline: socialGraph → mute → return)
  This plan extends: socialGraph → mute → curationPolicy → newCreatorVisibility → trending
- apps/hunch-web/src/lib/filter/social-graph.ts (CURATE-01 — already wired)
- apps/hunch-web/src/lib/filter/mute-list.ts (CURATE-02 — already wired)
- apps/hunch-web/src/components/market/ResolutionPanel.tsx (renders INVALID — CURATE-04 frontend)
- apps/hunch-web/Dockerfile (build → out/) feeds distribution pipelines

Nostr kinds added in this plan:
- 30890 KIND_DISPUTE used for community reports (CURATE-06) — frontend filter + mint aggregation

External services:
- Cloudflare Pages — Wrangler CLI (bunx wrangler) + Cloudflare account (user setup)
- Pinata — pinFileToIPFS API (user setup: PINATA_JWT secret)
- web3.storage — w3 CLI (user setup: W3_TOKEN)
- Radicle — rad CLI (user setup: rad init + push)
- Codeberg — git remote add (user setup: SSH key)
- Tor — torrc on target host (user setup: hidden service generation)
</interfaces>

<user_setup>
  - service: cloudflare-pages
    why: "OPS-01 deploy hunch.io clearnet"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard → My Profile → API Tokens → Create Token (Pages:Edit + Account:Read)"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard → right sidebar Account ID"
    dashboard_config:
      - task: "Create Pages project named 'hunch'"
        location: "Cloudflare Dashboard → Workers & Pages → Create application → Pages"
      - task: "Connect custom domain hunch.io (registered via Njalla per CONTEXT.md item 8)"
        location: "Pages project → Custom domains"
  - service: pinata
    why: "OPS-03 IPFS pin (primary)"
    env_vars:
      - name: PINATA_JWT
        source: "Pinata Dashboard → API Keys → New Key (pinFileToIPFS scope)"
  - service: web3.storage
    why: "OPS-03 IPFS pin (redundant secondary)"
    env_vars:
      - name: W3_TOKEN
        source: "console.web3.storage → API Tokens → Create"
  - service: tor-hidden-service
    why: "OPS-02 hunch.onion"
    dashboard_config:
      - task: "Provision a VPS for Tor hidden service (Njalla / Orangewebsite per pseudonymity lock)"
        location: "Operator VPS"
      - task: "Generate hidden service via tor + record .onion v3 address"
        location: "/var/lib/tor/hunch/ on VPS"
  - service: radicle
    why: "OPS-04 source mirror (Radicle, primary trust-minimized)"
    env_vars:
      - name: RAD_PASSPHRASE
        source: "rad self → key passphrase under user opsec"
  - service: codeberg
    why: "OPS-04 source mirror (Codeberg, secondary EU-hosted)"
    dashboard_config:
      - task: "Create Codeberg organization 'hunch-protocol' (pseudonymous)"
        location: "https://codeberg.org/repo/create"
</user_setup>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anti-spam pipeline (CURATE-01..07)</name>
  <files>apps/hunch-web/src/lib/curation/policy.ts, apps/hunch-web/src/lib/curation/policy.test.ts, apps/hunch-web/src/lib/curation/community-reports.ts, apps/hunch-web/src/lib/curation/new-creator-visibility.ts, apps/hunch-web/src/lib/curation/trending.ts, apps/hunch-web/src/hooks/use-markets.ts, apps/hunch-web/src/components/curation/CurationBadge.tsx, apps/hunch-web/src/components/curation/ReportMarketButton.tsx, apps/hunch-web/src/app/legal/curation/page.tsx, apps/hunch-web/docs/CURATION_POLICY.md, crates/hunch-mint/src/curation/mod.rs, crates/hunch-mint/src/curation/reports.rs, crates/hunch-mint/tests/curation.rs</files>
  <read_first>
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (Anti-Spam Pattern decision block)
    - docs/HIP-1.md (kind:30890 dispute event tag schema)
    - docs/HIP-5.md (reputation event aggregation for new-creator gradual visibility)
    - docs/legal/TERMS.md (Phase 1 strawman §5 — frontend curation policy basis)
    - apps/hunch-web/src/lib/filter/social-graph.ts (Plan 02-02 — existing filter)
    - apps/hunch-web/src/lib/filter/mute-list.ts (Plan 02-02 — existing filter)
    - apps/hunch-web/src/hooks/use-markets.ts (Plan 02-02 — pipeline to extend)
    - crates/hunch-mint/src/dlc/invalid_cet.rs (Plan 02-01 — INVALID CET, CURATE-04 already wired)
  </read_first>
  <behavior>
    - Test 1: shouldHide(market) returns true when market.content matches violence/harm/sanctioned keywords (CURATE-05); list is configurable
    - Test 2: shouldHide returns false for benign markets (e.g., "Will BTC exceed 100k USD by Dec 31?")
    - Test 3: community-reports aggregator counts kind:30890 events with tag ["flag", "invalid"] referencing market id; threshold at N=3 raises "disputed" badge
    - Test 4: new-creator-visibility ramp: market by creator with NO prior reputation events surfaces at 0% probability on day 0 → 100% by day 7 (linear)
    - Test 5: trending threshold: market with <10 unique buyer pubkeys does NOT appear in Trending tab; ≥10 does
    - Test 6: pipeline composition: use-markets returns markets filtered through all 5 stages in order
    - Test 7: mint-side ingest_report records kind:30890 event into local SQLite + exposes via /v1/reports/{market_id} for frontend cross-check (defense in depth)
  </behavior>
  <action>
    apps/hunch-web/src/lib/curation/policy.ts (CURATE-05):
    ```typescript
    export const HIDDEN_KEYWORDS = [
      'assassinate', 'murder', 'rape', 'kill', 'bomb',
      'sanctioned-entity-1', 'sanctioned-entity-2', // populated from OFAC list snapshot
      'cp', 'csam',
    ]; // TODO Phase 3: rotate from official OFAC SDN list weekly
    export function shouldHide(market: MarketEvent): boolean {
      const lower = (market.question + ' ' + (market.outcomeRule || '')).toLowerCase();
      return HIDDEN_KEYWORDS.some(k => lower.includes(k));
    }
    ```
    Doc-comment anchors: "Frontend curation only — protocol stays neutral. Hidden markets remain readable on Nostr; this enforces hunch.io ToS §5 from Phase 1 strawman (counsel signoff pending — CONTEXT.md item 10)."

    apps/hunch-web/src/lib/curation/community-reports.ts (CURATE-06):
    async function loadReports(marketId): Promise<Report[]> queries NDK kind:30890 with #e tag = marketId; filters tag ["flag", "invalid"]. countInvalidReports(reports) returns count. Threshold N=3 → markets show "Reported by community ⚠" badge (does not auto-hide; informational).

    apps/hunch-web/src/lib/curation/new-creator-visibility.ts (CURATE-07):
    rampVisibility(creator: string, reputationEvents: ReputationEvent[]): number returns probability 0..1. Creator with ≥3 positive reputation events from distinct pubkeys → 1.0 immediately. Else linear ramp from creator's first observed event (kind:0 metadata earliest timestamp OR first kind:30888 ever published): day 0 = 0.0, day 7 = 1.0. Use as opacity / sort-rank in browse pipeline.

    apps/hunch-web/src/lib/curation/trending.ts (CURATE-03):
    export const TRENDING_MIN_PARTICIPANTS = 10;
    countUniqueParticipants(marketId, swapEvents): number counts distinct pubkeys publishing kind:38888 swap orders for this market. Markets below threshold filtered out of Trending tab.

    apps/hunch-web/src/hooks/use-markets.ts (EDIT existing from Plan 02-02 — extend pipeline):
    Pipeline order: subscribe kind:30888 → filterMarketsBySocialGraph (default ON) → filterMarketsByMute → filter via policy.shouldHide → augment with community-reports badge → apply new-creator-visibility ramp (sort + dim low-visibility markets) → if on Trending tab, filter via trending.countUniqueParticipants ≥ TRENDING_MIN_PARTICIPANTS.

    apps/hunch-web/src/components/curation/CurationBadge.tsx: small badge rendered on MarketCard for: "Reported ⚠ (N reports)" | "New creator (visibility ramping)" | "Hidden by hunch.io policy" (visible only with explicit "Show hidden" toggle in settings).

    apps/hunch-web/src/components/curation/ReportMarketButton.tsx: button on market detail → publishes kind:30890 event with tags ["e", marketId], ["flag", "invalid"], ["reason", userText]. Signs via auth store.

    apps/hunch-web/src/app/legal/curation/page.tsx: human-readable curation policy page (anchors CURATE-05 + CONTEXT.md item 10):
    - Why we curate (frontend layer, not protocol)
    - What gets hidden (violence / harm / sanctioned + community-reported beyond threshold)
    - How to challenge (publish kind:30891 vouch event; mute the policy by self-hosting fork)
    - Strawman pending counsel signoff per Phase 1+ followup

    apps/hunch-web/docs/CURATION_POLICY.md: same content in repo-readable form (≥40 lines).

    crates/hunch-mint/src/curation/mod.rs + reports.rs: defense in depth — mint also ingests kind:30890 reports (subscribes to relay) and stores into local SQLite. Exposes GET /v1/reports/{market_id} returning aggregate count + recent reporter pubkeys (no PII — pubkeys are pseudonymous). Frontend can cross-check mint count vs Nostr filter count (rough integrity signal). Plan 02-01 mint already exposes the route plumbing (http/routes.rs); this task adds the curation module + wires the route.

    crates/hunch-mint/tests/curation.rs: Test 7 + a smoke test verifying INVALID CET (Plan 02-01) still settles correctly when a kind:30890 dispute is present (no regression).

    apps/hunch-web/src/lib/curation/policy.test.ts: vitest covering Tests 1-6.
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun run test -- curation && bun run typecheck && bun run build; cd .. && cargo test -p hunch-mint --locked --test curation</automated>
  </verify>
  <acceptance_criteria>
    - bun run test -- curation passes ≥6 named tests in policy.test.ts
    - cargo test -p hunch-mint --locked --test curation passes Test 7 + the INVALID CET smoke test
    - bun run typecheck + bun run build exit 0
    - grep -E "shouldHide" apps/hunch-web/src/lib/curation/policy.ts matches
    - grep -E "30890" apps/hunch-web/src/lib/curation/community-reports.ts matches (kind:30890 dispute event)
    - grep -E "TRENDING_MIN_PARTICIPANTS" apps/hunch-web/src/lib/curation/trending.ts matches
    - grep -E "rampVisibility" apps/hunch-web/src/lib/curation/new-creator-visibility.ts matches
    - wc -l apps/hunch-web/docs/CURATION_POLICY.md ≥ 40
    - grep -E "protocol stays neutral|Frontend curation only" apps/hunch-web/src/lib/curation/policy.ts matches
    - grep -E "counsel signoff pending|counsel-pending" apps/hunch-web/docs/CURATION_POLICY.md matches (CONTEXT.md item 10)
  </acceptance_criteria>
  <done>Frontend curation pipeline (social-graph → mute → policy → reports → new-creator → trending) wired in use-markets; mint cross-check via kind:30890 ingestion; INVALID CET flow verified end-to-end; transparent CURATION_POLICY.md published.</done>
</task>

<task type="auto">
  <name>Task 2: Distribution — hunch.io (Cloudflare Pages) + hunch.onion (Tor) + IPFS pin (Pinata + web3.storage) (OPS-01..03)</name>
  <files>.github/workflows/deploy-hunch-io.yml, apps/hunch-web/wrangler.toml, apps/hunch-web/functions/_middleware.ts, deploy/cloudflare/pages-config.md, deploy/tor/torrc.example, deploy/tor/onion-deploy.sh, deploy/tor/README.md, .github/workflows/ipfs-pin.yml, scripts/ipfs-pin.ts, scripts/verify-mirror-hashes.ts, deploy/ipfs/README.md</files>
  <read_first>
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (Distribution Channels decision block + pseudonymity)
    - apps/hunch-web/Dockerfile (Plan 02-02 — out/ build output)
    - apps/hunch-web/next.config.mjs (Plan 02-02 — static export)
    - https://developers.cloudflare.com/pages/configuration/git-integration
    - https://developers.cloudflare.com/pages/functions/middleware (cf-ipcountry for geo-block — wires Plan 02-02 ip-check)
    - https://docs.pinata.cloud/api-reference/endpoint/pin-file-to-ipfs
    - https://web3.storage/docs/intro
    - https://community.torproject.org/onion-services/setup
  </read_first>
  <action>
    `.github/workflows/deploy-hunch-io.yml` (OPS-01):
    - Triggers: push to main, manual workflow_dispatch
    - Jobs: build (cd apps/hunch-web && bun install && bun run build) → deploy (uses cloudflare/pages-action@v1 with CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID secrets, project-name=hunch, directory=apps/hunch-web/out)
    - Comment in workflow header: "Deploys to hunch.io clearnet mirror. Domain registered via Njalla per CONTEXT.md item 8."

    apps/hunch-web/wrangler.toml: pages_build_output_dir = "out"; compatibility_date = "2026-05-28".

    apps/hunch-web/functions/_middleware.ts (Cloudflare Functions edge middleware for cf-ipcountry):
    ```typescript
    export const onRequest: PagesFunction = async (ctx) => {
      const country = ctx.request.cf?.country || ctx.request.headers.get('cf-ipcountry');
      const response = await ctx.next();
      response.headers.set('x-hunch-country', String(country || ''));
      return response;
    };
    ```
    Plan 02-02 ip-check.ts reads x-hunch-country header (set client-side via meta tag injected by Workers — or by reading the header via fetch-then-inspect on initial load). Document in deploy/cloudflare/pages-config.md.

    deploy/cloudflare/pages-config.md: setup walkthrough — create Pages project, connect repo, set CLOUDFLARE_API_TOKEN secret, custom domain hunch.io, functions enabled. ≥20 lines.

    deploy/tor/torrc.example (OPS-02): torrc snippet:
    ```
    HiddenServiceDir /var/lib/tor/hunch/
    HiddenServiceVersion 3
    HiddenServicePort 80 127.0.0.1:8000
    ```
    Plus operator note: "After first start, read /var/lib/tor/hunch/hostname for the .onion v3 address. Back up the private key under operator opsec."

    deploy/tor/onion-deploy.sh: bash script that, given a target host, copies apps/hunch-web/out/ via rsync to /var/www/hunch/ + reloads caddy/nginx pointing at /var/www/hunch/ + reads the .onion hostname + prints it. Defensive (set -euo pipefail; quote paths).

    deploy/tor/README.md: step-by-step Tor hidden service setup — install tor + caddy on VPS, copy torrc.example, run onion-deploy.sh, register .onion in DNS via Tor name service (or distribute via Nostr kind:0 metadata). ≥40 lines. Anchors pseudonymity (CONTEXT.md item 8: VPS via Njalla / Orangewebsite).

    .github/workflows/ipfs-pin.yml (OPS-03):
    - Triggers: release published, manual workflow_dispatch
    - Job 1: build apps/hunch-web/out → run scripts/ipfs-pin.ts → outputs CID
    - Job 2: comment CID on release + push to status/uptime.json (read by Task 3 status page)
    - Secrets: PINATA_JWT, W3_TOKEN

    scripts/ipfs-pin.ts (Bun): reads apps/hunch-web/out/ → tars → uploads to Pinata via pinFileToIPFS REST + uploads to web3.storage via w3 CLI. Returns CID. Asserts both services return the SAME CID (CIDv1 — content-addressed; identical bundle → identical CID).

    scripts/verify-mirror-hashes.ts (Bun): fetches index.html from:
    1. https://hunch.io/
    2. http://<onion>/ via SOCKS5 (`curl --socks5 127.0.0.1:9050` — script uses fetch with Tor proxy)
    3. https://ipfs.io/ipfs/<CID>/index.html

    Computes SHA256 of each + asserts all three match. Exit 0 on match, exit 1 on mismatch with diff. Stores result in status/uptime.json. This satisfies phase success criterion #2.

    deploy/ipfs/README.md: setup notes — Pinata + web3.storage account creation (anonymous email via mail.tm or Tutanota Onion per pseudonymity lock), API key scopes, CID propagation tips. ≥30 lines.
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy-hunch-io.yml && test -f apps/hunch-web/wrangler.toml && test -f apps/hunch-web/functions/_middleware.ts && test -f deploy/tor/torrc.example && test -f deploy/tor/onion-deploy.sh && bash -n deploy/tor/onion-deploy.sh && test -f scripts/ipfs-pin.ts && test -f scripts/verify-mirror-hashes.ts && bun run --bun scripts/verify-mirror-hashes.ts --dry-run 2>&1 | tee /tmp/verify.log</automated>
  </verify>
  <acceptance_criteria>
    - All workflow + config files exist
    - bash -n deploy/tor/onion-deploy.sh exits 0 (syntax valid)
    - scripts/ipfs-pin.ts + scripts/verify-mirror-hashes.ts parse via bun --bun (no runtime check needed without secrets — `bun run --bun script --help` exits 0)
    - grep -E "cloudflare/pages-action" .github/workflows/deploy-hunch-io.yml matches
    - grep -E "HiddenServiceDir" deploy/tor/torrc.example matches
    - grep -E "pinata" scripts/ipfs-pin.ts matches (case insensitive)
    - grep -E "sha256" scripts/verify-mirror-hashes.ts matches
    - grep -E "Njalla|Orangewebsite|pseudonym" deploy/tor/README.md deploy/ipfs/README.md matches at least once total
    - All workflows reference SIGNET deployment context (grep "signet" .github/workflows/deploy-hunch-io.yml) — if absent, add header comment per audit hold
    - wc -l deploy/tor/README.md ≥ 40; wc -l deploy/cloudflare/pages-config.md ≥ 20; wc -l deploy/ipfs/README.md ≥ 30
  </acceptance_criteria>
  <done>Three deployment pipelines wired: Cloudflare Pages (clearnet), Tor hidden service (onion), IPFS pin (Pinata + web3.storage); verify-mirror-hashes script asserts identical SHA256 across all three (phase success criterion #2).</done>
</task>

<task type="auto">
  <name>Task 3: Source mirrors + status page + Grafana monitoring (OPS-04, OPS-05, OPS-06)</name>
  <files>.github/workflows/sync-mirrors.yml, scripts/verify-mirrors.sh, docs/MIRRORS.md, status/index.html, status/check.ts, status/uptime.json, .github/workflows/status-check.yml, deploy/status/README.md, deploy/grafana/datasources.yml, deploy/grafana/dashboards/mint.json, deploy/grafana/dashboards/oracle.json, deploy/grafana/dashboards/relay.json, deploy/grafana/provisioning.yml, deploy/docker-compose.yml, deploy/prometheus.yml, deploy/operator-guide.md</files>
  <read_first>
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (distribution channels + pseudonymity)
    - deploy/docker-compose.yml (Plan 02-01 — extend with Grafana service)
    - deploy/prometheus.yml (Plan 02-01 — extend with relay metrics scrape)
    - crates/hunch-mint/src/metrics.rs (Plan 02-01 — metric names source of truth)
    - https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/ (dashboard JSON schema)
    - https://docs.radicle.xyz (Radicle CLI usage)
  </read_first>
  <action>
    `.github/workflows/sync-mirrors.yml` (OPS-04):
    - Triggers: push to main, schedule weekly (cron '0 0 * * 0')
    - Job: pushes current main HEAD to:
      a. GitHub (origin — assumed primary, already wired Plan 01)
      b. Codeberg (codeberg.org/hunch-protocol/hunch — secondary)
      c. Radicle (rad push — trust-minimized)
    - Then runs scripts/verify-mirrors.sh to confirm all three mirrors are at the same commit SHA
    - Secrets: CODEBERG_SSH_KEY, RAD_PASSPHRASE

    scripts/verify-mirrors.sh: bash script that git ls-remote each mirror + compares HEAD SHAs. Exits 1 if any mirror is stale (>1 commit behind). Defensive (set -euo pipefail; trap on error; quotes everywhere).

    docs/MIRRORS.md (≥30 lines): documents 3 mirrors + their purposes:
    - GitHub (primary discoverability; subject to platform censorship — see Phase 1 STORM § 1960 risk)
    - Codeberg (EU-hosted, lower censorship risk; secondary)
    - Radicle (truly decentralized, P2P; primary trust-minimized — clone via `rad clone rad:...`)
    Provides clone commands for each, public Radicle ID, and pseudonymity notes (all 3 accounts under `Silex` per CONTEXT.md).

    status/index.html (OPS-05): static HTML page (no JS framework — plain HTML + minimal vanilla JS) reading status/uptime.json + rendering a table:
    - Service column (hunch-mint, hunch-oracle, relay.hunch.markets, hunch.io, hunch.onion, IPFS pin)
    - Status column (UP / DOWN / DEGRADED with color)
    - Last checked timestamp
    - Last 7-day uptime %
    Cyberpunk dark theme inline. Self-deployed to status.hunch.markets via Cloudflare Pages (separate project from hunch.io — same Cloudflare account).

    status/check.ts: Bun script that:
    - Pings each service (HTTP HEAD for HTTP services, websocat for relay, curl --socks5 for onion)
    - Computes uptime % over the 7-day rolling window from status/history/<date>.json files
    - Writes status/uptime.json with current state + 7-day aggregate
    - Calls scripts/verify-mirror-hashes.ts (from Task 2) to add the SHA-match status

    status/uptime.json: schema { lastChecked: iso, services: [{name, url, status, last7DayUptime, sha256?}] }. Initial seed file with placeholder.

    `.github/workflows/status-check.yml`:
    - Triggers: schedule every 5 minutes (cron '*/5 * * * *') + workflow_dispatch
    - Runs status/check.ts → commits updated status/uptime.json + status/history/<date>.json
    - Pushes to main (token: GITHUB_TOKEN)
    - Caveat: 5min cron may not fit GitHub Actions free tier — alternative deployment path documented in deploy/status/README.md (e.g., self-host on the same VPS as Tor hidden service).

    deploy/status/README.md: setup walkthrough — Cloudflare Pages project `status`, custom domain status.hunch.markets, cron alternatives. ≥20 lines.

    deploy/grafana/datasources.yml: Prometheus datasource provisioning (url http://prometheus:9090).

    deploy/grafana/provisioning.yml: provisioning config pointing at /etc/grafana/provisioning/datasources/ and /etc/grafana/provisioning/dashboards/.

    deploy/grafana/dashboards/mint.json: Grafana dashboard JSON with panels:
    - mint_reserves_sats (gauge + time-series)
    - mint_tokens_issued_total (counter rate)
    - dlc_contracts_open (gauge)
    - dlc_settlements_total by outcome (stacked time-series: YES / NO / INVALID)
    - ldk_channel_balance_sats (gauge)
    - http_request_duration_seconds (histogram heatmap)

    deploy/grafana/dashboards/oracle.json: panels for oracle attestations published (kind:89 count), dispute events ingested (kind:30890), watcher status (block_height + news_api up/down), pending events count.

    deploy/grafana/dashboards/relay.json: panels for events ingested by kind (30888, 38888, 30891, 89, 88, etc.), connections active, NIP-13 PoW rejects, storage bytes used.

    deploy/docker-compose.yml (EXTEND Plan 02-01): add `grafana` service:
    ```yaml
    grafana:
      image: grafana/grafana:11.3.0
      ports: ["3000:3000"]
      environment:
        - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-changeme-signet-only}
        - GF_USERS_ALLOW_SIGN_UP=false
      volumes:
        - ./grafana/provisioning.yml:/etc/grafana/provisioning/dashboards/dashboards.yml
        - ./grafana/datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml
        - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
        - grafana-data:/var/lib/grafana
      networks: [hunch]
      depends_on: [prometheus]
    volumes:
      grafana-data:
    ```
    Plus extend prometheus.yml with relay scrape job (nostr-rs-relay exposes Prometheus metrics on its built-in /metrics endpoint).

    deploy/operator-guide.md (EXTEND Plan 02-01): append section:
    - "Monitoring": access Grafana at http://localhost:3000 (default user admin/changeme-signet-only); 3 pre-provisioned dashboards
    - "Status page": deploy status/ via Cloudflare Pages or self-host; URL status.hunch.markets
    - Update audit-hold reminder: "Grafana admin password must be rotated before any mainnet exposure (Phase 3 ops hardening)."
  </action>
  <verify>
    <automated>test -f .github/workflows/sync-mirrors.yml && test -f scripts/verify-mirrors.sh && bash -n scripts/verify-mirrors.sh && test -f docs/MIRRORS.md && wc -l docs/MIRRORS.md | awk '{exit ($1 >= 30) ? 0 : 1}' && test -f status/index.html && test -f status/check.ts && test -f .github/workflows/status-check.yml && for f in deploy/grafana/dashboards/mint.json deploy/grafana/dashboards/oracle.json deploy/grafana/dashboards/relay.json; do jq empty "$f"; done && docker compose -f deploy/docker-compose.yml config > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/verify-mirrors.sh exits 0
    - jq parses all 3 Grafana dashboard JSON files (mint, oracle, relay) without error
    - docker compose -f deploy/docker-compose.yml config exits 0 and now lists 5 services (relay, mint, oracle, prometheus, grafana)
    - grep -E "radicle|rad push" .github/workflows/sync-mirrors.yml matches
    - grep -E "codeberg" .github/workflows/sync-mirrors.yml matches
    - grep -E "Silex" docs/MIRRORS.md matches (pseudonymity reference)
    - grep -E "mint_reserves_sats" deploy/grafana/dashboards/mint.json matches
    - grep -E "kind:.89|kind 89|oracle" deploy/grafana/dashboards/oracle.json matches
    - grep -E "30888|kind" deploy/grafana/dashboards/relay.json matches
    - grep -E "changeme-signet-only|signet-only" deploy/docker-compose.yml matches (audit hold anchored in default password)
    - wc -l docs/MIRRORS.md ≥ 30; wc -l deploy/status/README.md ≥ 20
  </acceptance_criteria>
  <done>3 source mirrors verified weekly; status.hunch.markets public uptime page deployable; Prometheus + Grafana with 3 dashboards (mint + oracle + relay) provisioned via docker-compose.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — Phase 2 close gate — 3 mirrors identical + monitoring live + anti-spam end-to-end</name>
  <what-built>
    Phase 2 close gate. Anti-spam pipeline (CURATE-01..07) fully wired in frontend + mint cross-check; frontend deployed to hunch.io (Cloudflare Pages) + hunch.onion (Tor hidden service) + IPFS pin (Pinata + web3.storage) with SHA256 equality verified across all three; source mirrors live on GitHub + Radicle + Codeberg with weekly sync verification; status.hunch.markets public uptime page; Prometheus + Grafana monitoring stack on mint + oracle + relay with 3 dashboards.
  </what-built>
  <how-to-verify>
    Anti-spam (CURATE-01..07):
    1. cd apps/hunch-web && bun run test -- curation passes ≥6 named tests
    2. cargo test -p hunch-mint --locked --test curation passes (anti-spam cross-check + INVALID CET smoke)
    3. Visit /browse with default-ON social-graph filter — only markets from your follow graph (1-2 hop) visible
    4. Disable filter in /settings → all markets visible
    5. Visit /legal/curation → human-readable policy renders with PendingCounselNotice
    6. Read apps/hunch-web/docs/CURATION_POLICY.md — confirm ≥40 lines, transparent, mentions protocol-neutrality
    7. Click "Report" on a market → kind:30890 event publishes (verify in Nostr client)
    8. Trigger INVALID outcome via docker compose exec oracle hunch-oracle attest --outcome Invalid → mint broadcasts 50/50 refund CET → ResolutionPanel renders INVALID badge

    Distribution (OPS-01..04):
    9. Run scripts/verify-mirror-hashes.ts (with Tor SOCKS5 proxy live at 127.0.0.1:9050 and IPFS CID + onion address configured) → exits 0 with "SHA256 match across hunch.io / hunch.onion / IPFS pin"
    10. git ls-remote each of github/codeberg/radicle for current main HEAD — all three return same SHA (or .github/workflows/sync-mirrors.yml has run successfully recently)
    11. Read docs/MIRRORS.md — confirm clone command for each mirror is documented and tested
    12. Visit https://hunch.io/ in a clean browser — site loads, ToS gate visible
    13. Visit http://<onion>/ via Tor Browser — same site, identical content
    14. Visit https://ipfs.io/ipfs/<CID>/ — same site, identical content

    Status + Monitoring (OPS-05, OPS-06):
    15. Visit status.hunch.markets — page renders with current uptime for all 6 services
    16. status/uptime.json is fresh (< 10 min old timestamp)
    17. With docker-compose stack from Plan 02-01 + this plan's Grafana extension running locally: visit http://localhost:3000 (admin / changeme-signet-only) → 3 pre-provisioned dashboards (mint, oracle, relay) all render with live data
    18. mint dashboard shows mint_reserves_sats > 0 (after deposit) + dlc_contracts_open > 0 (after market creation)

    Pseudonymity + audit hold:
    19. grep -rE "(Alexandre|Cangini|alexandre\.cangini)" apps/hunch-web crates deploy docs status returns empty
    20. grep -rE "SIGNET ONLY|signet-only|signet only" deploy docs apps/hunch-web/docs returns ≥ 3 matches (audit hold anchored)
    21. Grafana admin password literal "changeme-signet-only" in deploy/docker-compose.yml is a deliberate forcing function — confirm operator-guide.md instructs rotation before Phase 3 mainnet exposure

    Phase 2 success criteria check:
    - SC#1 (E2E signet flow) — covered jointly with Plans 02-01 + 02-02; verified at this checkpoint via step 7-8 above
    - SC#2 (3 mirrors identical) — step 9 above
    - SC#3 (<1h external operator deploy) — operator-guide.md (Plan 02-01) extended with Grafana + status; spot-check by reading guide cover-to-cover
    - SC#4 (anti-spam working) — steps 1-8 above
    - SC#5 (geo-block + ToS gate) — verified in Plan 02-02 checkpoint; re-verify nothing regressed by visiting hunch.io with cf-ipcountry: US override → /blocked renders
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures (which step number).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Cloudflare | CLOUDFLARE_API_TOKEN held by operator; Cloudflare can serve different content than IPFS (mitigated by verify-mirror-hashes.ts) |
| operator → Pinata / web3.storage | Pin services hold the bundle; could delete (mitigated by 2 redundant services) |
| operator → Radicle / GitHub / Codeberg | Source mirrors — GitHub can deplatform (mitigated by Radicle as trust-minimized) |
| Grafana → Prometheus → mint/oracle | Internal docker network only; not exposed publicly without operator action |
| frontend curation policy | Frontend layer only — protocol stays neutral; bypass = self-host fork |
| status page | Public, no auth; reads from status/uptime.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-D-01 | T (Tamper) | Cloudflare serves modified content | mitigate | verify-mirror-hashes.ts cross-checks SHA256 vs IPFS pin; users can clone IPFS bundle directly via CID |
| T-02-D-02 | T (Tamper) | IPFS pin content modification | mitigate | CID is content-addressed — modification → different CID → cross-check fails |
| T-02-D-03 | T (Tamper) | Codeberg / GitHub repo modification | mitigate | sync-mirrors.yml verifies HEAD SHA consistency across all 3 mirrors; Radicle provides trust-minimized path |
| T-02-D-04 | R (Repudiation) | curation policy disputes | mitigate | docs/CURATION_POLICY.md transparent; users can publish kind:30891 vouch events; users can self-host fork without policy |
| T-02-D-05 | I (Info leak) | Cloudflare cf-ipcountry tracking | accept | header is request-scoped; not logged by us; documented in PRIVACY.md as Cloudflare's tracking surface (out of our control) |
| T-02-D-06 | I (Info leak) | Pinata / web3.storage upload metadata | accept | services see the bundle content (public); IP at upload time (we authenticate via CI — bot IP, not user IP) |
| T-02-D-07 | I (Info leak) | Grafana exposure | mitigate | Grafana bound to localhost in docker-compose; admin password is forcing-function placeholder; operator-guide.md requires rotation before mainnet |
| T-02-D-08 | D (DoS) | Cloudflare Pages rate-limits | accept | Cloudflare free tier limits documented; Tor + IPFS mirrors provide failover |
| T-02-D-09 | D (DoS) | Tor hidden service traffic | accept | Tor inherently rate-limits; documented in deploy/tor/README.md |
| T-02-D-10 | D (DoS) | community-report spam | mitigate | NIP-13 PoW filter at relay (Plan 02-01); threshold N=3 reports before badge — single reports don't move the needle |
| T-02-D-11 | E (Escalation) | curation policy expansion to censorship | mitigate | docs/CURATION_POLICY.md anchors hardcoded scope (violence/harm/sanctioned); changes require git commit + diff visibility; protocol layer stays neutral (markets remain readable on Nostr) |
| T-02-D-12 | E (Escalation) | GitHub Actions secret leak | accept | GitHub-managed secrets vault; documented in deploy/cloudflare/pages-config.md; rotation procedure in Phase 3 SEC-07 |
| T-02-D-13 | S (Spoof) | malicious mirror clone | mitigate | docs/MIRRORS.md lists official Radicle ID (trust anchor); cross-verify via Nostr kind:0 pubkey announce by Silex |
| T-02-D-14 | T (Tamper) | new-creator-visibility gaming (Sybil) | accept | creators with fake reputation events still gated by social-graph filter for users not following them; full Sybil resistance is Phase 4 federation work |
</threat_model>

<verification>
- Anti-spam: bun run test -- curation + cargo test -p hunch-mint --test curation both pass
- Distribution: scripts/verify-mirror-hashes.ts exits 0 across hunch.io + onion + IPFS
- Source mirrors: scripts/verify-mirrors.sh confirms identical HEAD SHA on GitHub + Codeberg + Radicle
- Status: status/uptime.json is current; status.hunch.markets renders with 6 service rows
- Monitoring: docker-compose up → Grafana with 3 dashboards live; mint dashboard shows mint_reserves_sats with real data
- Phase 2 close: all 62 requirements (Plans 01 + 02 + 03) demonstrably addressed
- Pseudonymity: zero real-name leakage across all Plan 03 artifacts
- Audit hold: SIGNET ONLY language anchored in ≥3 distribution + operations files
</verification>

<success_criteria>
This plan closes the Phase 2 gate via the 5 phase-level criteria (per ROADMAP):

1. **End-to-end signet flow** — verified at this checkpoint by walking the bet flow against the live docker-compose stack (Plans 02-01 + 02-02 + 02-03 working together)
2. **3 frontend mirrors identical** — scripts/verify-mirror-hashes.ts proves SHA256 equality across hunch.io / hunch.onion / IPFS pin
3. **External operator <1h deploy** — operator-guide.md (Plan 02-01 + this plan's monitoring/status extensions) reviewed end-to-end at checkpoint
4. **Anti-spam working** — Task 1 covers CURATE-01..07; INVALID outcome CET (Plan 02-01) verified end-to-end via step 8
5. **Geo-block US + ToS gate functional** — Plan 02-02 deliverable re-verified at step 13 with cf-ipcountry override; Plan 02-02 PendingCounselNotice anchors CONTEXT.md item 10 amendment
</success_criteria>

<output>
After completion, create .planning/phases/02-mainnet-spine/02-03-SUMMARY.md per template. Also append to .planning/phases/02-mainnet-spine/ a final PHASE-CLOSE.md summary cross-referencing all 3 plan SUMMARYs + listing the 5 success criteria with verification anchors.
</output>
