---
phase: 02-mainnet-spine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/hunch-web/package.json
  - apps/hunch-web/next.config.mjs
  - apps/hunch-web/tsconfig.json
  - apps/hunch-web/tailwind.config.ts
  - apps/hunch-web/postcss.config.mjs
  - apps/hunch-web/.eslintrc.json
  - apps/hunch-web/playwright.config.ts
  - apps/hunch-web/public/manifest.json
  - apps/hunch-web/public/robots.txt
  - apps/hunch-web/public/sw.js
  - apps/hunch-web/public/fonts/JetBrainsMono-Regular.woff2
  - apps/hunch-web/public/fonts/JetBrainsMono-Bold.woff2
  - apps/hunch-web/public/fonts/LICENSE.md
  - apps/hunch-web/src/app/layout.tsx
  - apps/hunch-web/src/app/page.tsx
  - apps/hunch-web/src/app/globals.css
  - apps/hunch-web/src/app/not-found.tsx
  - apps/hunch-web/src/app/blocked/page.tsx
  - apps/hunch-web/src/app/onboarding/page.tsx
  - apps/hunch-web/src/app/browse/page.tsx
  - apps/hunch-web/src/app/market/[id]/page.tsx
  - apps/hunch-web/src/app/market/create/page.tsx
  - apps/hunch-web/src/app/account/page.tsx
  - apps/hunch-web/src/app/oracles/page.tsx
  - apps/hunch-web/src/app/oracles/[pubkey]/page.tsx
  - apps/hunch-web/src/app/settings/page.tsx
  - apps/hunch-web/src/app/legal/terms/page.tsx
  - apps/hunch-web/src/app/legal/privacy/page.tsx
  - apps/hunch-web/src/components/Shell.tsx
  - apps/hunch-web/src/components/Nav.tsx
  - apps/hunch-web/src/components/Footer.tsx
  - apps/hunch-web/src/components/Toaster.tsx
  - apps/hunch-web/src/components/ui/button.tsx
  - apps/hunch-web/src/components/ui/card.tsx
  - apps/hunch-web/src/components/ui/dialog.tsx
  - apps/hunch-web/src/components/ui/input.tsx
  - apps/hunch-web/src/components/ui/select.tsx
  - apps/hunch-web/src/components/ui/tabs.tsx
  - apps/hunch-web/src/components/ui/sheet.tsx
  - apps/hunch-web/src/components/ui/badge.tsx
  - apps/hunch-web/src/components/ui/toast.tsx
  - apps/hunch-web/src/components/market/MarketCard.tsx
  - apps/hunch-web/src/components/market/MarketChart.tsx
  - apps/hunch-web/src/components/market/BuySellBox.tsx
  - apps/hunch-web/src/components/market/ResolutionPanel.tsx
  - apps/hunch-web/src/components/market/OracleBadge.tsx
  - apps/hunch-web/src/components/market/CreateMarketForm.tsx
  - apps/hunch-web/src/components/wallet/WalletConnect.tsx
  - apps/hunch-web/src/components/wallet/LightningDeposit.tsx
  - apps/hunch-web/src/components/wallet/LightningWithdraw.tsx
  - apps/hunch-web/src/components/account/Portfolio.tsx
  - apps/hunch-web/src/components/account/PositionRow.tsx
  - apps/hunch-web/src/components/oracle/OracleCard.tsx
  - apps/hunch-web/src/components/oracle/AttestationHistory.tsx
  - apps/hunch-web/src/components/reputation/ReputationForm.tsx
  - apps/hunch-web/src/components/geo/GeoBlockGate.tsx
  - apps/hunch-web/src/components/legal/TermsGate.tsx
  - apps/hunch-web/src/components/legal/PendingCounselNotice.tsx
  - apps/hunch-web/src/lib/nostr/ndk.ts
  - apps/hunch-web/src/lib/nostr/nip07.ts
  - apps/hunch-web/src/lib/nostr/nip46.ts
  - apps/hunch-web/src/lib/nostr/signer.ts
  - apps/hunch-web/src/lib/cashu/mint-client.ts
  - apps/hunch-web/src/lib/cashu/tokens.ts
  - apps/hunch-web/src/lib/lightning/webln.ts
  - apps/hunch-web/src/lib/types/events.ts
  - apps/hunch-web/src/lib/types/market.ts
  - apps/hunch-web/src/lib/filter/social-graph.ts
  - apps/hunch-web/src/lib/filter/mute-list.ts
  - apps/hunch-web/src/lib/geo/ip-check.ts
  - apps/hunch-web/src/lib/geo/tor-exits.ts
  - apps/hunch-web/src/lib/geo/tor-exits.json
  - apps/hunch-web/src/lib/i18n.ts
  - apps/hunch-web/src/i18n/config.ts
  - apps/hunch-web/src/i18n/en.json
  - apps/hunch-web/src/i18n/fr.json
  - apps/hunch-web/src/state/auth.ts
  - apps/hunch-web/src/state/wallet.ts
  - apps/hunch-web/src/hooks/use-markets.ts
  - apps/hunch-web/src/hooks/use-oracle.ts
  - apps/hunch-web/src/hooks/use-positions.ts
  - apps/hunch-web/src/hooks/use-tor-detection.ts
  - apps/hunch-web/src/lib/cashu/mint-client.test.ts
  - apps/hunch-web/src/lib/nostr/signer.test.ts
  - apps/hunch-web/src/lib/filter/social-graph.test.ts
  - apps/hunch-web/src/lib/geo/ip-check.test.ts
  - apps/hunch-web/tests/e2e/bet-flow.spec.ts
  - apps/hunch-web/tests/e2e/onboarding.spec.ts
  - apps/hunch-web/tests/e2e/tor-compatibility.spec.ts
  - apps/hunch-web/tests/e2e/geo-block.spec.ts
  - apps/hunch-web/docs/SELF_HOSTING.md
  - apps/hunch-web/Dockerfile
  - apps/hunch-web/docker-compose.yml
  - apps/hunch-web/scripts/fetch-tor-exits.ts
  - package.json
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10, UI-11, UI-12, UI-13, UI-14, UI-15, UI-16, UI-17, UI-18, UI-19, UI-20, UI-21]
requirements_addressed: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10, UI-11, UI-12, UI-13, UI-14, UI-15, UI-16, UI-17, UI-18, UI-19, UI-20, UI-21]
tags: [hunch, frontend, nextjs, tailwind, shadcn, ndk, cashu-ts, webln, nip07, nip46, i18n, pwa, geo-block, tor]

must_haves:
  truths:
    - "apps/hunch-web Next.js 15 App Router builds to static export; bun run build produces deployable out/"
    - "Tailwind + shadcn/ui cyberpunk dark theme; hot-pink accent on dark background; JetBrains Mono self-hosted (no Google Fonts)"
    - "NDK integration with NIP-07 default + NIP-46 alternate; no auto-generated keys (onboarding directs to Alby or Amber)"
    - "cashu-ts client connects to hunch-mint HTTP API; supports NUT-CTF conditional token issuance + DLEQ verification"
    - "WebLN integration handles Lightning deposit (mint quote → invoice → pay → tokens) and withdraw (melt quote → BOLT-11 → mint pays)"
    - "Browse page applies default 1-2 hop social-graph filter (kind:30000); markets outside follow graph HIDDEN by default"
    - "Market detail shows oracle reputation + chart + buy/sell box + resolution rules + verify-yourself link"
    - "Buy YES/NO flow: sat amount → WebLN pay → mint issues conditional token tied to chosen outcome"
    - "Sell flow: publishes kind:38888 atomic-swap order OR submits to mint /v1/swap"
    - "Resolution: shows kind:89 attestation + in-browser Schnorr verify badge + on-chain settlement tx link"
    - "Permissionless market creation: kind:30888 with question + outcome rule + oracle pubkey + expiry"
    - "Account page lists positions across markets with P&L + tx history"
    - "Oracle marketplace browsable; detail page shows reputation events + attestation history"
    - "Settings: mute users (NIP-51 kind:10000), choose relays (NIP-65 kind:10002), choose mints"
    - "Reputation form publishes kind:30891 per HIP-5"
    - "EN + FR locales cover all surface-visible strings"
    - "Tor Browser compatibility: no broken flows; fonts self-hosted; no tracking pixels"
    - "Mobile responsive + PWA install prompt (manifest.json + service worker)"
    - "Geo-block US: cf-ipcountry header check + Tor exit list + ToS gate first-visit modal"
    - "Self-hosting guide enables independent operator deploy in <1 hour"
    - "Phase 1 legal strawmen surfaced at /legal/terms + /legal/privacy with PendingCounselNotice (CONTEXT.md item 10)"
  artifacts:
    - path: "apps/hunch-web/next.config.mjs"
      provides: "Next.js 15 static export"
      contains: "output: 'export'"
    - path: "apps/hunch-web/src/lib/nostr/signer.ts"
      provides: "Unified Signer interface (NIP-07 / NIP-46)"
      contains: "interface Signer"
    - path: "apps/hunch-web/src/lib/cashu/mint-client.ts"
      provides: "cashu-ts wrapper for hunch-mint"
      contains: "CashuMint"
    - path: "apps/hunch-web/src/lib/filter/social-graph.ts"
      provides: "1-2 hop social graph filter default ON"
      contains: "30000"
    - path: "apps/hunch-web/src/lib/geo/ip-check.ts"
      provides: "cf-ipcountry geo-block US"
      contains: "US"
    - path: "apps/hunch-web/src/components/market/CreateMarketForm.tsx"
      provides: "Permissionless kind:30888 publisher"
      contains: "30888"
    - path: "apps/hunch-web/src/components/reputation/ReputationForm.tsx"
      provides: "kind:30891 reputation publisher (HIP-5)"
      contains: "30891"
    - path: "apps/hunch-web/src/components/legal/PendingCounselNotice.tsx"
      provides: "Draft, counsel-pending disclosure (CONTEXT.md item 10)"
      contains: "counsel"
    - path: "apps/hunch-web/src/i18n/fr.json"
      provides: "French locale strings"
      min_lines: 50
    - path: "apps/hunch-web/docs/SELF_HOSTING.md"
      provides: "<1h self-hosting guide"
      min_lines: 60
  key_links:
    - from: "apps/hunch-web/src/lib/cashu/mint-client.ts"
      to: "crates/hunch-mint/src/http/routes.rs"
      via: "HTTP API /v1/mint, /v1/melt, /v1/swap, /v1/info"
      pattern: "/v1/"
    - from: "apps/hunch-web/src/components/market/ResolutionPanel.tsx"
      to: "crates/hunch-oracle/src/nip88/attestation.rs"
      via: "kind:89 attestation subscription + Schnorr verify"
      pattern: "kind.*89"
    - from: "apps/hunch-web/src/lib/filter/social-graph.ts"
      to: "Nostr kind:30000 contact lists"
      via: "NDK filter for 1-2 hop follow graph"
      pattern: "30000"
    - from: "apps/hunch-web/src/components/geo/GeoBlockGate.tsx"
      to: "Cloudflare cf-ipcountry + Tor exit list"
      via: "Runtime geo check at app shell mount"
      pattern: "US"

# Rationale for >15 files_modified: Next.js 15 App Router frontend with ~12
# routes, ~25 components, i18n (EN+FR), ~8 lib modules, ~8 test files. Coarse
# granularity collapses 21 UI reqs into 6 tasks (NOT 21). Splitting further
# would create cross-component churn (a single bet flow touches signer + cashu
# client + WebLN + market chart + buy/sell box — all co-required).
---

<objective>
Build the Hunch reference frontend (`apps/hunch-web`) — Next.js 15 + Tailwind + shadcn/ui + NDK + cashu-ts + WebLN. Covers UI-01..21 (21 requirements). Static-exportable bundle (deployable to Cloudflare Pages, IPFS, Tor hidden service). Cyberpunk dark theme. NIP-07 + NIP-46 auth. Full bet flow (browse → market detail → buy/sell → resolution → portfolio). Permissionless market creation. Oracle marketplace. EN + FR i18n. PWA install. Geo-block US + ToS gate. Self-hosting guide.

Purpose: Deliver the user-facing surface that exercises the protocol services from Plan 02-01. Frontend is the path through which success criterion #1 (E2E signet flow) becomes user-visible.

Output: A working `bun run build` static bundle + Playwright E2E suite + self-hosting guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-mainnet-spine/02-CONTEXT.md
@.planning/phases/01-cypherpunk-foundation/01-CONTEXT.md
@docs/HIP-1.md
@docs/HIP-2.md
@docs/HIP-3.md
@docs/HIP-5.md
@.planning/phases/02-mainnet-spine/02-01-SUMMARY.md
@package.json

<interfaces>
Mint HTTP API (from Plan 02-01 Task 4 openapi.yaml):
- POST /v1/mint/quote → { quote_id, request_invoice, amount_sat }  (NUT-04)
- POST /v1/mint        → { proofs: BlindedSignature[] }              (NUT-04 issue)
- POST /v1/melt/quote  → { quote_id, fee_sat }                       (NUT-05)
- POST /v1/melt        → { paid: true, preimage }                    (NUT-05 pay)
- POST /v1/swap        → { outputs: Proof[] }                        (NUT-03 + NUT-CTF)
- GET  /v1/info        → MintInfo (NUT-06; nuts list + NUT-CTF feature)
- GET  /metrics        → Prometheus
- GET  /docs           → Swagger UI

Nostr event kinds (from crates/hunch-protocol/src/kinds.rs):
- 30888 KIND_MARKET, 38888 KIND_ORDER, 38889 KIND_TIER2_ORDER
- 30890 KIND_DISPUTE, 30891 KIND_REPUTATION, 30892 KIND_MINT_ANNOUNCE
- 88 KIND_ORACLE_ANNOUNCE (NIP-88), 89 KIND_ORACLE_ATTESTATION
- 30000 Nostr contact lists (social-graph filter)

NUT-CTF token shape (HIP-3 + crates/hunch-mint/src/nutctf/token.rs):
```typescript
interface ConditionalToken {
  keysetId: string;
  condition: { oraclePubkey: string; eventId: string; outcome: "YES" | "NO" | "INVALID" };
  blindedSecret: string;
  signature: string;
  dleqProof: { e: string; s: string; r: string };
}
```

External SDKs:
- @nostr-dev-kit/ndk — Nostr SDK
- @cashu/cashu-ts — Cashu SDK
- WebLN: window.webln.{enable, sendPayment, makeInvoice}
- shadcn/ui: copy primitives directly into src/components/ui/
- @noble/curves: in-browser Schnorr verify for attestation badge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend scaffold + cyberpunk theme + base UI primitives (UI-01, UI-02)</name>
  <files>apps/hunch-web/package.json, apps/hunch-web/next.config.mjs, apps/hunch-web/tsconfig.json, apps/hunch-web/tailwind.config.ts, apps/hunch-web/postcss.config.mjs, apps/hunch-web/.eslintrc.json, apps/hunch-web/playwright.config.ts, apps/hunch-web/public/manifest.json, apps/hunch-web/public/robots.txt, apps/hunch-web/public/fonts/JetBrainsMono-Regular.woff2, apps/hunch-web/public/fonts/JetBrainsMono-Bold.woff2, apps/hunch-web/public/fonts/LICENSE.md, apps/hunch-web/src/app/layout.tsx, apps/hunch-web/src/app/page.tsx, apps/hunch-web/src/app/globals.css, apps/hunch-web/src/app/not-found.tsx, apps/hunch-web/src/components/Shell.tsx, apps/hunch-web/src/components/Nav.tsx, apps/hunch-web/src/components/Footer.tsx, apps/hunch-web/src/components/Toaster.tsx, apps/hunch-web/src/components/ui/button.tsx, apps/hunch-web/src/components/ui/card.tsx, apps/hunch-web/src/components/ui/dialog.tsx, apps/hunch-web/src/components/ui/input.tsx, apps/hunch-web/src/components/ui/select.tsx, apps/hunch-web/src/components/ui/tabs.tsx, apps/hunch-web/src/components/ui/sheet.tsx, apps/hunch-web/src/components/ui/badge.tsx, apps/hunch-web/src/components/ui/toast.tsx, package.json</files>
  <read_first>
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (Frontend stack decisions)
    - https://nextjs.org/docs/app/building-your-application/deploying/static-exports
    - https://ui.shadcn.com/docs/installation/next
    - CLAUDE.md (no analytics, no Google Fonts → self-host JetBrains Mono)
    - docs/legal/PRIVACY.md (Phase 1 strawman)
    - package.json (root Bun workspace from Plan 01)
  </read_first>
  <action>
    Initialize apps/hunch-web as Bun + Next.js 15 App Router (manual config, no interactive create-next-app). Root package.json workspaces array must include "apps/hunch-web".

    apps/hunch-web/package.json deps: next 15.0.3, react 19.0.0, react-dom 19.0.0, @nostr-dev-kit/ndk ^2.10.0, @cashu/cashu-ts ^1.2.0, zustand ^5.0.1, class-variance-authority ^0.7.0, tailwind-merge ^2.5.4, clsx ^2.1.1, lucide-react ^0.460.0, recharts ^2.13.3. devDeps: typescript ^5.6.3, @types/react ^19, @types/react-dom ^19, @types/node ^22, tailwindcss ^3.4.15, postcss ^8.4.49, autoprefixer ^10.4.20, vitest ^2.1.6, @playwright/test ^1.49.0, eslint ^9.16.0, eslint-config-next 15.0.3. Scripts: dev, build, start, lint, test (vitest), test:e2e (playwright), typecheck (tsc --noEmit).

    next.config.mjs: { output: 'export', images: { unoptimized: true }, trailingSlash: true, reactStrictMode: true }.

    tsconfig.json: extends Next default; strict: true; paths alias @/* -> src/*.

    tailwind.config.ts: cyberpunk dark theme — background #0a0a0a, foreground #fafafa, accent HOT-PINK #ff2d75 (Claude's Discretion per CONTEXT.md — picked over terminal-green for sharper visual signature), card #141414. HSL CSS variables. Font: JetBrains Mono (monospace) + system-ui (sans).

    src/app/globals.css: Tailwind base/components/utilities + @font-face self-hosted JetBrains Mono (Regular + Bold woff2 in public/fonts/). Body: bg-background text-foreground font-mono antialiased.

    Self-host fonts: download JetBrainsMono-Regular.woff2 + JetBrainsMono-Bold.woff2 from JetBrains GitHub release (Apache 2.0). Document source + license in public/fonts/LICENSE.md.

    public/manifest.json (PWA): name=Hunch, short_name=Hunch, start_url=/, display=standalone, background_color=#0a0a0a, theme_color=#ff2d75, icons (192, 512).

    public/robots.txt: User-agent * / Disallow /account / Disallow /settings.

    shadcn/ui primitives in src/components/ui/ (button, card, dialog, input, select, tabs, sheet, badge, toast) — copy canonical shadcn source, restyle for cyberpunk theme.

    src/components/Shell.tsx: wraps Nav + main + Footer. Mounts TermsGate and GeoBlockGate (from Task 6) as effect-only guards.

    src/components/Nav.tsx: top nav with logo + links (Browse, Create, Oracles, Account, Settings) + wallet connect button. Mobile via shadcn Sheet.

    src/components/Footer.tsx: legal links (/legal/terms, /legal/privacy) + GitHub + Radicle + Nostr links + "draft, counsel-pending" tag (CONTEXT.md item 10).

    src/app/layout.tsx: root layout with html lang from i18n config (Task 5); children inside Shell.

    src/app/page.tsx: server-component redirect to /browse.

    src/app/not-found.tsx: 404 with link back to /browse.

    playwright.config.ts: standard config, baseURL http://localhost:3000, chromium + firefox, trace on first-retry.

    .eslintrc.json: extends next/core-web-vitals.

    ZERO third-party trackers, ZERO Google Analytics, ZERO PostHog, ZERO Sentry, ZERO Google Fonts (CLAUDE.md Engineering Don'ts + PRIVACY).
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun install && bun run typecheck && bun run lint && bun run build && test -d out && test -f out/index.html; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun install + bun run typecheck + bun run build all exit 0 inside apps/hunch-web
    - apps/hunch-web/out/index.html exists (static export verified)
    - grep -rE "Google|googleapis|fonts.gstatic" apps/hunch-web/src apps/hunch-web/public returns empty
    - grep -rE "googletagmanager|google-analytics|posthog|sentry|fullstory" apps/hunch-web returns empty
    - apps/hunch-web/public/fonts/JetBrainsMono-Regular.woff2 exists
    - grep -E "output: 'export'" apps/hunch-web/next.config.mjs matches
    - grep -E "#ff2d75|hot.pink" apps/hunch-web/tailwind.config.ts apps/hunch-web/src/app/globals.css matches at least once
  </acceptance_criteria>
  <done>Frontend scaffold builds to static export; cyberpunk dark theme with hot-pink accent; JetBrains Mono self-hosted; no third-party trackers; shadcn/ui primitives in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth + wallet integration — NDK + NIP-07 + NIP-46 + cashu-ts + WebLN (UI-03, UI-04, UI-05)</name>
  <files>apps/hunch-web/src/lib/nostr/ndk.ts, apps/hunch-web/src/lib/nostr/nip07.ts, apps/hunch-web/src/lib/nostr/nip46.ts, apps/hunch-web/src/lib/nostr/signer.ts, apps/hunch-web/src/lib/cashu/mint-client.ts, apps/hunch-web/src/lib/cashu/tokens.ts, apps/hunch-web/src/lib/lightning/webln.ts, apps/hunch-web/src/lib/types/events.ts, apps/hunch-web/src/lib/types/market.ts, apps/hunch-web/src/state/auth.ts, apps/hunch-web/src/state/wallet.ts, apps/hunch-web/src/components/wallet/WalletConnect.tsx, apps/hunch-web/src/components/wallet/LightningDeposit.tsx, apps/hunch-web/src/components/wallet/LightningWithdraw.tsx, apps/hunch-web/src/lib/nostr/signer.test.ts, apps/hunch-web/src/lib/cashu/mint-client.test.ts</files>
  <read_first>
    - https://github.com/nostr-dev-kit/ndk (NIP-07 + NIP-46 examples)
    - https://github.com/cashubtc/cashu-ts (mint client example)
    - https://www.webln.guide (WebLN spec)
    - https://github.com/nostr-protocol/nips/blob/master/07.md
    - https://github.com/nostr-protocol/nips/blob/master/46.md
    - crates/hunch-mint/openapi.yaml (Plan 02-01 Task 4 — HTTP contract)
    - crates/hunch-protocol/src/kinds.rs
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (NIP-07 default, NIP-46 alternate, no auto-gen keys)
  </read_first>
  <behavior>
    - Test 1: NIP-07 signer reads window.nostr.getPublicKey() and rejects when extension absent
    - Test 2: NIP-46 signer connects via bunker URI and signs an event remotely
    - Test 3: Unified Signer picks NIP-07 first; falls back to NIP-46 only if user chose Connect path
    - Test 4: cashu-ts mint client calls GET /v1/info and parses NUT-CTF feature flag
    - Test 5: mint client builds a NUT-04 mint quote, deserializes invoice, exposes pay(invoice) → tokens end-to-end (mock fetch)
    - Test 6: WebLN wrapper degrades gracefully when window.webln absent (install-Alby toast)
  </behavior>
  <action>
    src/lib/nostr/ndk.ts: singleton NDK with default relays ["wss://relay.hunch.markets", "wss://relay.damus.io", "wss://nos.lol"] (overridable from settings — Task 4). NDK cache adapter (in-memory + IndexedDB).

    src/lib/nostr/signer.ts: export interface Signer { pubkey(): Promise<string>; signEvent(template): Promise<NostrEvent>; kind(): "nip07" | "nip46"; }. export async function detectSigner(): Promise<Signer | null> — NIP-07 first.

    src/lib/nostr/nip07.ts: Nip07Signer implements Signer reading window.nostr. Throws NoExtensionError if absent.

    src/lib/nostr/nip46.ts: Nip46Signer implements Signer using NDK's NDKNip46Signer with bunker URI input. Persists bunker connection in localStorage.

    src/state/auth.ts: Zustand store { signer, pubkey, connect(kind), disconnect() }. NO auto-key generation (CONTEXT.md decision). connect("nip07") throws NoExtensionError → UI toast directs to Alby.

    src/lib/cashu/mint-client.ts: thin wrapper over @cashu/cashu-ts CashuMint + CashuWallet. Adds requestConditionalMint(amountSat, condition: { oraclePubkey, eventId, outcome }) → POSTs /v1/mint with NUT-CTF condition per HIP-3.

    src/lib/cashu/tokens.ts: ConditionalToken type (mirrors interfaces block) + verifyDleqOrThrow(token) calling cashu-ts DLEQ verifier (MANDATORY per NUT-12 + CLAUDE.md Do).

    src/lib/lightning/webln.ts: enableWebln(): Promise<{ available, provider }>; payInvoice(bolt11); makeInvoice(amountSat, memo). Clean missing-webln detection.

    src/lib/types/events.ts: TS mirror of hunch-protocol event kinds (KIND_MARKET=30888, etc.). Hand-mirrored with TODO comment pointing to scripts/sync-kinds.ts (deferred to Phase 3).

    src/lib/types/market.ts: TS types matching MarketEvent + DlcContract + ConditionalToken from hunch-protocol.

    src/state/wallet.ts: Zustand store { proofs, balanceSat, deposit(amount), withdraw(amount, invoice), refresh() }. Proofs persisted to localStorage; balance computed.

    src/components/wallet/WalletConnect.tsx: button → modal with two options ("Connect Nostr Extension (Alby, nos2x)" + "Connect via NIP-46 (Amber, nsec.app)"). NIP-46 path: paste bunker URI.

    src/components/wallet/LightningDeposit.tsx: flow — sat input → mint quote → render BOLT-11 + QR + "Pay with Alby" button (webln.sendPayment) → poll quote status → call mint to issue blinded outputs → update wallet store.

    src/components/wallet/LightningWithdraw.tsx: flow — sat input → input/generate BOLT-11 → mint melt quote → submit proofs → mint pays → success toast.

    Test files: signer.test.ts (Tests 1-3, vitest + jsdom mocks of window.nostr). mint-client.test.ts (Tests 4-5, vitest + vi.spyOn(globalThis, 'fetch') — no msw dep).
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun install && bun run test && bun run typecheck; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun run test passes ≥6 named tests across signer.test.ts + mint-client.test.ts
    - bun run typecheck exits 0
    - grep -E "window.nostr" apps/hunch-web/src/lib/nostr/nip07.ts matches
    - grep -E "bunker://" apps/hunch-web/src/lib/nostr/nip46.ts matches
    - grep -E "verifyDleq" apps/hunch-web/src/lib/cashu/tokens.ts matches (NUT-12 mandatory)
    - grep -rE "generatePrivateKey|generateSecretKey" apps/hunch-web/src returns empty (no auto-key-gen per CONTEXT.md)
    - grep -E "window.webln|webln.enable" apps/hunch-web/src/lib/lightning/webln.ts matches
  </acceptance_criteria>
  <done>NIP-07 default + NIP-46 alternate auth; cashu-ts mint client + NUT-CTF; WebLN deposit + withdraw wired to mint HTTP API; no auto-key-gen.</done>
</task>

<task type="auto">
  <name>Task 3: Browse + market detail + bet flow + resolution (UI-06, UI-07, UI-08, UI-09, UI-10, UI-11)</name>
  <files>apps/hunch-web/src/app/onboarding/page.tsx, apps/hunch-web/src/app/browse/page.tsx, apps/hunch-web/src/app/market/[id]/page.tsx, apps/hunch-web/src/components/market/MarketCard.tsx, apps/hunch-web/src/components/market/MarketChart.tsx, apps/hunch-web/src/components/market/BuySellBox.tsx, apps/hunch-web/src/components/market/ResolutionPanel.tsx, apps/hunch-web/src/components/market/OracleBadge.tsx, apps/hunch-web/src/lib/filter/social-graph.ts, apps/hunch-web/src/lib/filter/mute-list.ts, apps/hunch-web/src/hooks/use-markets.ts, apps/hunch-web/src/lib/filter/social-graph.test.ts</files>
  <read_first>
    - docs/HIP-1.md (kind:30888 schema + tags)
    - docs/HIP-2.md (DLC outcomes YES/NO/INVALID rendered in resolution UI)
    - docs/HIP-5.md (reputation event format — OracleBadge content)
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (Anti-Spam Pattern — default ON 1-2 hop filter, INVALID wiring)
    - https://github.com/nostr-dev-kit/ndk (replaceable kind:30888 subscription patterns)
    - apps/hunch-web/src/lib/types/market.ts (Task 2 — MarketEvent)
    - apps/hunch-web/src/lib/cashu/mint-client.ts (Task 2 — requestConditionalMint)
  </read_first>
  <action>
    src/app/onboarding/page.tsx (UI-06): step-by-step modal explainer:
    1. "Hunch is a protocol, not a product." (no Hunch custody)
    2. "No KYC. You self-custody." (Nostr pubkey = identity)
    3. "Multi-oracle marketplace." (link to /oracles)
    4. "Install Alby or use NIP-46." (→ WalletConnect modal)
    5. "Phase 2 = signet only. Terms are draft, counsel-pending." (inline PendingCounselNotice)
    Persists hasCompletedOnboarding=true in localStorage; redirects to /browse on completion.

    src/lib/filter/social-graph.ts (UI-07 default-on): async function loadFollowGraph(viewerPubkey, hops: 1 | 2 = 2): Promise<Set<string>> queries NDK kind:30000 contact lists of viewer + (hops=2) friends-of-viewer. Returns Set. function filterMarketsBySocialGraph(markets, graph): MarketEvent[] returns only markets whose creator ∈ graph. SocialGraphFilter defaults to enabled; toggle in settings (Task 4).

    src/lib/filter/mute-list.ts: loadMuteList(pubkey) reads NIP-51 kind:10000 mute list; filterMarketsByMute(markets, muteList) filters muted creators + topic keywords.

    src/hooks/use-markets.ts: returns { markets, loading } subscribing to NDK kind:30888 + applies socialGraph + mute filters in sequence. Pagination via NDK since.

    src/app/browse/page.tsx (UI-07): tabs for Categories (politics/sport/crypto/culture/all), search bar, trending sort (CURATE-03 — min-participants threshold; under-threshold shown in "All" only). Renders MarketCard grid. Filter toggle (default ON; disable from settings).

    src/components/market/MarketCard.tsx: card showing question + oracle badge + current YES price (placeholder "Live" until orderbook lands) + expiry + category.

    src/app/market/[id]/page.tsx (UI-08): full detail. Sections: Question + outcome rule + oracle badge → MarketChart (recharts YES-price time-series; v1 derived from accepted swap proofs over time; empty state otherwise) → BuySellBox → ResolutionPanel.

    src/components/market/OracleBadge.tsx: oracle npub + reputation score (kind:30891 aggregate) + link to /oracles/[pubkey].

    src/components/market/BuySellBox.tsx (UI-09 + UI-10):
    - Buy YES / Buy NO buttons + sat amount input
    - Submit: walletStore.deposit() if insufficient → mintClient.requestConditionalMint(amount, { oraclePubkey, eventId, outcome: 'YES' | 'NO' }) → conditional token added to wallet
    - Sell tab: publishes kind:38888 atomic-swap order on Nostr OR POSTs to /v1/swap (toggle)
    - All flows show DLEQ verification status (icon ✓ / ✗)

    src/components/market/ResolutionPanel.tsx (UI-11): subscribes via NDK to kind:89 attestations filtered by ["#e", marketId] + ["#p", oraclePubkey]. Renders: outcome badge (YES/NO/INVALID) + "Signature verified ✓" (in-browser Schnorr via @noble/curves) + link to on-chain settlement tx on mempool.space signet + "Verify yourself" link to HIP-2 settlement-verification doc.

    src/components/market/MarketChart.tsx: recharts LineChart YES price vs time; empty state "No trades yet".

    social-graph.test.ts: vitest covers loadFollowGraph (1 hop), 2-hop expansion (mocks 2 layers of kind:30000 via fake NDK), filter correctness, default-ON behavior.

    Add @noble/curves to apps/hunch-web/package.json deps for in-browser Schnorr verify.
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun install && bun run test -- social-graph && bun run typecheck && bun run build; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun run test -- social-graph passes ≥3 named tests
    - bun run typecheck exits 0
    - bun run build exits 0; out/browse/index.html + out/onboarding/index.html exist
    - grep -E "kind.*30888|KIND_MARKET|30888" apps/hunch-web/src/hooks/use-markets.ts matches
    - grep -E "kind.*89|KIND_ORACLE_ATTESTATION" apps/hunch-web/src/components/market/ResolutionPanel.tsx matches
    - grep -E "INVALID" apps/hunch-web/src/components/market/ResolutionPanel.tsx matches (HIP-2 invalid rendered)
    - grep -E "default.*true|defaultOn|enabled:.*true" apps/hunch-web/src/lib/filter/social-graph.ts matches
    - grep -E "@noble/curves" apps/hunch-web/package.json matches
  </acceptance_criteria>
  <done>Browse + market detail + bet (buy/sell) + resolution flows working; default-ON social graph filter; in-browser Schnorr verification on resolution; conditional token DLEQ verification on every receive.</done>
</task>

<task type="auto">
  <name>Task 4: Create market + account + oracle marketplace + settings (UI-12, UI-13, UI-14, UI-15)</name>
  <files>apps/hunch-web/src/app/market/create/page.tsx, apps/hunch-web/src/app/account/page.tsx, apps/hunch-web/src/app/oracles/page.tsx, apps/hunch-web/src/app/oracles/[pubkey]/page.tsx, apps/hunch-web/src/app/settings/page.tsx, apps/hunch-web/src/components/market/CreateMarketForm.tsx, apps/hunch-web/src/components/account/Portfolio.tsx, apps/hunch-web/src/components/account/PositionRow.tsx, apps/hunch-web/src/components/oracle/OracleCard.tsx, apps/hunch-web/src/components/oracle/AttestationHistory.tsx, apps/hunch-web/src/hooks/use-oracle.ts, apps/hunch-web/src/hooks/use-positions.ts</files>
  <read_first>
    - docs/HIP-1.md (kind:30888 + kind:88 schemas)
    - docs/HIP-5.md (kind:30891 aggregation rules)
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (distribution + pseudonymity decisions)
    - apps/hunch-web/src/lib/cashu/tokens.ts (Task 2)
    - apps/hunch-web/src/lib/types/market.ts (Task 2)
    - crates/hunch-protocol/src/kinds.rs
  </read_first>
  <action>
    src/app/market/create/page.tsx (UI-12): form fields: Question (textarea, ≤280 chars), Outcome rule (textarea — natural language, persisted as tag), Oracle pubkey (select from kind:88 announce list OR paste arbitrary npub — protocol-neutral), Expiry (datetime picker, default +30 days), Category (select), Mint URL (default relay.hunch.markets, overridable). Submit: builds kind:30888 event with d-tag = UUIDv4, tags ["oracle", pubkey], ["expiry", iso], ["outcome_rule", text], ["category", cat], ["mint", url]. Signs via auth store. Publishes via NDK. Toast on success with link to /market/[id].

    src/components/market/CreateMarketForm.tsx: the form component.

    src/hooks/use-positions.ts: returns { positions, pnl, txHistory }. Reads wallet store proofs + cross-references each conditional token's condition.eventId against kind:30888 events for market metadata. P&L = current implied YES price × position - cost basis.

    src/app/account/page.tsx (UI-13): three tabs:
    - Positions: Portfolio lists open positions across markets, grouped by market, with P&L
    - History: chronological mint/melt/swap from wallet + Nostr
    - Identity: current npub + signer kind + ReputationForm (Task 5) prompt

    src/components/account/Portfolio.tsx: list of PositionRow.

    src/components/account/PositionRow.tsx: market question + position size sats + outcome (YES/NO) + implied current price + P&L. Link to market detail.

    src/hooks/use-oracle.ts: useOracle(pubkey) returns oracle announce (kind:88) + attestation history (kind:89 filter) + reputation aggregate (kind:30891 filter).

    src/app/oracles/page.tsx (UI-14): grid of OracleCard for known oracles (kind:88 subscribers). Sort by reputation. Cards link to /oracles/[pubkey].

    src/components/oracle/OracleCard.tsx: name (from NIP-01 kind:0 metadata) + npub + reputation score (vouches - reports) + #attestations + relays.

    src/app/oracles/[pubkey]/page.tsx: oracle detail — about, reputation events list, full attestation history.

    src/components/oracle/AttestationHistory.tsx: paginated list of kind:89 events with outcome + market link + Schnorr verify badge.

    src/app/settings/page.tsx (UI-15) sections:
    - Mute list: input pubkey/topic, list current mutes, remove. Persists via NIP-51 kind:10000.
    - Relays: editable list, default ["wss://relay.hunch.markets", "wss://relay.damus.io", "wss://nos.lol"]; saves to NDK + kind:10002 NIP-65 outbox event.
    - Mints: editable list, default ["https://mint.hunch.markets"]; persisted to localStorage.
    - Filter: toggle social-graph filter default (CONTEXT.md item 6 — default ON; user can disable).
    - Theme: locked dark (UI-02; structure future-ready for light in v2).
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun run typecheck && bun run build && test -d out/account && test -d out/oracles && test -d out/settings && test -d out/market/create; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun run typecheck exits 0
    - bun run build exits 0
    - All 4 route exports exist in out/: account, oracles, settings, market/create
    - grep -E "30888" apps/hunch-web/src/components/market/CreateMarketForm.tsx matches
    - grep -E "10000" apps/hunch-web/src/app/settings/page.tsx matches (NIP-51 mute list)
    - grep -E "10002|NIP-65" apps/hunch-web/src/app/settings/page.tsx matches (NIP-65 outbox)
    - grep -E "permissionless|anyone.*create" apps/hunch-web/src/app/market/create/page.tsx matches (protocol neutrality)
  </acceptance_criteria>
  <done>Permissionless market creation + portfolio + oracle marketplace + settings all functional; NIP-51 mute lists + NIP-65 outbox published from settings.</done>
</task>

<task type="auto">
  <name>Task 5: Reputation publishing + i18n + Tor compatibility + PWA (UI-16, UI-17, UI-18, UI-19)</name>
  <files>apps/hunch-web/src/components/reputation/ReputationForm.tsx, apps/hunch-web/src/i18n/config.ts, apps/hunch-web/src/i18n/en.json, apps/hunch-web/src/i18n/fr.json, apps/hunch-web/src/lib/i18n.ts, apps/hunch-web/src/hooks/use-tor-detection.ts, apps/hunch-web/tests/e2e/tor-compatibility.spec.ts, apps/hunch-web/tests/e2e/onboarding.spec.ts, apps/hunch-web/public/sw.js</files>
  <read_first>
    - docs/HIP-5.md (kind:30891 schema — ReputationForm source of truth)
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (specifics — i18n EN+FR, PWA install)
    - https://2019.www.torproject.org/projects/torbrowser/design (Tor fingerprint surface)
    - apps/hunch-web/src/state/auth.ts (Task 2 — signer)
  </read_first>
  <action>
    src/components/reputation/ReputationForm.tsx (UI-16): form with target type radio (oracle / market / creator) + target pubkey/id + action radio (vouch / report) + score (-3..+3) + evidence URL (optional) + free-text comment. Submit: kind:30891 event per HIP-5 with tags ["target_type", t], ["target", id], ["action", "vouch"|"report"], ["score", n], optional ["evidence", url], content = comment. Signs + publishes via NDK. Embedded on OracleDetail + MarketDetail + Account pages.

    src/i18n/config.ts: export const LOCALES = ['en', 'fr'] as const; export const DEFAULT_LOCALE = 'en'. Picker stored localStorage hunch.locale; initial from navigator.language first segment.

    src/i18n/en.json + src/i18n/fr.json: flat key-value JSON ≥50 keys each — nav labels, button texts, form labels, onboarding copy, error messages, confirm dialogs. French uses proper terminology (parier, résolution, oracle, marché, carnet d'ordres).

    src/lib/i18n.ts: export function t(key, vars?): string — locale → key lookup, EN fallback if missing, {var} substitution. Lightweight (no next-intl dep).

    Wire t() into all visible strings across Tasks 1-4 components (this task explicitly migrates existing components).

    src/hooks/use-tor-detection.ts (UI-18): isTorBrowser(): boolean — detects: (a) window.navigator.userAgent includes "Tor"; (b) screen resolution round (anti-fingerprint default); (c) Intl.DateTimeFormat().resolvedOptions().timeZone === 'UTC'. Used for behavior tweaks (longer relay timeouts) — NOT to disable features (all flows must work over Tor per UI-18).

    Tor verification: tests/e2e/tor-compatibility.spec.ts — Playwright launches context with Tor-like settings (UA override, timezone UTC, no WebRTC) and walks onboarding + browse + market detail asserting no broken paths. No actual Tor network needed — simulates fingerprint.

    public/sw.js (PWA — UI-19): minimal service worker:
    - Cache-first for static (/_next/static/*, /fonts/*)
    - Network-first for HTML routes
    - Skip cache for Nostr / Cashu API calls
    - Registered via inline script in src/app/layout.tsx (navigator.serviceWorker.register('/sw.js')) only when process.env.NODE_ENV === 'production'.

    PWA install prompt: BeforeInstallPromptEvent listener in Shell.tsx surfaces one-time toast "Install Hunch as app" → e.prompt().

    tests/e2e/onboarding.spec.ts: full onboarding modal sequence + WalletConnect open → close (without connecting), assert localStorage hasCompletedOnboarding set, redirect to /browse.
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun run typecheck && bun run build && bun run test:e2e -- --project=chromium tests/e2e/onboarding.spec.ts tests/e2e/tor-compatibility.spec.ts; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun run typecheck exits 0
    - bun run build exits 0
    - Playwright onboarding + tor-compatibility specs pass (chromium)
    - wc -l apps/hunch-web/src/i18n/en.json apps/hunch-web/src/i18n/fr.json — both ≥50 lines
    - grep -E "30891" apps/hunch-web/src/components/reputation/ReputationForm.tsx matches
    - grep -E "serviceWorker.register" apps/hunch-web/src/app/layout.tsx matches
    - grep -E "isTorBrowser" apps/hunch-web/src/hooks/use-tor-detection.ts matches
    - apps/hunch-web/public/sw.js exists
  </acceptance_criteria>
  <done>Reputation form publishes kind:30891 per HIP-5; EN + FR locales cover all surface strings; Tor Browser compatibility verified; PWA install prompt + service worker active.</done>
</task>

<task type="auto">
  <name>Task 6: Geo-block US + ToS gate + legal pages + self-hosting guide (UI-20, UI-21)</name>
  <files>apps/hunch-web/src/app/blocked/page.tsx, apps/hunch-web/src/components/geo/GeoBlockGate.tsx, apps/hunch-web/src/lib/geo/ip-check.ts, apps/hunch-web/src/lib/geo/tor-exits.ts, apps/hunch-web/src/lib/geo/tor-exits.json, apps/hunch-web/src/lib/geo/ip-check.test.ts, apps/hunch-web/src/components/legal/TermsGate.tsx, apps/hunch-web/src/components/legal/PendingCounselNotice.tsx, apps/hunch-web/src/app/legal/terms/page.tsx, apps/hunch-web/src/app/legal/privacy/page.tsx, apps/hunch-web/tests/e2e/bet-flow.spec.ts, apps/hunch-web/tests/e2e/geo-block.spec.ts, apps/hunch-web/docs/SELF_HOSTING.md, apps/hunch-web/Dockerfile, apps/hunch-web/docker-compose.yml, apps/hunch-web/scripts/fetch-tor-exits.ts</files>
  <read_first>
    - docs/legal/TERMS.md (Phase 1 strawman — surfaced verbatim with PendingCounselNotice)
    - docs/legal/PRIVACY.md (Phase 1 strawman)
    - .planning/phases/02-mainnet-spine/02-CONTEXT.md (item 10 — strawmen sufficient for Phase 2; success criterion #5 amendment)
    - .planning/phases/01-cypherpunk-foundation/01-CONTEXT.md (geo-block decision)
    - CLAUDE.md (Geo-block US — principle 3)
    - https://check.torproject.org/exit-addresses (Tor exit list source — cached locally at build)
  </read_first>
  <action>
    src/lib/geo/ip-check.ts: async function getCountryCode(): Promise<string | null>. PRIMARY path: Cloudflare Pages injects cf-ipcountry via Cloudflare Functions middleware (functions/_middleware.ts emits it as a meta tag at edge; client reads). FALLBACK: if header absent (Tor, self-host, direct), fall through to Tor exit check (no third-party IP lookup — preserves privacy). Doc-comment explicitly anchors: "No third-party IP-lookup services used by default — privacy-preserving by design. Self-hosts can configure their own GeoIP if needed."

    src/lib/geo/tor-exits.ts: reads bundled JSON at src/lib/geo/tor-exits.json (fetched at build time by scripts/fetch-tor-exits.ts from check.torproject.org/exit-addresses). isTorExit(ip): boolean checks against list. Note: Tor exits not US-specific — geo-block triggers ONLY when (a) IP→US AND (b) NOT Tor exit. Tor users get ToS gate, not IP block.

    src/lib/geo/tor-exits.json: initial bundled list (placeholder with 5-10 known exits for dev — production cron updates weekly).

    scripts/fetch-tor-exits.ts: Bun script fetching exit list + writing tor-exits.json. Documented in SELF_HOSTING.md as weekly cron.

    src/lib/geo/ip-check.test.ts: vitest covers Cloudflare header path, Tor exit detection, US block decision matrix.

    src/components/geo/GeoBlockGate.tsx: client component mounted in Shell.tsx. On first render: getCountryCode(); if "US", router.push('/blocked'); if Tor exit, surfaces ToS notice but does NOT block (Tor users get ToS gate, not IP block — protocol neutrality).

    src/app/blocked/page.tsx: shown to geo-blocked. Explains: "Hunch is not available in the United States. The protocol is open-source — anyone can host a frontend in any jurisdiction. Links to GitHub + Radicle source mirrors." NO bypass option.

    src/components/legal/TermsGate.tsx: first-visit modal. localStorage hunch.tosAcceptedAt not present → modal blocks navigation. Body summarizes TERMS + PRIVACY key points (no KYC, no US, self-custody, oracle marketplace, signet-only Phase 2, draft-counsel-pending) + PendingCounselNotice. Two buttons: "I understand and accept" (sets localStorage timestamp + signer pubkey if connected) + "Leave" (window.location to https://bitcoin.org).

    src/components/legal/PendingCounselNotice.tsx: banner: "These terms are draft strawmen pending counsel sign-off (Phase 1+ followup per docs/legal/PHASE-1-FOLLOWUP.md). Use at your own risk. Phase 2 ships to signet only — no mainnet exposure." Reused in Footer, TermsGate, /legal/* pages. Anchors CONTEXT.md item 10.

    src/app/legal/terms/page.tsx: renders docs/legal/TERMS.md content as MDX (or marked → static HTML). Top with PendingCounselNotice.

    src/app/legal/privacy/page.tsx: same for PRIVACY.md.

    tests/e2e/bet-flow.spec.ts (full happy path):
    1. Visit / → /onboarding (first visit) → ToS gate visible
    2. Accept ToS → /browse
    3. Connect mock NIP-07 signer (Playwright window.nostr injection)
    4. Click first market → /market/[id]
    5. Open BuySellBox → enter 1000 sats → simulate WebLN pay (mock) → assert conditional token added to wallet store
    6. Navigate /account → assert position visible
    7. Submit reputation event (mock relay write) → assert kind:30891 event published

    tests/e2e/geo-block.spec.ts: simulate cf-ipcountry: US → assert redirect to /blocked. Simulate cf-ipcountry: CH → assert no redirect.

    apps/hunch-web/docs/SELF_HOSTING.md (UI-21 — ≥60 lines):
    1. Prerequisites (Bun ≥1.1, Node ≥20 fallback, 1GB RAM)
    2. Clone (git clone https://github.com/Silexperience210/hunch)
    3. Build (cd apps/hunch-web && bun install && bun run build)
    4. Deploy options:
       - Cloudflare Pages: bunx wrangler pages deploy out (config snippet)
       - Tor hidden service: cp out/* /var/www/hunch/, torrc snippet HiddenServiceDir
       - IPFS pin: ipfs add -r out/ → Pinata or web3.storage
       - Plain static host (Caddy / Nginx): Caddyfile / nginx.conf snippet
    5. Override defaults (custom mint URL, relays, locale) via env at build time
    6. Update Tor exit list cron (weekly bun run scripts/fetch-tor-exits.ts)
    7. Pseudonymity reminder: register domain via Njalla
    8. Geo-block: Cloudflare Workers middleware path OR self-hosted GeoIP
    9. Troubleshooting (CORS, NDK relay timeouts, WebLN missing)
    10. Audit hold: "SIGNET ONLY in Phase 2 — do not connect to mainnet mint URLs"

    apps/hunch-web/Dockerfile: multi-stage oven/bun:1.1 build → caddy:2 final stage serving /out.

    apps/hunch-web/docker-compose.yml: single service hunch-web exposing port 8000 (local self-host testing).
  </action>
  <verify>
    <automated>cd apps/hunch-web && bun run test -- ip-check && bun run build && bun run test:e2e -- tests/e2e/bet-flow.spec.ts tests/e2e/geo-block.spec.ts && wc -l docs/SELF_HOSTING.md | awk '{exit ($1 >= 60) ? 0 : 1}' && docker compose -f docker-compose.yml config; cd ../..</automated>
  </verify>
  <acceptance_criteria>
    - bun run test -- ip-check passes
    - bun run build exits 0
    - Playwright bet-flow.spec.ts + geo-block.spec.ts pass
    - apps/hunch-web/docs/SELF_HOSTING.md ≥60 lines
    - docker compose -f apps/hunch-web/docker-compose.yml config exits 0
    - grep -E "cf-ipcountry|cfipcountry" apps/hunch-web/src/lib/geo/ip-check.ts matches
    - grep -E "SIGNET ONLY|signet-only" apps/hunch-web/docs/SELF_HOSTING.md matches
    - grep -E "counsel" apps/hunch-web/src/components/legal/PendingCounselNotice.tsx matches
    - grep -E "/blocked" apps/hunch-web/src/components/geo/GeoBlockGate.tsx matches
    - grep -rE "ipapi|ipinfo|ip-api" apps/hunch-web/src returns empty (no third-party IP lookup by default per privacy anchor)
  </acceptance_criteria>
  <done>Geo-block US functional (cf-ipcountry + Tor exit list, no third-party IP lookup); ToS gate blocks first-visit nav; legal pages surface Phase 1 strawmen with PendingCounselNotice; self-hosting guide enables <1h deploy; full bet-flow E2E + geo-block E2E pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Checkpoint — frontend E2E + signet bet flow + Tor compatibility sign-off</name>
  <what-built>
    apps/hunch-web Next.js 15 frontend — static export, cyberpunk dark theme, NIP-07 + NIP-46 auth, cashu-ts mint client, WebLN deposit/withdraw, full bet flow (browse → market detail → buy/sell → resolution → portfolio), permissionless market creation, oracle marketplace, EN+FR i18n, PWA install, Tor compatibility, geo-block US + ToS gate, self-hosting guide. All 21 UI requirements addressed.
  </what-built>
  <how-to-verify>
    1. cd apps/hunch-web && bun install && bun run typecheck && bun run lint && bun run build exits 0
    2. apps/hunch-web/out/ contains index.html, browse/, market/create/, account/, oracles/, settings/, blocked/, legal/terms/, legal/privacy/, onboarding/
    3. bun run test (vitest) passes all unit + integration tests
    4. bun run test:e2e (Playwright chromium) passes onboarding + bet-flow + tor-compatibility + geo-block specs
    5. Start dev server: bun run dev. Visit http://localhost:3000 → ToS gate visible → accept → /browse loads with empty state (no markets yet — expected pre-Plan 02-03 distribution)
    6. With docker-compose stack from Plan 02-01 running locally, run full signet bet flow manually:
       a. Connect NIP-07 (install Alby or test extension)
       b. Deposit 50k sats via Mutinynet faucet → mint quote → pay → tokens visible
       c. /market/create → publish a test market
       d. /market/[id] → Buy YES 10k sats → conditional token issued
       e. Trigger oracle attestation (docker compose exec oracle hunch-oracle attest --event-id <id> --outcome YES)
       f. /market/[id] ResolutionPanel shows attestation with verified ✓ badge
       g. /account → position appears with realized P&L
    7. Visit / under Tor Browser (or simulated): confirm no broken flows, no third-party requests in dev console
    8. Simulate cf-ipcountry: US (browser devtools header override) → /blocked page renders
    9. Confirm /legal/terms + /legal/privacy show PendingCounselNotice + content from Phase 1 strawmen
    10. grep -rE "(Alexandre|Cangini|alexandre\.cangini)" apps/hunch-web returns empty (pseudonymity preserved)
    11. grep -rE "googletagmanager|google-analytics|posthog|sentry|fullstory|googleapis" apps/hunch-web returns empty (no trackers, no Google Fonts)
    12. Read apps/hunch-web/docs/SELF_HOSTING.md — confirm a self-hoster could complete deploy in <1 hour
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures (which step number).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → mint HTTP | Frontend → Plan 02-01 mint REST API (NUT requests, conditional swap proposals) |
| browser → relay WSS | NDK websocket to nostr-rs-relay (publish kind:30888, kind:30891, kind:10000, kind:10002) |
| browser → WebLN provider | window.webln (Alby / Mutiny / Phoenix) signs Lightning payments; user trust |
| browser → NIP-46 bunker | Nostr Connect remote signer (Amber, nsec.app); user trust |
| ToS gate / geo-block | Frontend self-imposes restriction; trivially bypassable by self-host — protocol stays neutral |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-F-01 | S (Spoof) | NIP-07 extension impersonation | accept | trust boundary owned by user's chosen browser extension; documented in onboarding (install Alby from official source) |
| T-02-F-02 | S (Spoof) | NIP-46 bunker MITM | accept | trust boundary owned by user's chosen remote signer; bunker URI displayed to user; documented |
| T-02-F-03 | S (Spoof) | malicious mint URL injection | mitigate | settings mint list is user-managed; default is relay.hunch.markets; warning toast when adding new mint; DLEQ verify on every receive catches forged tokens |
| T-02-F-04 | T (Tamper) | conditional token tampering in transit | mitigate | verifyDleqOrThrow on every receive (cashu-ts NUT-12) per CLAUDE.md "Do verify Cashu DLEQ proofs on every token receive" |
| T-02-F-05 | T (Tamper) | oracle attestation tampering | mitigate | in-browser Schnorr verify via @noble/curves in ResolutionPanel; failed sig shows ✗ badge + warning |
| T-02-F-06 | T (Tamper) | market event tampering | mitigate | NDK auto-verifies event sigs at receive; tampered events rejected before reaching UI |
| T-02-F-07 | R (Repudiation) | user position dispute | mitigate | all positions backed by signed conditional tokens stored locally; user can prove ownership by holding the secret |
| T-02-F-08 | I (Info leak) | analytics / fingerprinting | mitigate | zero third-party trackers, self-hosted fonts (no Google Fonts), no IP-lookup services by default — see ip-check.ts doc-comment + acceptance criteria |
| T-02-F-09 | I (Info leak) | Tor user deanonymization | mitigate | useTorDetection adjusts behavior (longer timeouts, no WebRTC); no third-party API calls regardless of Tor state |
| T-02-F-10 | I (Info leak) | localStorage leakage | accept | proofs in localStorage are required for wallet UX; user told in onboarding "your browser is your wallet" |
| T-02-F-11 | D (DoS) | NDK relay flooding | accept | NDK handles relay rate-limiting; user can swap relays in settings |
| T-02-F-12 | D (DoS) | malicious market spam in browse | mitigate | default-ON social graph filter (1-2 hop) + NIP-51 mute lists + new-creator gradual visibility (CURATE-07, implemented in Plan 02-03) |
| T-02-F-13 | E (Escalation) | US user bypassing geo-block | accept | geo-block is best-effort ToS-enforcement at frontend layer; protocol stays neutral; documented in /legal/terms |
| T-02-F-14 | E (Escalation) | ToS gate localStorage bypass | accept | ToS acceptance is consent record, not access control; documented strawman pending counsel signoff |
| T-02-F-15 | S (Spoof) | malicious shadcn / NDK / cashu-ts dependency | mitigate | Bun lockfile pinned in CI; supply-chain audit deferred to Phase 3 SEC-06 per CONTEXT.md audit-hold |
</threat_model>

<verification>
- bun run typecheck + bun run lint + bun run build all exit 0
- bun run test passes all vitest tests
- bun run test:e2e passes onboarding + bet-flow + tor-compatibility + geo-block specs
- apps/hunch-web/out/ contains all 12 routes as static HTML
- Self-hosting guide ≥60 lines and references SIGNET-ONLY anchor
- Zero real-name leakage (grep returns empty)
- Zero third-party trackers / Google Fonts / IP-lookup services (grep returns empty)
- All 21 UI requirements demonstrably addressed (artifact + key_link traceability)
</verification>

<success_criteria>
Phase success criterion #1 (E2E signet flow user-visible, joint with Plan 02-01):
1. Onboarding + ToS gate + WalletConnect flow lands user at /browse with NIP-07 connected
2. Lightning deposit via WebLN credits wallet store
3. Bet via BuySellBox issues NUT-CTF conditional token (DLEQ verified)
4. Oracle attestation rendered in ResolutionPanel with Schnorr verify ✓
5. Account portfolio shows realized P&L after settlement

Phase success criterion #4 (anti-spam working — frontend layer):
- Default-ON social graph filter hides unfollowed creators
- NIP-51 mute lists work
- INVALID outcome renders correctly in ResolutionPanel
- New-creator gradual visibility deferred to Plan 02-03

Phase success criterion #5 (geo-block + ToS gate functional):
- /blocked renders for US users
- ToS gate blocks first-visit nav until accepted
- PendingCounselNotice anchors CONTEXT.md item 10

Phase success criterion #2 (3 frontend mirrors) — Plan 02-03 deploys; this plan delivers the buildable static bundle.
</success_criteria>

<output>
After completion, create .planning/phases/02-mainnet-spine/02-02-SUMMARY.md per template.
</output>
